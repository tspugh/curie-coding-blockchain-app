# Local MVP Loop — SUPERSEDED

This loop prompt orchestrated overnight CTO work on the `icd-10-parser` pipeline:
Bedrock Titan embeddings, Comprehend Medical NER, pgvector/RDS retrieval, SQS queue,
Rust Lambda workers, and the Linear board for that workstream.

Archived to: `docs/raw/archived/loop-prompts-local-mvp.md`

A replacement orchestration prompt for the Somnia chain-native build should target
`somnia-agent-kit` contract deployment, `AgentRegistry`/`AgentExecutor` wiring, and
the insurance↔hospital claim adjudication flow — not AWS services or REST endpoints.
Write that prompt once the first Somnia prototype contracts are deployed.

---

**See also** — superseded. The active loop prompt is [[iteration-prompt]].
