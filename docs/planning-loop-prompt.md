# Research & Planning Loop Prompt — SUPERSEDED

This prompt orchestrated the research/planning loop for the icd-10-parser coder-UI
product: SQS queue design, pgvector/RDS schema, Comprehend Medical entity mapping,
Bedrock embedding models, AWS CDK stacks, and competitor analysis (Solventum, GaleAI,
CodaMetrix). The "decided stack" it recorded (Lambda, RDS, Glue, SQS, Comprehend
Medical, CDK) is entirely dead. The repository path it referenced
(`/home/thomaspugh/projects/icd-10-parser`) belongs to the prior codebase.

Archived to: `docs/raw/archived/planning-loop-prompt.md`

A replacement planning loop for the Somnia chain-native build should orient around
`somnia-agent-kit` contract roles (`AgentRegistry`, `AgentManager`, `AgentVault`,
`AgentExecutor`), the on-chain insurance↔hospital agreement and conditional fund-release
state machine, off-chain AI coding agent attestation, and the Curie encrypted-pub-sub
model. See `AGENTS.md` for current project direction.

---

**See also** — superseded. The active loop prompt is [[loop-prompts/iteration-prompt|iteration-prompt]].
