# Open Research Questions

Running backlog for the [`/research`](../../.claude/skills/research/SKILL.md) skill. Active items are picked up by future research iterations; investigated items move down to the historical section with a one-line answer and a link to the findings file.

Three tracks: **Market** (ICD-10 coding market + AI incumbents), **Agreement layer** (insurance↔hospital settlement on-chain), **Somnia** (chain-native architecture).

---

## Active (not yet investigated)

### Track 1 — Market

~~- How do AHIMA / AAPC certification rules constrain AI-only coding? Is there a CCA/CCS/CPC equivalent expected for AI agents, or a published guidance document? — added 2026-05-14, priority: medium~~ → investigated 2026-05-16 → [docs/research/regulatory/ahima-aapc-ai-coding-certification-oversight-requirements.md](regulatory/ahima-aapc-ai-coding-certification-oversight-requirements.md)
~~- Should cliqueue exclude Medicare Advantage risk-adjustment coding from Phase 1 scope — with a hard gate in `ClaimsAdjudicator` requiring `requiresHumanAttestation = true` for MA-billed claim types — given active DOJ enforcement targeting AI vendors that auto-code MA diagnoses? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/medicare-advantage-ma-exclusion-gate-phase1-scope.md](regulatory/medicare-advantage-ma-exclusion-gate-phase1-scope.md)
~~- Should the on-chain `blockedPayerIds` mapping use `bytes32` hashes of payer EDI IDs (preserving privacy of the specific MA plan) or store the raw EDI ID string — and does storing raw payer IDs on a public chain create any HIPAA or competitive-sensitivity concern? — added 2026-05-16, priority: medium~~ → investigated 2026-05-16 → [docs/research/regulatory/blockedpayerids-on-chain-privacy-design.md](regulatory/blockedpayerids-on-chain-privacy-design.md)
- Should cliqueue's hospital BAA include an explicit "Medicare Advantage Exclusion Exhibit" listing the Phase 1 scope limitation and the mechanism (payer ID blocklist) — giving hospital compliance teams a documented defense if an MA claim accidentally enters the system due to a missing entry in the blocklist? — added 2026-05-16, priority: high
- Should `BlockReason` be a typed `enum` in `IClaimsAdjudicator` (e.g., `MA_EXCLUSION = 1`, `CONTRACT_DISPUTE = 2`, `OTHER = 3`) — enabling auditors and Ormi subgraph definitions to decode reason codes in `PayerBlocked` events without off-chain documentation, and does publishing an on-chain reason code create any competitive-sensitivity concern beyond what the commitment hash already mitigates? — added 2026-05-16, priority: medium
- Should the deployment runbook include an annual CMS MA plan update procedure — subscribing to the CMS MA/Part D Contract and Enrollment Data annual release (published each January), computing new `keccak256(BLOCKLIST_DOMAIN || canonicalPayerId)` commitment hashes, and calling `blockPayer()` via `BLOCKLIST_ADMIN_ROLE` before the January effective date — so newly CMS-issued MA plans are blocked before any hospital can accidentally submit them? — added 2026-05-16, priority: high
- Should cliqueue define a canonical payer ID format specification (e.g., uppercase ASCII, trimmed, exactly 5 digits zero-padded for CMS IDs) and publish it in the hospital onboarding checklist — preventing different representations of the same payer (e.g., "12345" vs "12345 " vs "012345") from producing different hashes and silently bypassing the blocklist? — added 2026-05-16, priority: high
- Should `ClaimsAdjudicator` enforce canonical payer ID format in `blockPayer()` on-chain (e.g., `require(bytes(payerId).length == 5)` for CMS-style IDs) — or defer format validation to the off-chain deployment runbook, accepting the risk that a malformed ID silently fails to block the intended payer? — added 2026-05-16, priority: medium
- As cliqueue scales to Phase 2, what human attestation architecture (analogous to the inpatient DRG SBT gate) would satisfy the OIG MA ICPG "human clinical oversight and override capability" requirement for MA risk-adjustment diagnoses — enabling a Phase 2 MA expansion without autonomous coding? — added 2026-05-16, priority: medium
~~- Should cliqueue's hospital BAA include a "Payer Contract Attestation Exhibit" documenting that the SBT on-chain attestation satisfies Humana/Cigna's credentialed coder review contractual requirement — and has any healthcare attorney published analysis of whether on-chain attestation satisfies payer contract language requiring "validated by credentialed coders"? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/market/payer-contract-attestation-exhibit-on-chain-sbt.md](market/payer-contract-attestation-exhibit-on-chain-sbt.md)
- Should cliqueue commission an outside healthcare attorney to publish an opinion letter specifically analyzing whether an on-chain SBT attestation with a registered CCS/RHIA credential number satisfies typical Humana and Cigna payer contract language requiring "validated by credentialed coders" — and would publishing this opinion strengthen the go-to-market story for revenue cycle directors? — added 2026-05-16, priority: high
- Should the Payer Contract Attestation Exhibit be a one-page exhibit in the hospital BAA or a standalone legal document separate from the BAA — and does separating it from the BAA reduce cliqueue's exposure if the exhibit's representations later prove insufficient for a specific payer contract? — added 2026-05-16, priority: medium
- Should cliqueue's onboarding checklist explicitly flag Humana and Cigna as payers requiring the Attestation Exhibit — with a simplified workflow for hospitals contracting only with UHC, Aetna, or BCBS (where no published credentialed coder attestation contractual requirement exists as of May 2026)? — added 2026-05-16, priority: high
- Does the OIG February 2026 MA Compliance Guidance's "human-in-the-loop must be more than a formality" standard set a precedent that a future OIG GCPG update will extend the substantive review requirement to fee-for-service autonomous coding — and should cliqueue's SBTRegistry architecture plan for a regulatory extension that requires attestation for all claim types (not just inpatient DRG)? — added 2026-05-16, priority: medium
~~- What is the mean time-to-payment for a clean vs. denied-then-resubmitted claim, and how does that latency interact with the 90-day timely-filing window? — added 2026-05-14, priority: medium~~ → investigated 2026-05-17 → [docs/research/market/coding-market-size-and-denial-losses.md](market/coding-market-size-and-denial-losses.md)
~~- Is there a primary-source estimate of coding-error-attributable denied dollar value (not just count) as a share of the $262B total 2024 denied pool? — added 2026-05-14, priority: medium~~ → investigated 2026-05-17: coding errors confirmed as top-named denial reason (Premier 2024) but no national cost-fraction isolation exists; ~$52B estimate (~20% × $262B) remains best available approximation → [docs/research/market/coding-market-size-and-denial-losses.md](market/coding-market-size-and-denial-losses.md)
- Should cliqueue's ROI model for hospital procurement explicitly separate "coding-error denial rework savings" ($57.23 × coding-error denial count) from "RFI denial float reduction" ($14.4B annualized pipeline) — and publish a two-scenario ROI calculator in the hospital onboarding package? — added 2026-05-17, priority: high
~~- Does any payer companion guide (UHC, Aetna, Cigna, Humana) publish the adjudication-to-payment cycle time specifically for claims with electronic 275 attachments vs. paper/fax — enabling cliqueue to cite a full end-to-end comparison rather than the 5–7 day administrative matching window alone? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO payer companion guide or regulator publishes a days-to-payment benchmark isolating the 275 variable; adjudication clock is paused during documentation development (Medicare 45-day other-than-clean clock pauses; commercial state prompt pay clocks pause similarly) and resumes identically for electronic vs. paper receipt; CMS-0053-F $781.98M savings is labor-cost reduction ($22/transaction), not cycle time; correct ROI is documentation-pending window (60–120 days paper → 5–7 day matching window), not adjudication speed post-receipt → [docs/research/market/electronic-275-adjudication-to-payment-cycle-time.md](market/electronic-275-adjudication-to-payment-cycle-time.md)
- Should cliqueue's hospital ROI calculator present RFI denial value with a range ("60–120 days → 5–7 day payer matching window") rather than a point estimate — including a confidence qualifier noting that adjudication timeline after matching is payer-contract-dependent? — added 2026-05-17, priority: high
- Should cliqueue's hospital onboarding ROI calculator explicitly label the two components ("documentation-pending window: 60–120 days → 5–7 day matching window" and "post-receipt adjudication period: 14–45 days, payer-contract dependent, NOT reduced by electronic 275") — so hospital finance teams understand the sourced ROI boundary? — added 2026-05-17, priority: high
- Is there a private payer that publishes a specific adjudication SLA for claims submitted with a proactive unsolicited 275 (vs. claims where the payer must issue a 277 RFI) — enabling cliqueue to cite a post-receipt adjudication advantage for proactive unsolicited submission? — added 2026-05-17, priority: medium
- At what AR days threshold (e.g., claims >90 days outstanding due to documentation pending) does cliqueue's value proposition create a measurable Days-Sales-Outstanding improvement that hospital CFOs can calculate from their own benchmarks without relying on published national benchmarks? — added 2026-05-17, priority: medium
- Should the prior research file [[agreement-layer/rfi-denial-pathway-paauthash-on-chain-scope|rfi-denial-pathway-paauthash-on-chain-scope]] be formally corrected to update the "90-day paper" baseline to "60–120 days (Kodiak Solutions VP/FAH podcast)" and remove the unsourced "24-72h electronic" turnaround claim? — added 2026-05-17, priority: high
~~- Does cliqueue's on-chain `ClaimSubmitted` event plus `paAuthHash` attachment commitment directly address the RFI denial pathway — i.e., can a payer that receives an on-chain claim submission with a linked CDA attachment hash treat that as a complete attachment submission, eliminating the 90-day RFI delay? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO direct elimination — CMS-0053-F mandates solicited 277/275 workflow; payer retains right to issue 277 RFI even after proactive 275 submission; `paAuthHash` is a provenance anchor (eliminates "we never received it" disputes, not payer RFI rights); ROI framing corrected to "90-day paper → 24-72h electronic 277/275 acceleration post-May 2028" → [docs/research/agreement-layer/rfi-denial-pathway-paauthash-on-chain-scope.md](agreement-layer/rfi-denial-pathway-paauthash-on-chain-scope.md)
~~- Should cliqueue's ROI calculator present the RFI denial value as "277/275 response acceleration (90-day paper → 24-72h electronic)" rather than "eliminates RFI denials" — and is there a published HFMA or Kodiak benchmark for electronic vs. paper 275 attachment response turnaround time that quantifies this acceleration? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES to ROI calculator revision; CORRECTED FRAMING: "60–120 day paper/fax baseline" (Kodiak VP Matt Szaflarski, FAH podcast — 89% resolved but not until 60/90/120 days later) vs. "5–7 day electronic 275 payer matching window" (UHC/Anthem companion guides); NO primary source supports "24-72h electronic turnaround" — that claim must be removed; CMS-0053-F sets no mandatory payer response window → [docs/research/market/rfi-denial-cycle-time-benchmark-electronic-vs-paper.md](market/rfi-denial-cycle-time-benchmark-electronic-vs-paper.md)
- Should the hospital BAA's Settlement Finality Exhibit include a one-paragraph "Attachment Provenance Clause" documenting that the on-chain `paAuthHash` constitutes tamper-evident proof of documentation availability at submission time — creating an appeals record the hospital can reference if a payer issues a 277 after the `paAuthHash` timestamp? — added 2026-05-17, priority: high
- After CMS-0053-F's May 2028 compliance deadline, does a payer's failure to provide an electronic 277 request (if the payer is still using paper/fax) create an actionable HIPAA administrative simplification violation — and should cliqueue's hospital onboarding checklist include a step for hospitals to confirm their payers' CMS-0053-F compliance status before expecting the 24-72h electronic turnaround? — added 2026-05-17, priority: medium
- Is there a primary-source study that breaks down the $25.7B adjudication cost by denial reason (coding vs. PA vs. eligibility vs. RFI) — or is Premier's "clerical/data errors are top reason" qualitative statement the best available attribution? — added 2026-05-17, priority: medium
~~- Given AHIMA's explicit framework requiring human DRG reconciliation for inpatient claims, should cliqueue include a "human-attestor" agent role (certified RHIA/CCS coder signing on-chain) alongside the AI coding agent for inpatient claim types? — added 2026-05-14, priority: high~~ → investigated 2026-05-16 → [docs/research/market/inpatient-drg-human-attestor-architecture.md](market/inpatient-drg-human-attestor-architecture.md)
~~- Should `ClaimsAdjudicator` enforce a `claimType` field (inpatient/outpatient) at submission time that gates the `requiresHumanAttestation` flag — so the contract enforces the regulatory distinction automatically and a hospital cannot submit an inpatient DRG claim without a valid SBT holder signing? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/market/claimtype-enum-on-chain-enforcement-inpatient-outpatient.md](market/claimtype-enum-on-chain-enforcement-inpatient-outpatient.md)
~~- Should the `claimType` → `requiresHumanAttestation` mapping be hardcoded as an `immutable` constant or stored as a governance-controlled `mapping(uint8 claimType => bool)` behind the `TimelockController` — so that a future CMS regulation mandating outpatient attestation can be enabled without a contract upgrade? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/market/claimtype-attestation-governance-immutable-vs-timelock.md](market/claimtype-attestation-governance-immutable-vs-timelock.md)
~~- Should the `attestationRequired` governance mapping in `ClaimsAdjudicator` be initialized at deployment with all known TOB second-digit categories, or lazily default to `false` for unmapped `claimType` values (relying on a `ClaimTypeAdded` governance event to signal new entries)? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/market/attestation-required-mapping-initialization-tob-taxonomy.md](market/attestation-required-mapping-initialization-tob-taxonomy.md)
~~- Does Somnia's 1M-gas cold-SLOAD surcharge apply to a `mapping(uint8 => bool)` slot last written in a prior block but not yet read in the current block — and would batching multiple outpatient claim submissions in a single transaction warm the mapping slot after the first read, effectively amortizing the surcharge across the batch? — added 2026-05-16, priority: medium~~ → investigated 2026-05-16 → [docs/research/market/attestation-required-mapping-initialization-tob-taxonomy.md](market/attestation-required-mapping-initialization-tob-taxonomy.md)
- Should the hybrid attestation architecture (`immutable bool INPATIENT_REQUIRES_ATTESTATION = true` + governed `mapping(uint8 claimType => bool) attestationRequired` for outpatient and novel claim types) be documented as a formal spec amendment to `ClaimsAdjudicator` before the next feature branch regeneration? — added 2026-05-16, priority: high
~~- Should the `validClaimType` modifier in `ClaimsAdjudicator` check `claimType < CLAIM_TYPE_COUNT` (an immutable uint8 set at deployment) or maintain a separate `mapping(uint8 => bool) registeredClaimTypes` — and does the second approach have the same silent-bypass risk as the `attestationRequired` mapping itself? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/market/validclaimtype-modifier-digit9-governance-gap.md](market/validclaimtype-modifier-digit9-governance-gap.md)
~~- When CMS assigns new TOB second-digit codes under digit 9 ("Reserved for National Assignment"), does cliqueue's `TimelockController` governance path (minimum 48-hour delay) create a gap window where the new claim type cannot be submitted until governance activates it — and should the deployment runbook pre-register digit 9 as a `ClaimTypeAdded` placeholder with `attestationRequired = true` (conservative safe default for unknown types)? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/market/validclaimtype-modifier-digit9-governance-gap.md](market/validclaimtype-modifier-digit9-governance-gap.md)
- Should the `attestationRequired` mapping initialization values (all `false` at deployment except where governance mandates) be published in a cliqueue "Claim Type Policy" document that hospital procurement teams can review alongside the Settlement Token Policy? — added 2026-05-16, priority: medium
~~- Does Corti Symphony's MCP interface surface the claim category/TOB distinction — does the hospital pass `claimType` to Symphony, or must cliqueue's adapter infer it from the clinical note type — and does Symphony return ICD-10-PCS codes for inpatient vs. CPT codes for outpatient based on claim type? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/ai-models/corti-symphony-tob-claim-type-adapter.md](ai-models/corti-symphony-tob-claim-type-adapter.md)
- Should cliqueue's off-chain agent spec formally define a `TOB_TO_SYMPHONY_SYSTEM` mapping table as a standalone TypeScript constant and publish it in the adapter spec — so hospital integration engineers can verify and extend it for non-standard TOB categories (home health, hospice, swing bed) without touching the contract layer? — added 2026-05-16, priority: high
- Does Symphony's CDI endpoint use the same `system` array parameter as the standard coding endpoint — enabling cliqueue to reuse the same adapter function for pre-encounter CDI queries and final claim coding? — added 2026-05-16, priority: medium
- If a hospital submits a mixed-category 837I batch (both inpatient and outpatient claims), should cliqueue's adapter split by TOB category and make separate Symphony calls per claim, or can Symphony process a multi-system request with all four system values simultaneously — and does the response correctly label each code by source system? — added 2026-05-16, priority: medium
- Should the `ClaimsAdjudicator` enforce an `ICD10PCS_REQUIRED` flag for inpatient claims (requiring at least one ICD-10-PCS procedure code alongside ICD-10-CM diagnosis codes) as a second on-chain guard against TOB misreporting — and would this add gas cost to the inpatient submission path that exceeds acceptable thresholds? — added 2026-05-16, priority: medium
~~- Does the Corti Symphony MCP tool return a confidence score (0.0–1.0) per code alongside the code array — enabling the attestor's off-chain agent to programmatically decide whether the claim routes to human review before the on-chain commitment? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/ai-models/corti-symphony-confidence-score-and-routing.md](ai-models/corti-symphony-confidence-score-and-routing.md)
~~- Should cliqueue define a formal `CodingConfidenceHeuristic` specification — a weighted scoring function over `evidences.length`, `alternatives.length`, and `inCandidatesArray` — and publish it as a testable TypeScript module in the off-chain agent spec, so hospital deployments can calibrate thresholds against their own code mix? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/ai-models/coding-confidence-heuristic-spec.md](ai-models/coding-confidence-heuristic-spec.md)
~~- Should the `CodingConfidenceHeuristic` module version be anchored on-chain (e.g., as a `bytes32 heuristicVersion` field in the `ClaimSubmitted` event) — creating a permanent audit record of which routing version was used for each claim, inspectable by OIG auditors without accessing off-chain systems? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/heuristic-version-on-chain-anchoring.md](somnia/heuristic-version-on-chain-anchoring.md)
- Should cliqueue's hospital BAA include a Calibration Attestation Exhibit — a signed document stating the hospital's chosen `MIN_EVIDENCES` and `MAX_ALTERNATIVES` values and the validation study conducted before setting those values — to document that cliqueue is not the party that chose the routing threshold for that hospital's claim mix? — added 2026-05-16, priority: high
- Should `ClaimsAdjudicator` maintain an on-chain `heuristicVersionRegistry` mapping (`bytes32 hash → string versionString`) governed by the `TimelockController`, so that OIG auditors can decode human-readable version strings without off-chain lookups — and does adding this registry create a DASP risk under the GENIUS Act if version registration is treated as a material upgrade to the protocol? — added 2026-05-16, priority: medium
- Does Somnia's block explorer (or Ormi's indexer) support filtering `ClaimSubmitted` events by indexed `bytes32` topic — enabling cliqueue's audit tooling to query "all claims submitted with heuristic version v1.2.3" via a standard `eth_getLogs` filter without a separate off-chain index? — added 2026-05-16, priority: medium
- Is there a published calibration dataset (labeled confidence vs. human-override frequency) for Corti Symphony codes that would let cliqueue validate whether `evidences < 2` or `alternatives >= 3` correlates with human coder disagreement rates — or must cliqueue conduct its own validation study with a pilot hospital before publishing default thresholds? — added 2026-05-16, priority: medium
- Is there a published calibration dataset (labeled confidence vs. human-override frequency) for Symphony codes that would let cliqueue validate whether `evidences < 2` or `alternatives >= 3` correlates with human coder disagreement rates — or must cliqueue conduct its own validation study during pilot? — added 2026-05-16, priority: medium
- Should cliqueue request per-code confidence scores from Corti as an enterprise API addition during BAA negotiation — and is this a negotiating point that would change the routing architecture if granted? — added 2026-05-16, priority: medium
- Given Humana/Cigna's contractual requirement for credentialed coder attestation of AI-coded claims, should cliqueue market the SBTRegistry + on-chain attestation as a "payer contract compliance artifact" for revenue cycle directors — and should a one-page "Attestation Compliance Brief" be drafted for hospital onboarding? — added 2026-05-16, priority: medium
~~- The Change Healthcare breach exposed systemic clearinghouse concentration risk — is there a documented provider appetite for decentralized alternatives, or is the standard response simply adding a second clearinghouse backup (Waystar/Availity redundancy)? — added 2026-05-14, priority: medium~~ → investigated 2026-05-15 → [docs/research/market/change-healthcare-clearinghouse-concentration-and-decentralization-appetite.md](market/change-healthcare-clearinghouse-concentration-and-decentralization-appetite.md)
~~- Given that payer contract exclusivity clauses bind providers to specific clearinghouses (36% of AMA survey respondents after Change Healthcare), does cliqueue's on-chain settlement layer require explicit payer permission (or contract amendment) before a hospital can add it alongside its existing clearinghouse — and should the onboarding checklist include a payer-approval legal review step? — added 2026-05-15, priority: high~~ → investigated 2026-05-16 → [docs/research/market/payer-contract-exclusivity-permission-requirement.md](market/payer-contract-exclusivity-permission-requirement.md)
- Should cliqueue's onboarding documentation segment payer-specific risk (Anthem/Availity-gateway hospitals vs. UHC hospitals) and recommend different contract review scopes accordingly — reducing onboarding time for the majority of hospitals with flexible payer clearinghouse policies? — added 2026-05-16, priority: high
- Should cliqueue publish a one-page "Settlement Layer Non-Clearinghouse Determination Memo" (establishing the on-chain layer is not a healthcare clearinghouse and not a 837/835 EDI transaction) as a companion to the CAQH CORE Non-Applicability Memo in the hospital onboarding package? — added 2026-05-16, priority: medium
~~- Does the CAQH CORE operating-rules framework create an affirmative obligation to route all claim status and remittance through certified clearinghouses — or is an on-chain settlement layer that supplements (not replaces) the EDI rail categorically outside CORE's scope? — added 2026-05-15, priority: medium~~ → investigated 2026-05-15 → [docs/research/market/caqh-core-scope-and-payer-exclusivity-onboarding.md](market/caqh-core-scope-and-payer-exclusivity-onboarding.md)
- With TriZetto/Waystar also breached in 2025–2026, is there a viable early-adopter segment among the ~14% of providers willing to submit claims directly to payers (bypassing clearinghouses) — and how should cliqueue's go-to-market sequence target this segment before mainstream multi-clearinghouse adopters? — added 2026-05-15, priority: medium
~~- Do major payer provider contracts (Anthem, UHC, Aetna, Cigna, Humana) contain explicit "exclusive clearinghouse data sharing" clauses covering post-adjudication settlement data, or are such clauses limited to 837 claim submission routing — and has any healthcare attorney published analysis of this distinction? — added 2026-05-15, priority: high~~ → investigated 2026-05-16 → [docs/research/market/payer-contract-data-sharing-restrictions.md](market/payer-contract-data-sharing-restrictions.md)
- Should cliqueue publish a one-page "CAQH CORE Non-Applicability Memo" establishing that the on-chain settlement layer is not a HIPAA standard transaction and is not subject to CORE operating rules, as part of the hospital onboarding documentation package? — added 2026-05-15, priority: medium
- Does cliqueue's on-chain real-time settlement qualify as an "RTA process" under CAQH CORE Phase IV 450's definition — and does this RTA categorization simplify the regulatory framing for payer procurement teams reviewing the onboarding package? — added 2026-05-15, priority: medium
~~- Should cliqueue's hospital onboarding checklist include a one-paragraph CAA-21 gag clause defense memo — arguing that any payer contract clause restricting a hospital from sharing de-identified claims data with cliqueue as a business associate is unenforceable under the Consolidated Appropriations Act 2021, and that hospitals may report such clauses to DOL/HHS? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/caa21-gag-clause-provider-vendor-data-sharing.md](regulatory/caa21-gag-clause-provider-vendor-data-sharing.md)
- Has any hospital's payer contract been reviewed by counsel post-CAA-21 and found to contain a post-adjudication data sharing restriction that survives the gag clause prohibition — and is there a published legal opinion or CMS enforcement action documenting such a clause? — added 2026-05-16, priority: medium
- Should cliqueue develop a standard "Payer Engagement Letter" template offering payers a direct BAA for plan-side analytics — converting cliqueue's role from hospital-BA to dual-BA — and would this dual-BA structure be sufficient for the plan's GCPCA attestation compliance to shield the hospital from restrictive provider-contract clause risk? — added 2026-05-16, priority: high
- Should the hospital onboarding payer contract review checklist distinguish "pre-adjudication routing clauses" (837 submission exclusivity, outside CAA-21's scope for hospital→cliqueue flows) from "post-adjudication data sharing clauses" (more likely to trigger DOL January 2025 functional restriction analysis) — reducing legal review scope for the majority of hospital payer contracts? — added 2026-05-16, priority: high
- Has any major health system's counsel published a legal opinion analyzing whether the DOL January 2025 "functional restriction" guidance extends CAA-21's protection to provider-vendor data flows — and is this a ripe area for cliqueue to commission a targeted outside-counsel opinion letter as a go-to-market collateral document? — added 2026-05-16, priority: medium
- Does Avaneer Health's real-time adjudication product (permissioned consortium blockchain, backed by Aetna/Anthem) compete directly with cliqueue's on-chain settlement layer — and should cliqueue proactively monitor Avaneer's product roadmap for convergence into the neutral public-chain audit trail use case? — added 2026-05-16, priority: medium
- Given the HFMA benchmark of 30-extra-days per denial rework cycle, is there a quantified dollar value (as % of total denied AR) attributable specifically to the coding-error sub-category of denials — and does this inform cliqueue's ROI model beyond the $262B total denied pool figure already in docs/research/market/? — added 2026-05-16, priority: medium
- Should cliqueue's deployment runbook include a step to subscribe to the NUBC annual update announcement (published each July for the following January effective date) and add a calendar trigger to queue a `TimelockController` `addClaimType()` proposal at least 5 business days before the effective date — so the governance transaction completes before new facility types become submittable on Somnia? — added 2026-05-16, priority: high
- Should `ClaimsAdjudicator` emit a `ClaimTypeRegistered(uint8 claimType, bool attestationRequired)` event whenever `addClaimType()` is called via the timelock, enabling hospital integration engineers to automatically detect new claim type activations via an Ormi subgraph subscription without polling? — added 2026-05-16, priority: medium
- Is the `registeredClaimTypes` mapping initialization (8 active second-digit values at deployment) gas-cost significant enough on Somnia to warrant a constructor loop pattern vs. individual `_setClaimType()` calls — and does Somnia's deployment gas model charge separately for each SSTORE in the constructor vs. charged as a single batch? — added 2026-05-16, priority: low

### Track 2 — Agreement layer

~~- Has Circle formally applied to add Somnia to its CCTP V2 network, and what is the expected timeline for native (not bridged) USDC on Somnia mainnet? Is there a public list of chains in the CCTP V2 pipeline? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/usdc-cctp-somnia-native-vs-bridged.md](agreement-layer/usdc-cctp-somnia-native-vs-bridged.md)
~~- What governance mechanism should control the `stablecoinAddress` parameter in `ClaimsAdjudicator` — a multisig, a timelock, or an on-chain DAO vote — and what minimum block notice period is required before a token address change to prevent rug-pull risk? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/stablecoin-address-governance-timelock.md](agreement-layer/stablecoin-address-governance-timelock.md)
- Should cliqueue publish a formal "Settlement Token Policy" document that hospital and payer legal teams can review — specifying current settlement token, custodian, reserve backing, and upgrade governance — to satisfy enterprise procurement? — added 2026-05-15, priority: medium
~~- What is the minimum claim volume threshold at which a provider's per-claim `(billedCents, adjudicatedCents, timestamp)` tuple on a public chain becomes statistically re-identifiable under §164.514(b)(1) Expert Determination — and should cliqueue implement automatic batching below a configurable `minBatchThreshold` setting in `ClaimsAdjudicator`? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/regulatory/re-identification-risk-low-volume-provider-batching.md](regulatory/re-identification-risk-low-volume-provider-batching.md)
- Should cliqueue publish a formal de-identification expert determination report (§164.514(b)(1)) for its on-chain data model, and can a single network-level expert determination cover all hospital deployments or must each hospital obtain its own? — added 2026-05-15, priority: medium
~~- For a batched `ClaimBatchSettled` event emitting `totalBilledCents` for n≥11 claims, does the aggregate amount still constitute a re-identification risk if the provider treats only one DRG code group (e.g., single-specialty orthopedic practice with near-uniform claim values) — and at what coefficient of variation in billing amounts does batch aggregation become insufficient as a mitigation? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/regulatory/batch-aggregation-homogeneous-provider-cv-l-diversity.md](regulatory/batch-aggregation-homogeneous-provider-cv-l-diversity.md)
~~- If `totalBilledCents` is suppressed from the `ClaimBatchSettled` on-chain event, can payer systems still reconcile on-chain settlement proofs with their 835 remittance files using only `(providerHash, batchSize, windowEnd)` — and does this break CAQH CORE Phase IV RTA interoperability? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/835-reconciliation-without-on-chain-billed-amount.md](agreement-layer/835-reconciliation-without-on-chain-billed-amount.md)
~~- Should the `ClaimBatchSettled` event emit `adjudicatedCents` (the payment amount) or suppress it too — and if suppressed, how does the payer agent trigger the 835 adapter without the on-chain settlement amount? Is emitting `adjudicatedCents` lower-risk than `billedCents` because it is the settled (not submitted) amount, and adversarial inference from it is harder? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/adjudicated-cents-on-chain-emission-risk.md](agreement-layer/adjudicated-cents-on-chain-emission-risk.md)
~~- Should cliqueue publish a per-provider billing-CV classification methodology as part of the hospital onboarding checklist, so that `minBatchThreshold` is set by the hospital's privacy officer before first contract deployment — and what is the liability exposure if the hospital sets an incorrect (too-low) threshold? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/regulatory/minbatchthreshold-governance-network-vs-hospital.md](regulatory/minbatchthreshold-governance-network-vs-hospital.md)
~~- Should the `minBatchThreshold` configuration be set at the network level (cliqueue governance) or at the hospital level (`HOSPITAL_ADMIN` role), and does allowing hospital-level override create a compliance gap where a hospital reduces its own threshold to 1 (defeating the de-identification protection)? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/regulatory/minbatchthreshold-governance-network-vs-hospital.md](regulatory/minbatchthreshold-governance-network-vs-hospital.md)
- Is there a ZK range proof pattern allowing `ClaimsAdjudicator` to publish a provably correct `totalBilledCents` while simultaneously proving CV≥20% without revealing the individual claim distribution? — added 2026-05-15, priority: medium
- Should the payer agent's RPC fallback for retrieving per-claim `adjudicatedCents` when `amountSuppressed = true` be a Somnia `eth_call` against `ClaimsAdjudicator.getClaim(claimId)` for each claim in the batch — and does issuing n≥50 RPC calls per batch event (for CV<10% providers) create a latency bottleneck for 835 generation at P95 claim volume? — added 2026-05-15, priority: medium
- Does the TiC MRF's public disclosure of negotiated rates (by NPI + procedure code) create an antitrust concern for cliqueue if `totalAdjudicatedCents` on-chain enables real-time cross-payer rate comparison — and should the Settlement Token Policy document address this? — added 2026-05-15, priority: low
- Should cliqueue's hospital onboarding CV-classification workflow also classify providers by their TiC MRF coverage (i.e., whether their in-network rates are already public for their specialty mix) — since providers with fully public TiC rates carry no additional re-identification risk from emitting `adjudicatedCents` on-chain? — added 2026-05-15, priority: medium
- Does the expert determination report covering cliqueue's on-chain data model need to be re-commissioned each time a new hospital deploys (particularly low-volume specialty providers), or can a single "platform-level" report with a defined `minBatchThreshold` constraint serve all deployments — and what are the procurement implications if each hospital must obtain its own? — added 2026-05-15, priority: medium
- Is the "required for compliance with HIPAA administrative simplification" exemption from Minimum Necessary (§164.502(b)(2)) broad enough to cover cliqueue's use of ICD-10 codes in on-chain transactions, given that chain interactions are not themselves HIPAA-mandated EDI transactions? — added 2026-05-15, priority: low
~~- Does Somnia's team (Improbable) have an active dialogue with Circle for CCTP V2 integration or Bridged USDC Standard adoption — and should cliqueue initiate this as a protocol-level integration request to unblock enterprise procurement before the GENIUS Act effective date (Jan 2027)? — added 2026-05-15, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/circle-cctp-somnia-bridged-usdc-standard-genius-act.md](somnia/circle-cctp-somnia-bridged-usdc-standard-genius-act.md)
- Should cliqueue's `ClaimsAdjudicator` enforce a token whitelist (both USDC.e and native USDC addresses) so that a future in-place upgrade from Stargate USDC.e to Circle native USDC requires only a TimelockController-governed token address change with no contract upgrade? — added 2026-05-15, priority: high
~~- Has Frax Finance submitted a formal PPSI license application to the OCC or FDIC under the GENIUS Act framework — and if so, what is the expected approval timeline relative to the January 2027 effective date, which would determine whether USDso qualifies as a GENIUS Act-compliant settlement token for cliqueue before the DASP prohibition takes effect in July 2028? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/agreement-layer/frax-ppsi-application-status-genius-act-timeline.md](agreement-layer/frax-ppsi-application-status-genius-act-timeline.md)
~~- Does the GENIUS Act's definition of "digital asset service provider" encompass a smart contract like `ClaimsAdjudicator` that holds escrow stablecoin balances — or does it only apply to human-operated custodial intermediaries? If the former, cliqueue must have a PPSI-compliant token by July 18, 2028; if the latter, the DASP obligation falls on the hospital and payer signatories. — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/genius-act-dasp-smart-contract-scope.md](regulatory/genius-act-dasp-smart-contract-scope.md)
~~- Does the UUPS proxy upgrade mechanism (which allows the governance multisig to replace `ClaimsAdjudicator`'s implementation) cause the governance multisig holders to be treated as "operators" of a custodial intermediary under the GENIUS Act, rather than as developers of an immutable protocol — and does this require cliqueue to either (a) permanently disable the proxy upgrade after launch or (b) obtain explicit regulatory guidance? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/uups-proxy-governance-multisig-dasp-operator-risk.md](regulatory/uups-proxy-governance-multisig-dasp-operator-risk.md)
- Should cliqueue's Settlement Token Policy document include a one-paragraph "GENIUS Act Non-DASP Determination" section citing the distributed ledger protocol exclusion and the "person" requirement, so hospital and payer procurement teams have documented legal support for their determination that using `ClaimsAdjudicator` does not make them DASPs? — added 2026-05-16, priority: high
- Does renouncing ProxyAdmin ownership after a post-launch stability window (making `ClaimsAdjudicator` de-facto immutable) satisfy the "immutable and self-custodial software interface" exclusion under the GENIUS Act — and does this make cliqueue's governance multisig holders categorically not DASPs after the renounce date? — added 2026-05-16, priority: medium
- Should cliqueue's hospital BAA include a warranty clause stating that the governance multisig holder does not earn compensation or profit from `ClaimsAdjudicator`'s operation — to document the "for compensation or profit" element exclusion for the purposes of a GENIUS Act non-DASP determination? — added 2026-05-16, priority: medium
~~- Has Treasury published its Section 9 DeFi DASP study (due January 2026) — and if so, does it address whether governance participants in upgradeable stablecoin escrow contracts are DASPs? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/treasury-section9-defi-dasp-scope-study-status.md](regulatory/treasury-section9-defi-dasp-scope-study-status.md)
- Do hospitals and payers using `ClaimsAdjudicator` to settle internally contracted obligations qualify as "offering or selling" stablecoins to US persons under GENIUS Act Section 8, or does the B2B settlement context remove them from the DASP prohibition's scope — and has any law firm published analysis of this specific B2B settlement attribution question? — added 2026-05-16, priority: medium
- Should cliqueue's Settlement Token Policy document explicitly list USDC.e (not PPSI-certified, Stargate-bridged) and native USDC (Circle, on PPSI track via OCC charter) as Tier 1 and Tier 2 tokens respectively, with USDso listed as Tier 3 (contingent on Frax PPSI status) — so hospital procurement teams have a structured compliance path? — added 2026-05-16, priority: high
- Is there a viable path for Frax Finance to obtain Treasury's "comparable regime" determination for the Cayman Islands regulatory framework — and has any other Cayman-incorporated stablecoin issuer obtained such a determination under any pre-GENIUS framework? — added 2026-05-16, priority: medium
- Would the Somnia Foundation be willing to adopt the Bridged USDC Standard for the existing USDC.e contract, or would it require deploying a new contract and migrating liquidity — and does this migration path require a Somnia governance vote? — added 2026-05-16, priority: medium
- At what Somnia USDC.e on-chain liquidity threshold ($M) would Circle be likely to exercise its Bridged USDC Standard upgrade option (granting Circle ownership of the bridged contract for native USDC upgrade), based on precedents from Linea and World Chain? — added 2026-05-16, priority: medium
- Under the GENIUS Act's 18-month grace period (effective January 2027), is there a viable window to launch MVP with USDC.e and migrate to native USDC before hospital procurement teams require PPSI compliance? — added 2026-05-15, priority: medium
- Does the Treasury March 2026 Section 9(e) illicit finance report constitute the full Section 9 legislative recommendations output on DeFi DASP scope, or is a separate DeFi DASP scope document still outstanding — and has Congress received and responded to the DeFi actor classification recommendations? — added 2026-05-16, priority: medium
~~- At what upgrade-authority threshold does the "total independent control over network architecture" test (SIFMA standard, Nov 2025 ANPR comment) cause a governance multisig to become a DASP — does a 3-of-5 multisig with a 48-hour timelock fall below that threshold, or does any upgrade authority regardless of process constitute "total control"? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/multisig-timelock-dasp-control-threshold.md](regulatory/multisig-timelock-dasp-control-threshold.md)
- Should cliqueue's Settlement Token Policy document carry a formal legal opinion letter from outside counsel (rather than an internal non-DASP determination memo) to satisfy hospital and payer procurement teams during the 2026–2028 regulatory ambiguity window before Section 9(d) DeFi rulemaking is finalized? — added 2026-05-16, priority: high
~~- Does the GENIUS Act's "distributed ledger protocol" exclusion cover an upgradeable transparent proxy contract where the implementation can be replaced by a governance multisig, or does upgradeability break the "distributed ledger protocol" characterization — and has any law firm published analysis of this specific proxy-upgrade + DLP-exclusion interaction? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/regulatory/dlp-exclusion-upgradeable-proxy-dasp-analysis.md](regulatory/dlp-exclusion-upgradeable-proxy-dasp-analysis.md)
- Should cliqueue's outside legal opinion letter specifically address the upgradeable proxy + DLP exclusion interaction — arguing that "publicly available and accessible executable software deployed to a distributed ledger" encompasses proxy-pattern contracts where the proxy address is immutable even if the implementation can be swapped? — added 2026-05-16, priority: high
- Does the "publicly available and accessible" requirement in the GENIUS Act DLP definition require that `ClaimsAdjudicator`'s source code (including the implementation contract) be publicly verified on Somnia's block explorer — and would a private (unverified) implementation contract break the DLP exclusion? — added 2026-05-16, priority: medium
- Is there a governance architecture (e.g., time-lock-only with no multisig) that eliminates the "governance participant" DASP risk while preserving emergency pause functionality — or does any upgrade authority necessarily implicate a "person" who could be classified as a DASP under a future expansive rulemaking? — added 2026-05-16, priority: medium
- Would a 4-of-7 multisig with a 7-day timelock clearly satisfy a future rulemaking that adopts the SIFMA "effective or ongoing ability to alter or upgrade" standard — or does any collective upgrade authority above 0-of-N fall within "effective" control regardless of threshold? — added 2026-05-16, priority: medium
- Should cliqueue's hospital BAA specify which governance Position (A: 3-of-5+48h, B: 4-of-7+7d, C: ProxyAdmin-renounced) the current deployment occupies, with a covenant to escalate to Position B upon achieving $10M TVL — giving hospital legal teams a documented DASP-risk upgrade path for the 2026–2028 regulatory ambiguity window? — added 2026-05-16, priority: medium
~~- Should the `TimelockController` minimum delay in `ClaimsAdjudicator` be stored as a mutable parameter (updatable only through the timelock itself) or hard-coded as an immutable constant to prevent a governance decision from shortening the notice period below the enterprise-safe 48-hour floor? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/timelock-min-delay-immutable-vs-mutable.md](agreement-layer/timelock-min-delay-immutable-vs-mutable.md)
- Should cliqueue publish a formal on-chain `SettlementTokenPolicy` document hash (EIP-712 typed data) anchored in `ClaimsAdjudicator`'s storage, linking to an off-chain governance PDF that hospital procurement teams can reference — and does this create any new HIPAA or GENIUS Act compliance obligations? — added 2026-05-15, priority: medium
- At what USDC escrow balance threshold (e.g., >$10M total locked in `ClaimsAdjudicator`) should the proposer multisig threshold be automatically escalated (e.g., from 3-of-5 to 4-of-7) — and can this escalation itself be governed by the timelock without introducing a bootstrap paradox? — added 2026-05-15, priority: medium
- Does the immutable `MIN_DELAY_FLOOR` in a custom `TimelockController` wrapper create a deployment-time commitment that hospital legal teams can independently verify via block explorer bytecode — and should cliqueue's deployment runbook include a step for the hospital's auditor to confirm the floor value before first claim submission? — added 2026-05-15, priority: medium
~~- Should `ClaimsAdjudicator` use the Compound/Uniswap-style Timelock.sol (a separate contract from OpenZeppelin's `TimelockController`) or extend OZ's `TimelockController` with an overridden `updateDelay()` that enforces `MIN_DELAY_FLOOR` — and does the choice affect the UUPS proxy upgrade path? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/timelock-uups-proxy-upgrade-path.md](agreement-layer/timelock-uups-proxy-upgrade-path.md)
~~- Should cliqueue use OZ `TransparentUpgradeableProxy` (which auto-deploys a `ProxyAdmin`) or a custom ProxyAdmin contract with the `TimelockController` as its owner — and does the auto-deployed ProxyAdmin pattern work with Hardhat/Foundry deploy scripts on Somnia? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/transparent-proxy-proxyadmin-timelock-deployment.md](agreement-layer/transparent-proxy-proxyadmin-timelock-deployment.md)
- Should the `PAUSER` role in `ClaimsAdjudicator` (not behind the timelock) be a dedicated 2-of-3 emergency multisig separate from the 3-of-5 governance multisig, or the same key set — and does separating them create a key management burden that outweighs the security benefit at MVP scale? — added 2026-05-15, priority: medium
~~- Given Somnia's undocumented EVM hardfork level, should cliqueue's pre-deployment checklist include a canary contract test that exercises `immutable` variables, `block.basefee`, and ERC-7201 namespaced storage slots before deploying `ClaimsAdjudicator`? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/evm-hardfork-level-paris-and-canary-tests.md](somnia/evm-hardfork-level-paris-and-canary-tests.md)
~~- Does Somnia's AOT EVM compilation support Cancun-level opcodes (`TLOAD`, `TSTORE`, `MCOPY`) — and should cliqueue publish a canary contract test result (pass/fail per opcode) in the deployment runbook before any production deployment? (compile with `evmVersion: "paris"` as safe default until confirmed) — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/evm-hardfork-level-paris-and-canary-tests.md](somnia/evm-hardfork-level-paris-and-canary-tests.md)
- Has Somnia published or committed to a timeline for upgrading to Shanghai or Cancun EVM level — and should cliqueue's contract architecture plan for a future EVM upgrade that could affect Paris-compiled deployed bytecode? — added 2026-05-15, priority: medium
- Should cliqueue include a post-deployment smoke test calling every public function of `ClaimsAdjudicator` from a Somnia mainnet fork before directing hospital systems to the live contract address — and should this be automated in CI? — added 2026-05-15, priority: high
- Should cliqueue's deployment runbook include a step for the hospital's legal/audit team to independently verify the `TimelockController` address registered as `ProxyAdmin.owner()` via the Somnia block explorer before the first claim submission? — added 2026-05-15, priority: medium
- Should the `.openzeppelin/somnia-mainnet.json` implementation-tracking file be committed to the cliqueue repo or stored externally — and does exposing implementation addresses in a public repo create a security risk? — added 2026-05-15, priority: low

- Does the AgentVault `updateDailyLimit()` function allow setting the limit above the 100 ether cap enforced at `createVault`, or is 100 SOMI an immutable hard cap per vault? — added 2026-05-14, priority: high
~~- Is USDso (Frax on Somnia) suitable as a claim settlement stablecoin for US hospital-payer payments, or does the lack of a published HIPAA/enterprise compliance framework make USDC the only defensible choice for MVP? — added 2026-05-14, priority: high~~ → investigated 2026-05-15 → [docs/research/agreement-layer/usdc-vs-usdso-settlement-stablecoin.md](agreement-layer/usdc-vs-usdso-settlement-stablecoin.md)
- What is the minimum SOMI reserve buffer (in days of operation at P95 claim volume and ATH SOMI price) that cliqueue should maintain, and should it be held on-chain in the vault or in a hospital-operated off-chain treasury with a top-up oracle trigger? — added 2026-05-14, priority: medium
~~- What is the optimal struct packing layout for a `Claim` record on Somnia to minimize cold-SLOAD exposure while keeping a dispute-tier mirror contract in sync with primary contract state? — added 2026-05-14, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/claim-struct-packing-cold-sload-dispute-architecture.md](somnia/claim-struct-packing-cold-sload-dispute-architecture.md)
~~- What is the AgentVault daily-limit calibration strategy when claim volume exceeds the 0.01–100 ether per-day ceiling? At scale ($631/claim × 10,000 claims/day), is a multi-vault fan-out pattern needed? — added 2026-05-14, priority: high~~
~~- What is the actual per-claim gas cost (in USD) for a Somnia native `createAdvancedRequest(subcommitteeSize=5)` call vs. the $4–10 outsourced coding benchmark? Does the $0.03/invocation figure cover the full lifecycle including callback gas? — added 2026-05-14, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/per-claim-gas-economics-llm-inference-cost.md](somnia/per-claim-gas-economics-llm-inference-cost.md)
~~- Should cliqueue's off-chain agent layer implement a `SOMI_PRICE_CIRCUIT_BREAKER` that automatically reduces subcommitteeSize from 5 to 3 when the estimated per-claim cost exceeds a configurable ceiling (e.g., $0.50), rather than blocking claim submission entirely? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/somi-price-circuit-breaker-oracle-architecture.md](somnia/somi-price-circuit-breaker-oracle-architecture.md)
~~- Is the Somnia platform's 0.07 SOMI/agent LLM Inference price fixed at the protocol level or can it be updated by Somnia governance — and should cliqueue's deployment docs disclose this pricing-change risk to hospital procurement teams alongside the SOMI reserve buffer requirement? — added 2026-05-16, priority: medium~~ → investigated 2026-05-16 → [docs/research/somnia/somi-inference-fee-governance-cliff-unlock-risk.md](somnia/somi-inference-fee-governance-cliff-unlock-risk.md)
~~- Has the Somnia platform published a formal commitment to advance notice before changing the 0.07 SOMI/agent LLM Inference price — and should cliqueue's deployment documentation include a monitoring step (subscribe to Somnia governance announcements) to detect fee changes before they affect hospital SOMI reserve sizing? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/somi-inference-fee-governance-cliff-unlock-risk.md](somnia/somi-inference-fee-governance-cliff-unlock-risk.md)
- Should cliqueue publish a "Somnia Platform Dependency Risk Memo" disclosing the runner fee governance gap (no advance notice, unilaterally changeable) and the September 2026 cliff unlock risk — so hospital procurement teams have documented external disclosure support before approving deployment? — added 2026-05-16, priority: high
- When Somnia transitions to dynamic on-chain pricing (replacing the 0.07 SOMI fixed runner fee with an on-chain median of actual resource consumption), should cliqueue's SOMI circuit breaker be extended to cap per-claim inference cost in dollar terms (not just SOMI terms) — catching both SOMI price spikes and Somnia fee increases simultaneously? — added 2026-05-16, priority: medium
- Should the hospital BAA's SOMI Reserve Exhibit specify that the 0.07 SOMI fee assumption is subject to Somnia platform changes — and that if the inference fee changes by more than 20%, cliqueue will issue an updated Reserve Exhibit within 10 business days, giving hospitals a contractual re-sizing trigger? — added 2026-05-16, priority: high
- Should the `SOMI_PRICE_CIRCUIT_BREAKER` ceiling values ($0.50 for sub=5, $0.25 for sub=3) be published as configurable constants in the hospital BAA Settlement Finality Exhibit — so hospital finance teams can set their own per-claim cost ceiling rather than accepting cliqueue's default? — added 2026-05-16, priority: medium
- Should the off-chain agent's SOMI price cache TTL (5-minute suggested) be shortened to 1 minute during high-volatility periods (SOMI 24h move >5%) — and is there a CoinGecko API webhook or push notification for price threshold crossings that would let the agent react to price spikes without polling? — added 2026-05-16, priority: low
~~- At a 10,000 claim/day volume, cliqueue's daily SOMI reserve requirement (sub=5) is 4,000 SOMI/day (~$672/day at spot) — should the hospital or cliqueue hold this reserve, and how does SOMI's ~12× ATL-ATH volatility range affect the reserve sizing rule? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/somi-reserve-holding-architecture-compliance.md](somnia/somi-reserve-holding-architecture-compliance.md)
- Should the SOMI reserve sizing rule (30-day buffer at 2× 6-month average price) be published in the hospital BAA as a "SOMI Reserve Exhibit" — binding the hospital to maintaining a minimum buffer before any claim submission — and what is cliqueue's liability if the hospital's reserve is depleted mid-batch causing stuck claims? — added 2026-05-16, priority: high
~~- Does the approaching September 2026 SOMI cliff unlock (team + investor tokens, ~26% of supply) justify recommending hospitals acquire SOMI post-cliff rather than pre-cliff to avoid holding a depreciating position — and should cliqueue's deployment runbook include a cliff-unlock risk disclosure? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/somi-inference-fee-governance-cliff-unlock-risk.md](somnia/somi-inference-fee-governance-cliff-unlock-risk.md)
- Is there a SOMI staking option that allows the hospital to earn yield on its reserve buffer while maintaining sufficient liquid SOMI for daily claim throughput — and does staking SOMI change the IRS tax treatment from "property held for operational use" to "investment generating income" (potentially creating UBTI exposure for nonprofit hospitals)? — added 2026-05-16, priority: medium
~~- How does the OpPC challenge window duration interact with Somnia's cold-SLOAD surcharge? If a claim is challenged 48 hours after submission, is the state slot still in the hot cache, or does dispute resolution incur the 1M-gas cold-read penalty? — added 2026-05-14, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/claim-struct-packing-cold-sload-dispute-architecture.md](somnia/claim-struct-packing-cold-sload-dispute-architecture.md)
~~- What is the actual Somnia OpPC challenge window duration (in blocks or seconds) — is the 48-hour assumption validated anywhere in Somnia's documentation or governance proposals, and does a longer window (7 days) fundamentally change the dispute-tier mirror contract requirement? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/oppc-challenge-window-bft-finality-architecture.md](somnia/oppc-challenge-window-bft-finality-architecture.md)
~~- Should `disputeWindowSeconds` have a network-level minimum floor (e.g., 21,600 = 6 hours, enforced by `TimelockController` min-delay logic) to prevent governance from setting an operationally unworkable sub-hour window — and should this floor be documented in the hospital BAA as a settlement finality guarantee? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/dispute-window-floor-governance-baa.md](somnia/dispute-window-floor-governance-baa.md)
~~- Should cliqueue define a `ClaimDisputed(bytes32 indexed claimId, address indexed disputant, uint256 disputeWindowEnd)` event that hospital and payer monitoring systems subscribe to via Ormi/Somnia WebSocket — enabling automated payer back-office systems to flag the claim for manual review within the dispute window without polling? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/claim-disputed-event-websocket-subscription.md](somnia/claim-disputed-event-websocket-subscription.md)
~~- Should the cliqueue dispute-listener middleware be a standalone TypeScript service deployed by cliqueue, or a hospital-deployed integration module — and does the choice affect BAA obligations (if cliqueue runs it, cliqueue is a BA processing PHI-adjacent claim references)? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/dispute-listener-middleware-deployment-baa-obligations.md](somnia/dispute-listener-middleware-deployment-baa-obligations.md)
- Should cliqueue publish a formal "Non-PHI Determination Memo" (outside-counsel-reviewed) covering the dispute-listener and all other cliqueue-operated services — so hospital privacy officers have documented legal support rather than relying on cliqueue's self-assessment, especially given the 2026 HIPAA Security Rule BA verification tightening? — added 2026-05-16, priority: high
~~- For Architecture A (cliqueue-operated listener, fires claimId hash only), does requiring the payer to perform its own `(claimId → payer claim reference)` lookup create a payer integration barrier that makes Architecture B (hospital self-hosted) necessary for the first production hospital deployment — and should cliqueue's onboarding checklist include a payer readiness questionnaire before selecting the architecture? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/agreement-layer/architecture-a-payer-integration-barrier-claimid-lookup.md](agreement-layer/architecture-a-payer-integration-barrier-claimid-lookup.md)
- Should the onboarding checklist include a formal "Payer Readiness Questionnaire" — assessing whether the payer's back-office team can build a CLM01-HMAC verification receiver before Architecture A is selected for a given hospital deployment? — added 2026-05-16, priority: high
~~- Should the shared `hmacSalt` (required for payer CLM01 verification under Architecture A) be delivered to the payer via a separate BAA-governed key-exchange process — and does this key-exchange create a new BA relationship between cliqueue and the payer (since cliqueue would then be transmitting a key that enables reconstruction of PHI-linked claim references)? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/agreement-layer/hmacsalt-key-exchange-ba-relationship-analysis.md](agreement-layer/hmacsalt-key-exchange-ba-relationship-analysis.md)
- Should the Architecture A key-exchange be hospital-direct-to-payer (hospital delivers hmacSalt to payer via its own BAA-governed channel, bypassing cliqueue entirely) — eliminating cliqueue's salt-routing risk and making the HIPAA analysis a pure covered-entity-to-covered-entity exchange? — added 2026-05-16, priority: high
- Should cliqueue publish a "Non-PHI Key Routing Memo" (outside-counsel-reviewed) analyzing the hmacSalt under 45 CFR 160.103 and §164.514(c) — so hospital privacy officers have documented external legal support before approving Architecture A deployment? — added 2026-05-16, priority: high
- If the hospital's payer contract requires all claim-related data exchange through an approved clearinghouse, does routing the hmacSalt directly from hospital to payer (bypassing cliqueue) still satisfy that contractual requirement — or does the salt exchange require separate documentation as a non-clearinghouse supplemental integration? — added 2026-05-16, priority: medium
- For Architecture B, should the hospital's self-hosted dispute-listener be documented as a reference TypeScript implementation in the cliqueue open-source repo — to reduce hospital IT integration friction and demonstrate that no cliqueue-operated service ever touches the hospital's EDI data? — added 2026-05-16, priority: high
~~- If cliqueue later adds an enrichment step to the dispute-listener (looking up the hospital's EDI mapping to include `CLM01` in the payer webhook payload), does that single addition transform cliqueue from not-a-BA to a BA requiring a full executed BAA — and should this be documented as a hard feature gate in the product spec? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/agreement-layer/enrichment-step-ba-status-feature-gate.md](agreement-layer/enrichment-step-ba-status-feature-gate.md)
- Should the enrichment gate (`enrichmentEnabled` flag + BAA prerequisite) be enforced not just in deployment documentation but also in the cliqueue dispute-listener codebase itself — with a startup assertion that checks for a signed BAA acknowledgment before the enrichment code path is activated? — added 2026-05-16, priority: high
- Should cliqueue's BAA template include a specific "Dispute Enrichment Exhibit" addendum (separate from the main BAA scope) so that hospitals deploying Architecture A without enrichment can execute a minimal BAA, and only upgrade to the full enrichment scope when procurement permits — reducing time-to-first-deployment? — added 2026-05-16, priority: high
- Does the enrichment feature also create a BA subcontractor chain if the hospital's self-hosted Architecture B deployment is operated by a third-party IT managed services vendor — and must that MSP execute a BAA with cliqueue before Architecture B is deployed on MSP-managed infrastructure? — added 2026-05-16, priority: medium
~~- Does Somnia's Data Streams SDK `topicOverrides` support topic[1] filtering (indexed `bytes32 claimId`) for server-side per-claim filtering, or is client-side filtering in `onData` the only option — and should cliqueue's dispute-listener use raw `eth_subscribe` (confirmed per-claim topic filtering) over the Data Streams SDK? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/data-streams-sdk-vs-eth-subscribe-topic-filtering.md](somnia/data-streams-sdk-vs-eth-subscribe-topic-filtering.md)
~~- Does Somnia's AOT-compiled EVM correctly implement `eth_subscribe("logs")` topic[1] filtering in practice — has any developer published a confirmed test result on Somnia mainnet (chain ID 5031) showing a `topics: [sig, indexedValue]` subscription receiving only matching logs, and should cliqueue's pre-deployment canary suite include a topic[1] filter verification test? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/eth-subscribe-topic1-filter-somnia-log-gas-costs.md](somnia/eth-subscribe-topic1-filter-somnia-log-gas-costs.md)
- Should cliqueue's pre-deployment canary test suite include a topic[1] filter verification test (deploy a minimal event-emitting contract, subscribe with `topics: [sig, specificValue]`, emit two events with different indexed values, verify only one is received) — and should this test result be published in the deployment runbook as a signed attestation alongside the Paris-level opcode canary results? — added 2026-05-16, priority: high
- Should all prior Somnia event gas estimates in research docs (e.g., [[somnia/staffing-floor-reached-event-design|staffing-floor-reached-event-design]] citing "~1,006 gas") be formally corrected with a Somnia-specific errata using the confirmed formula `3200 + 5120 × topic_count + 160 × size` — and should a "Somnia LOG gas reference" constant table be published in the deployment runbook to prevent future research notes from using Ethereum baseline figures? — added 2026-05-16, priority: high
- Should [[somnia/per-claim-gas-economics-llm-inference-cost|per-claim-gas-economics-llm-inference-cost]] be revised to include event emission gas costs using Somnia's LOG formula (e.g., LOG3 `ClaimSubmitted` = 23,680 gas, LOG2 `ClaimBatchSettled` = 18,560 gas) — and does including these costs materially change the per-claim economics conclusion ($0.03/claim LLM inference dominates)? — added 2026-05-16, priority: medium
~~- Should cliqueue's dispute-listener implement connection affinity (pinning to one WebSocket backend node) to avoid the documented `eth_unsubscribe` failure on Somnia load-balanced endpoints — and does this require a dedicated non-load-balanced WebSocket endpoint from Somnia or a third-party RPC provider (GetBlock, Validation Cloud)? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/dispute-listener-websocket-connection-affinity.md](somnia/dispute-listener-websocket-connection-affinity.md)
- Should cliqueue's dispute-listener spec mandate Path B (reconnect + resubscribe + `eth_getLogs` backfill from hospital-local checkpoint) as the default MVP implementation — with the checkpoint stored in the hospital-local PostgreSQL store — and explicitly document that Somnia's public WSS endpoint is acceptable for MVP but a dedicated provider endpoint (GetBlock dedicated node) is recommended for $10M+ TVL production deployments? — added 2026-05-16, priority: high
- At what claim dispute volume (disputes per day) does the `eth_getLogs` backfill on reconnect (fetching all `ClaimDisputed` events from `lastSeenBlock` to `head`) become a latency bottleneck — and should the backfill query be bounded to a configurable maximum block range (e.g., 24 hours of blocks) with an alert if the gap exceeds that bound? — added 2026-05-16, priority: medium
- Should cliqueue proactively test GetBlock's dedicated Somnia node to empirically confirm that WebSocket subscriptions are pinned to a single backend process (not load-balanced across GetBlock's internal cluster) — and publish this test result alongside the topic[1] filter canary test in the deployment runbook? — added 2026-05-16, priority: high
- Should cliqueue audit all planned on-chain events (`ClaimSubmitted`, `ClaimAdjudicated`, `ClaimBatchSettled`, `PayerBlocked`, `AttestorCountChanged`, `StaffingFloorReached`, `DisputeWindowUpdated`) for topic count and data size to compute Somnia-correct gas budgets before the next feature branch regeneration? — added 2026-05-16, priority: medium
- Should the dispute-listener middleware implement a "missed event" recovery path by calling `eth_getLogs` from the last processed block on WebSocket reconnection — and should the last-processed block number be checkpointed in a hospital-local database or in an Ormi subgraph entity to survive middleware restarts? — added 2026-05-16, priority: medium
- Should the `MIN_DISPUTE_WINDOW_FLOOR` (24 hours) and `MAX_DISPUTE_WINDOW_CEILING` (7 days) be deployed as `immutable` bytecode constants or `constant` in `ClaimsAdjudicator` — and does either choice affect the Somnia block explorer's ability to display them to hospital auditors? — added 2026-05-16, priority: medium
- Should the Settlement Finality Exhibit in the hospital BAA specify the `ClaimsAdjudicator` proxy contract address and Somnia block explorer URL for verifying `MIN_DISPUTE_WINDOW_FLOOR` — so a hospital's auditor can independently confirm the floor is bytecode-immutable without relying on cliqueue's word? — added 2026-05-16, priority: high
- Should `ClaimsAdjudicator` emit a `DisputeWindowUpdated(uint256 oldWindow, uint256 newWindow)` event on every `setDisputeWindow()` governance execution — creating an immutable on-chain log of governance changes that hospital compliance teams and payer procurement can subscribe to? — added 2026-05-16, priority: medium
- At what claim volume (claims per day) does the cold-SLOAD cost for simultaneous disputed claims (3M gas per claim × N disputed claims) approach Somnia's block gas limit — and should `ClaimsAdjudicator` enforce a max-disputes-per-block rate limit to prevent a coordinated mass-dispute attack from blocking legitimate settlements? — added 2026-05-16, priority: medium
~~- Should `ClaimsDisputeRegistry` be a separate deployed contract or an internal storage region within `ClaimsAdjudicator` (using ERC-7201 namespaced slot separation) — and does the UUPS upgrade path affect the choice between these two dispute-isolation architectures? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/claims-dispute-registry-separate-vs-erc7201-internal.md](somnia/claims-dispute-registry-separate-vs-erc7201-internal.md)
- Should the `DisputeStorage` namespace include the `DisputeRecord` struct inline or reference a separate `IDisputeRecord` interface — so hospital audit tooling can verify the dispute record schema independently of the `ClaimsAdjudicator` ABI? — added 2026-05-16, priority: medium
- Should the stateless `ClaimsDisputeView` contract's ABI be pinned as a cliqueue spec artifact (versioned alongside the settlement contract spec), so hospital integration engineers can build indexing tools without coupling to `ClaimsAdjudicator`'s full ABI? — added 2026-05-16, priority: medium
- Should cliqueue's Foundry test suite include an upgrade safety test that verifies no storage slot collision between the main `ClaimsAdjudicator` namespace and the `cliqueue.dispute` namespace — using `forge inspect` output to confirm disjoint slot ranges? — added 2026-05-16, priority: high
- At what total claim volume (number of distinct active claim slots in `ClaimsAdjudicator`) does the Somnia 128M LRU cache become competitive enough that cliqueue slots risk eviction within a normal dispute window — and should cliqueue implement a "claim ping" heartbeat (warm-read no-op) for high-value disputed claims? — added 2026-05-16, priority: medium
~~- Does HIPAA's Minimum Necessary standard (§164.502(b)) constrain who can observe on-chain HMAC hashes, ICD-10 codes, and payment amounts emitted as Somnia public events? — added 2026-05-14, priority: medium~~ → investigated 2026-05-15 → [docs/research/regulatory/hipaa-minimum-necessary-on-chain-events.md](regulatory/hipaa-minimum-necessary-on-chain-events.md)
~~- What BAA (Business Associate Agreement) structure would a hospital's counsel require with Improbable (Somnia chain operator) before anchoring claim-derived hashes on Somnia mainnet? — added 2026-05-14, priority: medium~~ → investigated 2026-05-17: NO BAA WITH IMPROBABLE REQUIRED OR POSSIBLE — Improbable is not a BA under 45 CFR 160.103 because Somnia never receives PHI under cliqueue's architecture; conduit exception does not apply (validators persistently maintain ledger data, not transient transmission); BAA obligation falls on hospital↔cliqueue + cliqueue↔Corti sub-BA chain only; action item is a "Somnia Non-BA Determination Memo" for hospital onboarding → [docs/research/regulatory/improbable-somnia-operator-baa-analysis.md](regulatory/improbable-somnia-operator-baa-analysis.md)
- Should cliqueue's hospital BAA explicitly list Improbable/Somnia as a "non-BA infrastructure provider" in a Subprocessor Exhibit — distinguishing it from Corti (named BA subprocessor) — so hospital privacy officers can close the Somnia BAA question without a separate legal review engagement? — added 2026-05-17, priority: high
- Should the "Somnia Non-BA Determination Memo" in the hospital onboarding package be reviewed by outside HIPAA counsel before distribution — given the memo's conclusions directly affect each hospital's HIPAA compliance posture and no OCR enforcement precedent exists for blockchain HMAC hash anchoring? — added 2026-05-17, priority: high
- If Improbable later adds a "healthcare validator service" tier (permissioned Somnia node with contractual commitments to hospital data), does that create BA status requiring either a cliqueue↔Improbable BAA or a migration to a non-Improbable validator set — and should cliqueue's deployment runbook include a monitoring trigger for Somnia product changes that might affect the non-BA determination? — added 2026-05-17, priority: medium
~~- Are insurance carriers actively piloting non-EDI rails (FHIR Financial, CAQH CORE Phase IV, X12 alternatives)? Which carriers? What were the outcomes? — added 2026-05-14, priority: medium~~ → investigated 2026-05-17 → [docs/research/agreement-layer/fhir-payer-api-readiness-edi-replacement-timeline.md](agreement-layer/fhir-payer-api-readiness-edi-replacement-timeline.md)
~~- Does the CMS-0057-F Prior Authorization FHIR API (mandatory for major payers by Jan 2027) create a pre-submission prior-auth check integration opportunity for cliqueue's hospital agent — querying payer prior-auth requirements via FHIR before anchoring the claim hash on-chain, reducing denial rates for prior-auth-required procedures? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/regulatory/cms-0057f-prior-auth-api-presubmission-integration.md](regulatory/cms-0057f-prior-auth-api-presubmission-integration.md)
~~- Should cliqueue's `ClaimSubmitted` event include a `paStatus` field (`bytes1`: `0x00=NOT_CHECKED`, `0x01=NO_AUTH`, `0x02=AUTH_NEEDED`, `0x03=SATISFIED`, `0x04=PERFORMPA`, `0x05=CONDITIONAL`) anchored on-chain alongside the claim hash — encoding all 5 CRD paStatus values plus NOT_CHECKED — and does including `conditional` as a distinct value (rather than collapsing it into `auth-needed`) help auditors distinguish "PA determination deferred pending more detail" from "PA required and not obtained"? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/pastatus-on-chain-encoding-claim-submitted-event.md](somnia/pastatus-on-chain-encoding-claim-submitted-event.md)
~~- Should cliqueue anchor the `satisfied-pa-id` (X12 PA authorization number) as a `bytes32` hash in `ClaimSubmitted` when `paStatus = 0x02 (satisfied)` — creating an on-chain link between the PA approval and the claim without exposing the raw authorization number — and should the `satisfied-pa-id` hash be a second indexed topic or non-indexed data? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/satisfied-pa-id-on-chain-hash-claim-submitted.md](somnia/satisfied-pa-id-on-chain-hash-claim-submitted.md)
- Should `paStatus` encoding be published as a TypeScript `enum PaStatus` constant in the off-chain agent spec — so hospital integration engineers have a typed, versioned mapping between CRD `coveragePaDetail` codes and on-chain `uint8` bytes, versioned alongside the `CodingConfidenceHeuristic`? — added 2026-05-17, priority: high
~~- When `paStatus = 0x05 (conditional)` resolves later (after the ordering provider supplies more clinical detail and the payer makes a final determination), should a new `ClaimPaStatusResolved(bytes32 indexed claimId, uint8 indexed resolvedStatus)` event type capture the resolution on-chain — creating a full PA determination lifecycle audit trail? — added 2026-05-17, priority: medium~~ → investigated 2026-05-17 → [docs/research/somnia/claim-pa-status-resolved-event-design.md](somnia/claim-pa-status-resolved-event-design.md)
~~- Should `resolveConditionalPaStatus()` also accept a `bytes32 paAuthHash` parameter — enabling `conditional → satisfied` resolution (payer grants PA after receiving more clinical detail via PAS $inquire), in addition to `conditional → auth-needed` and `conditional → no-auth`? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/resolve-conditional-pa-auth-hash-parameter.md](somnia/resolve-conditional-pa-auth-hash-parameter.md)
~~- Should `ClaimPaStatusResolved` emit `paAuthHash` as a non-indexed `bytes32` data field when `resolvedStatus = 0x02 (satisfied)` — adding 32 bytes and 5,120 gas — so the authorization number commitment is auditable in the resolution event itself rather than requiring auditors to correlate across `ClaimSubmitted`? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/claim-pa-status-resolved-paauthash-emission.md](somnia/claim-pa-status-resolved-paauthash-emission.md)
~~- Should cliqueue publish a `ClaimPaLifecycle` TypeScript utility module encapsulating the `conditional → resolved` transition logic (validating legal resolution values, wrapping `resolveConditionalPaStatus()`) alongside `PaStatusEncoder` as a single versioned PA encoding spec artifact? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/pa-status-encoder-typescript-module-npm-ecosystem.md](somnia/pa-status-encoder-typescript-module-npm-ecosystem.md)
~~- Should `@cliqueue/pa-lifecycle` and `@cliqueue/cds-hooks-client` be published together as a `@cliqueue/*` monorepo with shared FHIR extension URL constants — preventing URL string drift between the CDS Hooks client (which parses the `ext-coverage-information` Task) and the PA lifecycle encoder (which hashes `satisfied-pa-id` to `paAuthHash`)? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES — pnpm workspaces + shared `@cliqueue/fhir-types` published package confirmed best practice; no Da Vinci npm constants exist; Medplum monorepo is canonical pattern; `@medplum/fhirtypes` as peerDep confirmed; Changesets + TS project references for versioning → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
- Should `@cliqueue/pa-lifecycle` include a `fromCoverageInformationTask(task: fhir4.Task): PaStatusResult` parser that extracts `paStatus` and `satisfied-pa-id` from a CDS Hooks `systemActions[].resource` Task — bridging the CDS Hooks client output directly to the on-chain encoder input without requiring hospital integration engineers to write parsing code? — added 2026-05-17, priority: high
~~- Should `@medplum/fhirtypes` be evaluated as a `peerDependency` of `@cliqueue/pa-lifecycle` for typing FHIR Task and Coverage resources — or should cliqueue vendor only the minimal Da Vinci extension interfaces needed to avoid pulling in the full Medplum type tree as a hospital-agent dependency? — added 2026-05-17, priority: medium~~ → resolved 2026-05-17: `@medplum/fhirtypes` as `peerDependency` is confirmed correct; do not bundle full FHIR type tree inside cliqueue packages → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
~~- Should `@cliqueue/contracts` (the ABI type package exporting `ClaimsAdjudicator` ABI and `ISBTRegistry`) be included in the same `@cliqueue/*` pnpm monorepo as `fhir-types`, `pa-lifecycle`, `cds-hooks-client`, and `cda-attachments` — or kept as a separate repo aligned with the Solidity contract release cycle? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: SEPARATE REPO — DeFi precedent (Uniswap, Aave, Compound, OZ all separate Solidity repos from TS SDK repos); wagmi CLI Foundry plugin is production-ready; commit generated `src/generated.ts`; per-chain address map pattern confirmed; Changesets publish gated on `forge build` artifact diff → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
~~- Should cliqueue's Foundry contracts repo include a Changesets config alongside Foundry — or use a simpler manual `npm version` + `npm publish` workflow tied explicitly to deployment tags, given the infrequent Solidity release cadence? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: USE CHANGESETS — OpenZeppelin Contracts (the canonical Solidity npm benchmark) uses `@changesets/cli`; low-cadence is not a disqualifier; single-package Changesets config with `changeset add` per ABI-changing PR is correct; manual `npm version` lacks the audit trail and semver-intent tracking Changesets provides → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
~~- Should `@cliqueue/contracts` export a `SOMNIA_CHAIN` viem chain definition (chain ID 5031, RPC URL, block explorer URL) alongside the generated ABI hooks — making it the single import point for Somnia connectivity in hospital agent apps and eliminating the need for hospital engineers to manually configure the chain? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO — Somnia (chain ID 5031) and Somnia Testnet (chain ID 50312) are natively present in viem@2.49.2 as `somnia` / `somniaTestnet`; `import { somnia } from 'viem/chains'` is the correct path; exporting a duplicate from `@cliqueue/contracts` would create version-skew risk → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
~~- Should the wagmi CLI `wagmi.config.ts` in the contracts repo support the Somnia mainnet and Somnia testnet chain IDs in the multi-chain address map — and does wagmi's built-in chain list include Somnia (chain ID 5031), or must cliqueue define a custom chain object and pass it explicitly? — added 2026-05-17, priority: medium~~ → investigated 2026-05-17: NATIVE — `somnia` (5031) and `somniaTestnet` (50312) are in viem@2.49.2; no custom `defineChain` needed; `wagmi.config.ts` uses `import { somnia, somniaTestnet } from 'viem/chains'` directly → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
~~- Should the `@cliqueue/contracts` Changesets config use `"access": "public"` publish config — and does npm require an explicit `publishConfig.access` field in `package.json` for `@cliqueue/*` scoped packages to publish publicly without a paid org account? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES — `"publishConfig": {"access": "public"}` IS required; scoped packages default to restricted without it; Changesets respects per-package publishConfig; npm org for @cliqueue is free for public packages → [docs/research/market/cliqueue-monorepo-package-architecture.md](market/cliqueue-monorepo-package-architecture.md)
- Should the `@cliqueue/contracts` GitHub Actions release workflow verify that the npm org `@cliqueue` is provisioned before the first `changeset publish` — and should the deployment runbook include a one-time `npm org:set @cliqueue public` setup step? — added 2026-05-17, priority: medium
- Should the `.changeset/config.json` in the `@cliqueue/*` monorepo explicitly list all five package names in a `"fixed"` or `"linked"` versioning group — or use independent versioning (Changesets default) where each package bumps on its own release cadence? — added 2026-05-17, priority: medium
- Should cliqueue pin the minimum npm CLI version in CI (e.g., `npm@10+`) to guarantee `publishConfig.access` is respected — since older npm versions had inconsistent handling of the `publishConfig` field for scoped packages? — added 2026-05-17, priority: low
- Should cliqueue pin a minimum viem peer dependency version in `@cliqueue/contracts` (e.g., `"viem": ">=2.49.2"`) to guarantee that `somnia` (chain ID 5031) is available natively — or rely on documentation alone, accepting that hospital engineers on older viem versions must define the chain manually? — added 2026-05-17, priority: medium
- Should the `@cliqueue/contracts` GitHub Actions release workflow run `forge build` → `wagmi generate` → `changeset publish` in a single job, or split into separate `build` and `publish` jobs with Foundry artifact upload between steps — and does Somnia require a network fork for `forge build` or does it compile offline? — added 2026-05-17, priority: medium
- Should `@cliqueue/fhir-types` export a shared `fromCoverageInformationTask(task: fhir4.Task): PaStatusResult` parser — removing it from `@cliqueue/pa-lifecycle` and making it available to `@cliqueue/cds-hooks-client` — or keep it in `pa-lifecycle` only, avoiding a circular or incorrect dependency direction between consumer packages? — added 2026-05-17, priority: medium
- Should the `@cliqueue/*` monorepo adopt Turborepo for build/test pipeline caching from day one — establishing a CI pattern that scales past 4 packages — or defer Turborepo until package count grows beyond 6 to avoid toolchain overhead during MVP development? — added 2026-05-17, priority: medium
- Should the hospital BAA include a "PA Conditional Resolution SLA" clause requiring the hospital agent to call `resolveConditionalPaStatus()` within N business days of receiving the PAS `ClaimUpdate` final response — with different windows for `conditional → auth-needed` (no further PA work) vs. `conditional → satisfied` (requires DTR + ClaimUpdate round-trip)? — added 2026-05-17, priority: medium
- Should cliqueue's `ClaimPaLifecycle` TypeScript module export a unified `resolveConditionalStatus(claimId, resolvedStatus, authorizationNumber?)` helper that handles both the CRD `satisfied-pa-id` path and the PAS `extension:authorizationNumber` path from `ClaimUpdate` responses — ensuring hospital integration engineers use the same hash derivation regardless of which PA pathway produced the authorization number? — added 2026-05-17, priority: high
- Should the Ormi subgraph entity for `ClaimPaStatusResolved` index `paAuthHash` as a queryable field — enabling payer agents to find all `conditional → satisfied` resolutions by auth hash without scanning all resolution events, and does adding a non-indexed field to a LOG3 event make it queryable via Ormi's indexer? — added 2026-05-17, priority: medium
- Should cliqueue publish a one-page "PA Lifecycle Event Schema" in the hospital onboarding checklist documenting `ClaimSubmitted` + `ClaimPaStatusResolved` field semantics, the paAuthHash conditional-zero rule, and cross-event correlation instructions — so payer integration engineers have a single reference without parsing the contract ABI? — added 2026-05-17, priority: high
~~- For the CRD client in cliqueue's TypeScript hospital agent, which CDS Hooks library should be used (`@cds-hooks/client` npm package or a hand-rolled implementation) — and does any existing TypeScript CDS Hooks client support the `order-sign` hook with the Coverage Information extension required by CRD v2.1.0? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/regulatory/cds-hooks-typescript-client-crd-v2-1.md](regulatory/cds-hooks-typescript-client-crd-v2-1.md)
- Should cliqueue define a `payerCRDEndpoint` field in the hospital onboarding checklist — so hospital integration engineers have a structured workflow for registering payer CRD endpoints before first claim submission — and how does a hospital discover a payer's CRD URL (FHIR capability statement lookup vs. manual directory)? — added 2026-05-17, priority: medium
- Should the payer CRD discovery prefetch template be cached at hospital onboarding time or re-fetched per-claim — and does caching create a staleness risk if the payer updates its prefetch requirements between the CMS-0057-F compliance deadline (Jan 2027) and subsequent IG updates? — added 2026-05-17, priority: medium
~~- For `paStatus = conditional` returned by a payer CRD endpoint: should cliqueue treat this identically to `auth-needed` (pause on-chain commitment, trigger PAS submission) or route to human review — and should `conditional` map to a distinct `paStatus` byte in `ClaimSubmitted` (0x05) so auditors can distinguish "PA deferred pending detail" from "PA required"? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/pastatus-on-chain-encoding-claim-submitted-event.md](somnia/pastatus-on-chain-encoding-claim-submitted-event.md)
- Should cliqueue publish its hand-rolled CDS Hooks 2.0 TypeScript client as a standalone open-source `@cliqueue/cds-hooks-client` npm package — filling the ecosystem gap (no maintained npm package exists as of May 2026) and providing hospital integration engineers a reusable typed client for CRD and future Da Vinci IG integrations? — added 2026-05-17, priority: medium
~~- Should `PA_HASH_DOMAIN_SEPARATOR` (used to salt `keccak256(abi.encodePacked(SALT, satisfiedPaId))` before anchoring `paAuthHash` in `ClaimSubmitted`) be published as a cliqueue-wide constant in the hospital onboarding checklist — enabling independent payer verification of `paAuthHash` without cliqueue's cooperation — and should it be the same constant as `CLIQUEUE_DOMAIN_SEPARATOR` used for hospitalId hashing in SBTRegistry, or a distinct domain-specific value? — added 2026-05-17, priority: high~~ → investigated 2026-05-17 → [docs/research/somnia/pa-hash-domain-separator-distinct-vs-shared.md](somnia/pa-hash-domain-separator-distinct-vs-shared.md)
- Should cliqueue publish a formal "Domain Constant Registry" (one-page spec artifact) listing all `bytes32` domain tags across `ClaimsAdjudicator`, `SBTRegistry`, and `PaStatusEncoder` — with preimage strings, hex values, and intended usage — versioned alongside the contract ABI and published in the hospital onboarding checklist? — added 2026-05-17, priority: high
- Should `CLIQUEUE_DOMAIN_SEPARATOR` (`keccak256("cliqueue.v1.hospitalId")`) be promoted to a `public constant` in `SBTRegistry` — so hospital compliance teams can independently verify `hospitalId` derivation from NPI using the Somnia block explorer and published constant, without requiring cliqueue's cooperation? — added 2026-05-17, priority: medium
~~- Should `PaStatusEncoder` (exporting `PA_HASH_DOMAIN_SEPARATOR`, `derivePaAuthHash()`, and the `PaStatus` enum) be published as part of the `@cliqueue/contracts` npm package alongside `ISBTRegistry` — giving payer integration engineers a reusable typed module for `paAuthHash` verification and `ClaimSubmitted` event decoding? — added 2026-05-17, priority: high~~ → resolved by 2026-05-17 investigation: no upstream library exists; must hand-roll as `@cliqueue/pa-lifecycle`; publish as standalone package not bundled with contracts → [docs/research/somnia/pa-status-encoder-typescript-module-npm-ecosystem.md](somnia/pa-status-encoder-typescript-module-npm-ecosystem.md)
~~- Should `paStatus` encoding (`PaStatus` enum) and `paAuthHash` derivation (`keccak256(abi.encodePacked(PA_HASH_DOMAIN_SEPARATOR, satisfiedPaId))`) be published together as a TypeScript `PaStatusEncoder` module — so all PA-related on-chain encoding is versioned, testable, and importable alongside `CodingConfidenceHeuristic` as a single spec artifact? — added 2026-05-17, priority: high~~ → resolved by 2026-05-17 investigation: confirmed hand-roll required; viem `encodePacked(['bytes32','string'],...)` is correct derivation; publish as `@cliqueue/pa-lifecycle` → [docs/research/somnia/pa-status-encoder-typescript-module-npm-ecosystem.md](somnia/pa-status-encoder-typescript-module-npm-ecosystem.md)
~~- Should the `ClaimSubmitted` event spec be formally amended now to include both `uint8 indexed paStatus` and `bytes32 paAuthHash` (non-indexed data, zero for paStatus ≠ satisfied) in a single amendment — rather than two sequential changes — given both fields are derived from the same CRD `coverage-information` extension object and the on-chain gas impact ($0.000209/emission at LOG4+32-byte data) is already confirmed acceptable? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES — single amendment is Solidity event stability canon; `event ClaimSubmitted(bytes32 indexed claimId, bytes32 indexed claimHash, uint8 indexed paStatus, bytes32 paAuthHash)` = LOG4 + 32 bytes = 28,800 gas on Somnia; `uint8 indexed paStatus` enables topic[3] filtering for AUTH_NEEDED/CONDITIONAL payer triage; bytes32(0) sentinel for paAuthHash is ABI-valid; `require(paStatus == 0x02 ? paAuthHash != bytes32(0) : paAuthHash == bytes32(0))` enforced in submitClaim() → [docs/research/somnia/claim-submitted-event-combined-amendment-pastatus-paauthash.md](somnia/claim-submitted-event-combined-amendment-pastatus-paauthash.md)
~~- Should `IClaimsAdjudicator` declare all primary events (`ClaimSubmitted`, `ClaimAdjudicated`, `PayerClaimRefSet`, `ClaimPaStatusResolved`, `StaffingFloorReached`, `AttestorCountChanged`) as a single interface-level event registry — creating one canonical ABI reference for Ormi subgraph schema generation and hospital integration engineers, rather than scattering event declarations across implementation files? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO — flat omnibus registry is wrong architecture. Use Uniswap v3 sub-interface pattern: `IClaimsEvents.sol` (only ClaimsAdjudicator events: ClaimSubmitted, ClaimAdjudicated, PayerClaimRefSet, ClaimPaStatusResolved) + `IClaimsAdjudicator is IClaimsEvents`. StaffingFloorReached + AttestorCountChanged remain in ISBTRegistry (separate contract, separate Ormi data source). Ormi subgraph binds to interface ABI — events must be in the ABI artifact supplied to `subgraph.yaml`; implementation-only events are not indexable from interface ABI. Declaring events in interfaces has zero runtime gas cost. Events do not affect ERC-165 interfaceId. → [docs/research/somnia/iclaimsadjudicator-event-registry-interface-design.md](somnia/iclaimsadjudicator-event-registry-interface-design.md)
- Should `IClaimsEvents.sol` be published as a standalone npm artifact in `@cliqueue/contracts/interfaces` alongside the full `IClaimsAdjudicator` ABI — so Ormi subgraph developers can import only the events interface without pulling the full contract ABI, and does this create a separate versioning concern? — added 2026-05-17, priority: high
~~- Should the Ormi subgraph manifest specify the `IClaimsAdjudicator` interface ABI (minimal) or the implementation ABI (full, including storage layout artifacts from Foundry) — and does the Foundry implementation ABI contain any artifacts that break `graph codegen` type generation? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: USE INTERFACE ABI (clean JSON array) — full forge build JSON output breaks graph-node deserialization; extract with `forge inspect IClaimsAdjudicator abi`; interface ABI is sufficient because IClaimsEvents declares all 4 indexable events; UUPS proxy pattern uses proxy address + implementation interface ABI; custom error types safe in current graph-node (fixed Jan 2022, PR #3144); Ormi fully The Graph-compatible → [docs/research/somnia/ormi-subgraph-abi-format-interface-vs-implementation.md](somnia/ormi-subgraph-abi-format-interface-vs-implementation.md)
- Should cliqueue's Foundry contracts repo CI script include a step that runs `forge inspect IClaimsAdjudicator abi > abis/IClaimsAdjudicator.json` and `forge inspect ISBTRegistry abi > abis/ISBTRegistry.json` — committing both clean ABI arrays as versioned artifacts alongside `src/generated.ts`? — added 2026-05-17, priority: high
~~- When `ClaimsAdjudicator` is upgraded via UUPS, does the Ormi hosted environment support an atomic "ABI-upgrade + subgraph-redeploy" workflow that preserves all indexed history — or must the hospital integration team stand up a new subgraph deployment and migrate historical queries? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO atomic upgrade path exists — The Graph deployment model is immutable per deployment ID; grafting exists but is explicitly not recommended for The Graph Network production ("can complicate indexing, may not be fully supported by all Indexers"); Ormi has no documented upgrade-specific capability beyond standard graph-node; correct production pattern is dual data sources + event renaming (new dataSource at upgrade block, rename conflicting events to avoid handler collision); full reindex from genesis is safe default for breaking ABI changes → [docs/research/somnia/ormi-subgraph-uups-upgrade-workflow.md](somnia/ormi-subgraph-uups-upgrade-workflow.md)
- Should cliqueue's deployment runbook specify a minimum advance-notice window (48 hours, matching the TimelockController min-delay) before a `ClaimsAdjudicator` UUPS upgrade that changes event signatures — giving hospital integration engineers time to prepare the updated subgraph before the upgrade executes on-chain? — added 2026-05-17, priority: high
- Should the Ormi subgraph `subgraph.yaml` use dynamic data source `templates` or static `dataSources` for the pre/post-upgrade event split in the dual-source pattern — and does the Ormi hosted environment support dynamic data source templates? — added 2026-05-17, priority: medium
- Should cliqueue publish a versioned `subgraph.yaml` artifact in the contracts repo alongside the ABI files — so hospital integration engineers can deploy the updated subgraph using the dual-source pattern without writing YAML from scratch after each `ClaimsAdjudicator` upgrade? — added 2026-05-17, priority: high
- Should `ISBTRegistry` be deployed as a UUPS proxy or as an immutable contract — and does that choice change which ABI file (proxy ABI vs implementation interface ABI) is supplied to the Ormi `ISBTRegistry` data source in `subgraph.yaml`? — added 2026-05-17, priority: medium
- Should `IClaimsEvents` and `ISBTRegistryEvents` be aggregated into a single `ICliqueueEvents.sol` top-level interface used only for documentation/tooling purposes (not imported by any contract) — so hospital integration engineers have a single-file reference for all on-chain events without creating a misleading cross-contract inheritance chain? — added 2026-05-17, priority: medium
- Should the Ormi subgraph entity for `ClaimSubmitted` index `paStatus` as a queryable enum field and `paAuthHash` as a nullable `Bytes` field (null when bytes32(0)) — enabling payer agents to query "all SATISFIED claims by hospitalId in the last 30 days" without full event log scan? — added 2026-05-17, priority: high
- Should cliqueue's pre-deployment canary suite include a `ClaimSubmitted` topic[3] filter verification test on Somnia mainnet (chain ID 5031) — confirming that `uint8` indexed values are correctly padded to 32-byte topics and filterable via `eth_subscribe` — published alongside the existing Paris opcode canary and topic[1] filter canary results? — added 2026-05-17, priority: high
~~- Has CMS published a proposed rulemaking for FHIR-based electronic attachments (CDex Attachments IG equivalent) — and what is the expected finalization timeline, which would signal when clinical documentation exchange for claims becomes a mandatory payer FHIR capability? — added 2026-05-17, priority: medium~~ → investigated 2026-05-17: CMS-0053-F finalized March 24, 2026 (Federal Register 2026-05676); adopts X12 275/277 + HL7 CDA; FHIR CDex not adopted; compliance deadline May 2028 → [docs/research/agreement-layer/fhir-payer-api-readiness-edi-replacement-timeline.md](agreement-layer/fhir-payer-api-readiness-edi-replacement-timeline.md)
~~- Should cliqueue's EDI adapter spec define a `PayerAdapterInterface` abstract class with a forward-compatibility seam — so a future FHIR `ClaimResponse` adapter can be swapped in per-payer without changing the on-chain settlement layer when any major payer ships a production FHIR Claims API? — added 2026-05-17, priority: medium~~ → investigated 2026-05-17: YES — define `PayerAdapterInterface` with `ClaimSubmissionAdapter` (837), `RemittanceAdapter` (835), and `ClaimsAttachmentAdapter` (275/277 + CDA) sub-interfaces; FHIR CDex extension point for future per-payer optional adoption → [docs/research/agreement-layer/fhir-payer-api-readiness-edi-replacement-timeline.md](agreement-layer/fhir-payer-api-readiness-edi-replacement-timeline.md)
~~- Should `PayerAdapterInterface` define a `ClaimsAttachmentAdapter` sub-interface specifically for X12 275/277 + HL7 CDA attachment exchange — separate from `ClaimSubmissionAdapter` (837) and `RemittanceAdapter` (835) — given CMS-0053-F's May 2028 compliance deadline creates a distinct implementation timeline from the MVP 837/835 requirement? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES — `ClaimsAttachmentAdapter` confirmed required; no maintained TypeScript npm library for X12 275 generation exists (node-x12 maintenance-mode since 2021; node-x12-edi JavaScript-only); must hand-roll X12 275 ISA/GS/ST/BHT/HL/BDS envelope — fourth confirmed ecosystem gap; BDS segment (not BIN) carries base64 CDA in 006020X314; `ClaimsAttachmentAdapter` needs `generateUnsolicited275()` AND `parseSolicited277CA()` — both directions required by CMS-0053-F; payer adoption accelerating (BCBS KS May 2025, NGS Medicare 6020 June 2025) → [docs/research/agreement-layer/x12-275-typescript-npm-ecosystem-bds-envelope.md](agreement-layer/x12-275-typescript-npm-ecosystem-bds-envelope.md)
~~- Should `@cliqueue/cda-attachments` be renamed `@cliqueue/claim-attachments` to encompass both the CDA document generation (C-CDA R2.1 templates) and the X12 275 envelope generation (ISA/GS/BDS wrapping) — or should these be separate packages with `@cliqueue/x12-275` importing `@cliqueue/cda-attachments`? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: SEPARATE PACKAGES — ecosystem precedent (node-x12, @stedi/edi-core, @medplum/ccda all treat EDI transport and CDA document generation as independent concerns); CMS-0053-F itself mandates separation (X12 275 = transport layer, HL7 C-CDA R2.1 = clinical content layer, independent standards bodies); correct dependency direction: `@cliqueue/x12-275` imports `AttachmentPayload` from `@cliqueue/fhir-types`; `@cliqueue/cda-attachments` returns `AttachmentPayload`; CDA never imports X12; fifth hand-roll ecosystem gap confirmed (`@cliqueue/x12-275` must hand-roll ISA/GS/ST/BHT/HL/BDS generation) → [docs/research/agreement-layer/cda-vs-x12-275-package-boundary.md](agreement-layer/cda-vs-x12-275-package-boundary.md)
- Should `@cliqueue/x12-275` export a `generateUnsolicited275(claimRef: string, payload: AttachmentPayload, isaControl: ISAControlParams): string` function — and should `ISAControlParams` (sender/receiver ISA IDs, ISA date/time, version 006020X314 vs 005010X210) be a required parameter or derived from hospital onboarding config? — added 2026-05-17, priority: high
- Should the `AttachmentPayload` interface (`contentType`, `bytes`, `filename?`, `documentId?`) be defined in `@cliqueue/fhir-types` (making it a shared dependency for both CDA and X12 packages) — or in a separate ultra-minimal `@cliqueue/attachment-types` package to avoid misleading FHIR coupling for the purely-EDI X12 consumer? — added 2026-05-17, priority: medium
- Should ISA13 (interchange control number) in `ISAControlParams` for `@cliqueue/x12-275` be auto-incremented via a hospital-local persistent counter (requiring state) or require explicit caller provision per call (stateless) — and does the CMS-0053-F 006020X314 spec require monotonic ISA13 values for deduplication purposes? — added 2026-05-17, priority: medium
~~- Should `ClaimsAttachmentAdapter` define a `generateUnsolicited275(claimRef: string, cdaDocument: Buffer): string` method and a `parseSolicited277CA(ediText: string): AttachmentRequest` method as the two required interface methods — encoding the full unsolicited + solicited lifecycle? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: CORRECTION — `parseSolicited277CA` is the wrong method name; 277CA (005010X214) is a claim receipt acknowledgment, NOT the solicited attachment request; the correct payer-side transaction is 277RFAI (005010X317) "Health Care Claim Request for Additional Information"; CMS-0053-F does NOT mandate payer-side 277RFAI electronic issuance by May 2028 — only provider-side 275 is mandated; solicited path is Phase 2 (fax/letter fallback sufficient for MVP); `AttachmentRequest` output type fully specified; Cigna + CMS/Medicare + BCBS KS + NGS Medicare have confirmed 275 companion guides; UHC confirmed 275 support but no public companion guide URL; Aetna/Humana/Anthem not confirmed → [docs/research/agreement-layer/x12-277rfai-solicited-attachment-request-parser-spec.md](agreement-layer/x12-277rfai-solicited-attachment-request-parser-spec.md)
- Should `ClaimsAttachmentAdapter` rename `parseSolicited277CA()` to `parseSolicited277RFAI(ediText: string): AttachmentRequest` — correcting the method name to reflect the actual payer-side X12 transaction (277RFAI / 005010X317), not the 277CA acknowledgment — and should this rename be published as a spec amendment before the next feature branch regeneration? — added 2026-05-17, priority: high
- Should `ClaimsAttachmentAdapter` Phase 1 MVP scope be restricted to `generateUnsolicited275()` only — deferring `parseSolicited277RFAI()` to Phase 2 on the grounds that CMS-0053-F does not mandate payer-side 277RFAI electronic issuance by May 2028 and fax/letter fallback is acceptable for solicited requests at MVP scale? — added 2026-05-17, priority: high
- Should cliqueue's hospital onboarding checklist include a "Payer 277RFAI Readiness Check" — asking the hospital which of their payers issue electronic 277RFAI requests vs. paper/fax requests — to determine whether `parseSolicited277RFAI()` must be implemented before first deployment? — added 2026-05-17, priority: medium
- Does the 277RFAI (005010X317) payer attachment control number REF qualifier vary by companion guide — and should `AttachmentRequest` carry a `rawPayerControlRef: { qualifier: string; value: string }` field (preserving the original REF segment qualifier alongside the value) to ensure correct round-trip REF construction in the 275 response regardless of payer-specific variation? — added 2026-05-17, priority: medium
- Should `@cliqueue/x12-275` (or the 275 sub-module of `@cliqueue/claim-attachments`) support dual-version output (005010X210 and 006020X314) via a `version` parameter — or target only 6020 for MVP, accepting that some payers still on 5010 require a configuration toggle? — added 2026-05-17, priority: medium
~~- Is there a payer adoption threshold (what % of US claim volume is covered by payers currently accepting X12 275 electronically as of May 2026) — and should cliqueue's MVP deployment guide restrict the `ClaimsAttachmentAdapter` to payers with published companion guides, deferring custom companion guide support to Phase 2? — added 2026-05-17, priority: medium~~ → investigated 2026-05-17: YES — companion-guide-restricted MVP scope confirmed correct; 32% electronic nationally (2024 CAQH Index); confirmed payers with guides: CMS Medicare, UHC (006020X314 + 9 clearinghouses), Cigna (005010X210), BCBS KS, NGS Medicare, Highmark; Aetna functionally active (8 clearinghouses, no explicit 275 branding); Elevance implementing (CAQH CORE webinar June 2025); Humana unconfirmed; estimated 55–70% of US claim volume covered by confirmed/implementing payers → [docs/research/market/x12-275-payer-adoption-mvp-scope.md](market/x12-275-payer-adoption-mvp-scope.md)
- Does Humana have an internal X12 275 trading partner specification distributed only via onboarding (not publicly posted) — and should cliqueue's payer onboarding checklist include a step for hospitals to request Humana's trading partner specification before deploying the attachment adapter? — added 2026-05-17, priority: high
- Should `@cliqueue/x12-275` export a `SUPPORTED_PAYERS` runtime registry (mapping payer EDI ID to companion guide version + ISA qualifiers) — enabling the hospital agent to validate payer readiness before generating a 275 envelope and surfacing an actionable error for unsupported payers? — added 2026-05-17, priority: high
- Given that 68% of attachment transactions remain non-electronic as of the 2024 CAQH Index (41% manual, 27% web portal), should cliqueue's hospital onboarding pitch frame the `ClaimsAttachmentAdapter` as a "CMS-0053-F readiness accelerator" (delivering May 2028 compliance 24 months early) rather than a "currently adopted standard" — and does this framing improve urgency for revenue cycle directors evaluating cliqueue in 2026? — added 2026-05-17, priority: high
- Should cliqueue's hospital onboarding checklist include a "CMS-0053-F Attachment Compliance Exhibit" documenting the hospital's X12 275/277 + CDA readiness before first claim submission — and should it specify that the May 2028 compliance deadline requires hospital IT vendor engagement at least 12 months before that date? — added 2026-05-17, priority: high
~~- Does any open-source TypeScript or Node.js library support HL7 CDA R2 document generation for X12 275 attachment payloads — or must cliqueue hand-roll a CDA template generator (parallel to the confirmed hand-roll requirement for `@cliqueue/cds-hooks-client` and `@cliqueue/pa-lifecycle`)? — added 2026-05-17, priority: medium~~ → investigated 2026-05-17: no dedicated npm library exists; @medplum/ccda is CCD/Patient Summary only (not X12-275-specific); hand-roll @cliqueue/cda-attachments using xmlbuilder2 confirmed as third ecosystem gap → [docs/research/agreement-layer/hl7-cda-r2-typescript-npm-ecosystem-x12-275.md](agreement-layer/hl7-cda-r2-typescript-npm-ecosystem-x12-275.md)
~~- Should `@cliqueue/cda-attachments` support only the CMS-0053-F attachment document types explicitly listed (medical records, imaging, clinical notes, telemedicine documentation, lab results) — or build a generic C-CDA R2.1 template engine extensible for any LOINC document type code? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: SCOPED to 6 practically relevant C-CDA R2.1 document templates (Progress Note, Consultation Note, Operative Note, Procedure Note, Discharge Summary, Unstructured Document); lab/imaging are section-level structures inside these templates, not standalone document types; CMS-0053-F does NOT cover prior auth (PA is CMS-0057-F scope); pure Node.js Schematron validation confirmed via `cda-schematron-validator` v1.1.12 (Oct 2022, HL7-used, no Java) → [docs/research/agreement-layer/hl7-cda-r2-typescript-npm-ecosystem-x12-275.md](agreement-layer/hl7-cda-r2-typescript-npm-ecosystem-x12-275.md)
~~- Should `@cliqueue/cda-attachments` be validated against the HL7 CDA R2 schema (`CDA.xsd`) and C-CDA R2.1 Schematron rules via a Java-based validator in CI (subprocess call or pre-validated XML fixture set) — and should cliqueue publish a set of known-valid CDA attachment fixtures as part of the hospital onboarding package? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO Java required — `cda-schematron-validator` v1.1.12 is pure Node.js (xmldom + xpath), used in HL7/fhir-cda-validation; Docker alternative via KvalitetsIT/cda-validator for stricter conformance; NIST online validator decommissioned 2018, no CLI/Docker available; publish known-valid XML fixtures as primary CI artifact → [docs/research/agreement-layer/hl7-cda-r2-typescript-npm-ecosystem-x12-275.md](agreement-layer/hl7-cda-r2-typescript-npm-ecosystem-x12-275.md)
- Should `@cliqueue/cda-attachments` include a `cdaAttachmentScope` TypeScript constant listing the 6 supported document template OIDs and their LOINC document type codes — published in the hospital onboarding checklist as a formal scope declaration, so hospital integration engineers know which attachment types trigger the CMS-0053-F path vs. the manual-submission fallback? — added 2026-05-17, priority: high
~~- Does `cda-schematron-validator` v1.1.12 correctly validate documents against the full C-CDA R2.1 Schematron rule set including VSAC value set assertions — or does it require separate VSAC API access to resolve value set bindings, which would require an NLM API key in CI? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: NO VSAC required — the terminology server (tx.fhir.org) is needed only for Schematron *generation* from the IG (one-time IG-author step), not for running validation. `cda-schematron-validator` v1.1.12 evaluates locally against pre-generated `.sch` + optional local `voc.xml` resource files. No NLM API key required in CI. Structural conformance (template OIDs, required sections, cardinality) is fully catchable offline. Value-set membership checks not in local voc.xml are surfaced as warnings (suppressible via `{ includeWarnings: false }`), not hard failures. XPath 2.0 limitations documented — must validate pre-generated `.sch` compatibility. → [docs/research/agreement-layer/cda-schematron-vsac-ci-validation.md](agreement-layer/cda-schematron-vsac-ci-validation.md)
- Should `@cliqueue/cda-attachments` commit the pre-generated C-CDA R2.1 `.sch` Schematron file directly into the repo — accepting that IG updates require a manual re-generation step — or generate it dynamically in CI using `fhir-cda-validation` with a tx.fhir.org terminology server call? — added 2026-05-17, priority: medium
~~- Does the C-CDA R2.1 `.sch` file generated by `fhir-cda-validation` require modifications to work with `cda-schematron-validator` v1.1.12's XPath 1.0 engine — since the tool's README notes "some limitations with XPath 2.0 functions and variable resolution", and does this limit structural conformance coverage for any of the 6 supported document template types? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES — HL7/fhir-cda-validation README (primary source) documents three confirmed gaps: (1) `current()` XPath 2.0 function not supported, tested manually; (2) dateTime comparisons not supported (e.g., "procedure start before document date"); (3) value sets with apostrophes excluded at generation. Variable resolution pre-processed via `<let>` literal substitution (not full Schematron). Tool self-assesses "seems to be catching most rules." Unstructured Document least affected; Operative Note, Procedure Note, Discharge Summary most affected (temporal ordering). Two-tier CI architecture confirmed: `cda-schematron-validator` (Tier 1, fast/offline) + KvalitetsIT Docker (Tier 2, Saxon-HE XSLT 2.0, release builds) → [docs/research/agreement-layer/cda-schematron-vsac-ci-validation.md](agreement-layer/cda-schematron-vsac-ci-validation.md)
- Should `@cliqueue/cda-attachments` publish a formal "Schematron Coverage Gap Memo" alongside the hospital onboarding checklist — disclosing the `current()` and dateTime XPath 2.0 limitation of `cda-schematron-validator` and documenting which rule classes are covered vs. not covered for the 6 supported templates? — added 2026-05-17, priority: high
- Should the KvalitetsIT/cda-validator Docker gate be mandatory in CI for all PRs touching `@cliqueue/cda-attachments` fixture files — or restricted to release builds to avoid Docker dependency in fast PR feedback loops? — added 2026-05-17, priority: medium
- Can cliqueue pre-audit the committed `.sch` file to enumerate all assertions using `current()` or XPath 2.0 date functions — producing a definitive count of "manually tested rules" — so hospital integration engineers know the exact coverage boundary? — added 2026-05-17, priority: medium
- Should `@cliqueue/cda-attachments` bundle a subset of `voc.xml` vocabulary files from the C-CDA 2.1 IG build covering only the 6 supported document template value sets — enabling offline terminology membership checks without shipping the full C-CDA vocabulary file? — added 2026-05-17, priority: medium
- Should `@cliqueue/cda-attachments` publish a set of known-valid CDA XML fixtures (one per supported document template type) as the primary CI validation artifact alongside Schematron test results — allowing payers to verify attachment conformance against the fixtures without running the Schematron engine themselves? — added 2026-05-17, priority: medium
- Does `@medplum/ccda`'s `convertFhirToCcda()` produce a valid C-CDA R2.1 document usable as a base template for non-patient-summary attachment types (consult note, operative note) if a custom FHIR Composition resource is passed — or is the IPS bridge hardcoded and incompatible with claims-attachment document types, ruling out @medplum/ccda even as a devDependency? — added 2026-05-17, priority: medium
- Should `@cliqueue/cda-attachments` be published as the fourth module in the `@cliqueue/*` monorepo alongside `@cliqueue/cds-hooks-client`, `@cliqueue/pa-lifecycle`, and `@cliqueue/contracts` — and should a shared `@cliqueue/fhir-types` internal package vendor only the minimal FHIR/CDA extension interfaces needed across all four modules? — added 2026-05-17, priority: high
~~- What is the optimal off-chain key-value store for the `(claimId → CLM01)` EDI adapter mapping — hospital-local encrypted DB, a FHIR server, or TEE-backed service — and how does it survive provider system failure without enabling PHI reconstruction from on-chain data? — added 2026-05-14, priority: high~~ → investigated 2026-05-16 → [docs/research/agreement-layer/edi-adapter-offchain-store-phi-reconstruction-risk.md](agreement-layer/edi-adapter-offchain-store-phi-reconstruction-risk.md)
~~- How should the `payerClaimRef` bytes32 slot in the Claim struct be handled when the payer has not yet assigned CLP07 at Submitted state — placeholder updated at Adjudicated transition (warm SSTORE cost) or initialized at submission (new slot cost)? — added 2026-05-14, priority: medium~~ → investigated 2026-05-17: Approach B (leave uninitialized) — both approaches are gas-equivalent on Somnia (200K for cold non-existent non-zero write at adjudication regardless); Approach A adds a spurious free-but-meaningless zero-write; DeFi canonical (Uniswap v3, Aave v3) is Approach B; 835 adapter must handle payerClaimRef == bytes32(0) as "CLP07 pending"; emit PayerClaimRefSet event at adjudication to avoid cold-SLOAD at settlement → [docs/research/agreement-layer/payerclaimref-slot-initialization-strategy.md](agreement-layer/payerclaimref-slot-initialization-strategy.md)
~~- Should `ClaimsAdjudicator` emit a `PayerClaimRefSet(bytes32 indexed claimId, bytes32 payerClaimRef)` event at adjudication — so the off-chain 835 adapter can cache the CLP07 hash without reading contract state at settlement time, avoiding the 1M cold-SLOAD for the adapter's reconciliation path? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: YES — DeFi canonical pattern (Uniswap/Aave/Compound/MakerDAO); LOG2 = 18,560 gas on Somnia (negligible); CLP07 is payer ICN assigned at adjudication; caching in hospital-local PostgreSQL from event is the industry standard 835 practice; eliminates cold-SLOAD from reconciliation hot path → [docs/research/agreement-layer/payerclaimrefset-event-835-adapter-caching.md](agreement-layer/payerclaimrefset-event-835-adapter-caching.md)
- Should `PayerClaimRefSet` carry `bytes32 indexed payerClaimRef` as a second indexed topic (LOG3, 23,680 gas on Somnia) — enabling payer systems to subscribe to their own ICN-space events and detect duplicate-payment risk without a provider-side lookup — or keep it LOG2 (18,560 gas) with only `claimId` indexed? — added 2026-05-17, priority: medium
- Should `IClaimsAdjudicator` declare `PayerClaimRefSet` alongside `ClaimSubmitted` and `ClaimAdjudicated` as an interface-level event — binding Ormi subgraph definitions to the interface ABI rather than the implementation contract? — added 2026-05-17, priority: high
- Should the hospital BAA's "835 Adapter Reference Implementation" exhibit include the `PayerClaimRefSet` cache schema (`claimId TEXT PRIMARY KEY, payerClaimRef BYTEA, cachedAt TIMESTAMPTZ`) as a normative artifact — so hospital IT vendors know the expected local store structure without reading the contract ABI? — added 2026-05-17, priority: medium
- Should the `Claim` struct's Slot 0 pack a `hasPayerRef` bool alongside `status` to allow O(1) determination of whether `payerClaimRef` has been set without incurring a cold-SLOAD of Slot 6 just to check if it's zero? — added 2026-05-17, priority: medium
- At what Somnia LRU cache occupancy (number of active claims per hospital per day) does Slot 6 (`payerClaimRef`) reliably stay within the 128M-slot LRU between submission and adjudication — and does the typical hospital workflow (adjudication within hours) keep it warm enough to avoid the 1M cold-SLOAD at settlement? — added 2026-05-17, priority: medium
- Should the `Superseded` and `Voided` terminal states be collapsed into one `Cancelled` state with a sub-reason byte, or kept separate to allow distinct downstream payer-system reconciliation? — added 2026-05-14, priority: medium
- How many hops deep can the replacement chain realistically grow (original → replacement → re-replacement), and should cliqueue enforce a maximum chain depth on-chain to prevent unbounded linked-list traversal in dispute resolution? — added 2026-05-14, priority: medium
- Should `payerICNHash` be stored as an indexed Somnia event topic or as contract state in an `ICNRegistry` mapping — and does Ormi's 120ms indexing lag introduce a race condition where a replacement 837 could arrive before the indexer catches up with `ClaimAdjudicated`? — added 2026-05-14, priority: medium
- What HMAC key rotation policy is required for the key used to generate both `claimId` and `payerICNHash`? If the key is rotated, does the system need an on-chain `(oldKey, newKey, rotation_timestamp)` migration record to maintain backward lookups for older claims? — added 2026-05-14, priority: medium
- Does Ormi's centralized hosting model require a separate BAA with Ormi Labs before cliqueue indexes HMAC-hashed claim data (even though the raw values are not PHI)? — added 2026-05-14, priority: low

### Track 3 — Somnia

- How does Somnia's cold-SLOAD 1M-gas surcharge interact with a claims state machine that may need to read old (cold) claim records for dispute resolution? Should disputes involve a separate hot-cache tier contract? — added 2026-05-14, priority: high
- What is the economic model for cliqueue agent-invocation deposit per claim? At ~$0.03/invocation, what does per-claim economics look like vs. the $4–10 outsourced coding benchmark? — added 2026-05-14, priority: high
~~- Does Somnia Phase 2 "Full Agent SDK" allow deploying a fine-tuned model (e.g., Llama fine-tuned on ICD-10 coding) as a custom on-chain agent, or only general-purpose LLMs? What is the expected Phase 2 timeline within 2026? — added 2026-05-14, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/phase2-custom-agents-and-llm-model-opacity.md](somnia/phase2-custom-agents-and-llm-model-opacity.md)
- For the off-chain RAG retrieval database backing the coding agent (if Corti Symphony is not used), what is the minimum ICD-10 code description + historical-claims dataset required to achieve commercial CAC-comparable accuracy (>90% automation rate), and is a FHIR Terminology Server sufficient as the retrieval corpus? — added 2026-05-14, priority: medium (deprioritized: Corti Symphony via MCP is now the preferred path)
- For the off-chain RAG retrieval database backing the coding agent, what is the minimum ICD-10 code description + historical-claims dataset required to achieve commercial CAC-comparable accuracy (>90% automation rate), and is a FHIR Terminology Server sufficient as the retrieval corpus? — added 2026-05-14, priority: high
- Does Corti's on-premise/self-hosted tier (limited preview as of Summer 2025) include the full Symphony medical coding model, or only speech/real-time clinical capture? — added 2026-05-14, priority: high
~~- What is Corti's standard BAA turnaround time for enterprise procurement, and can cliqueue pre-negotiate a standard form BAA for hospital onboarding rather than per-customer negotiation? — added 2026-05-14, priority: high~~ → investigated 2026-05-17: no self-serve BAA; 2–6 week enterprise timeline; Hospital→cliqueue→Corti sub-BAA chain legally valid under 45 CFR 164.502(e)(1)(ii); cliqueue should hold master Corti BAA as primary BA → [docs/research/regulatory/corti-baa-procurement-timeline-sub-baa-chain.md](regulatory/corti-baa-procurement-timeline-sub-baa-chain.md)
- Should cliqueue's hospital BAA include a "Subprocessor Exhibit" listing Corti by name (Azure US data region, FedRAMP/HIPAA certification status) — giving hospital privacy officers pre-packaged due-diligence documentation and eliminating per-hospital Corti security review? — added 2026-05-17, priority: high
- Should the cliqueue↔Corti master BAA include an annual SOC 2 Type II attestation clause — binding Corti to provide an annual audit report — to preemptively satisfy the proposed 2026 HIPAA Security Rule annual BA verification requirement before the final rule is published? — added 2026-05-17, priority: high
- Should cliqueue publish a "Vendor Procurement Timeline Exhibit" in the hospital onboarding checklist stating that the cliqueue↔Corti master BAA is already executed — so hospital procurement teams do not independently initiate Corti BAA negotiations, which would fragment cliqueue's subprocessor chain and create a parallel direct-BA relationship? — added 2026-05-17, priority: high
- Under the proposed 2026 HIPAA Security Rule final rule, does the annual business associate verification requirement apply to API-based AI vendors like Corti the same way as data storage vendors? — added 2026-05-14, priority: medium
~~- What on-chain credential representation is appropriate for a HumanAttestor in inpatient claims — a hash of (credentialNumber + attestorAddress + claimId), a delegated NFT, or a hospital-issued verifiable credential? — added 2026-05-14, priority: high~~ → investigated 2026-05-15
- Does Fathom's forthcoming inpatient DRG autonomous coding capability make cliqueue's human-attestor agent role irrelevant, or does on-chain settlement remain orthogonal to the choice of AI coder? — added 2026-05-14, priority: medium
- Given that Humana/Cigna already require credentialed-coder attestation for AI-coded claims contractually, does anchoring that attestation on Somnia (rather than paper/PDF workflows) represent sufficient differentiated value to justify the feature? — added 2026-05-14, priority: high
~~- What is the empirical end-to-end latency for a Somnia `createAdvancedRequest(subcommitteeSize=5)` LLM Inference callback on mainnet? Is the `timeout` parameter in seconds or blocks, and what is a safe default that avoids stuck claims? — added 2026-05-14, priority: high~~ → investigated 2026-05-15
- What is the empirically observed wall-clock time for a `createAdvancedRequest(subcommitteeSize=3, threshold=2)` LLM Inference callback on Somnia mainnet — can a test call confirm whether it completes in <30 seconds, and does increasing subcommitteeSize to 5 measurably increase latency? — added 2026-05-15, priority: high
- Does the Somnia platform contract emit a `RequestCreated` event at submission and a `RequestFulfilled` event at callback, enabling the UI to show a live timer from submission to attestation without polling contract state? — added 2026-05-15, priority: medium
~~- Can `inferToolsChat` call back to on-chain contract state as a "tool" (e.g., read prior claim records for modifier-code lookups) without PHI leaving the TEE boundary? — added 2026-05-14, priority: medium~~ → investigated 2026-05-15 → [docs/research/somnia/phase2-custom-agents-and-llm-model-opacity.md](somnia/phase2-custom-agents-and-llm-model-opacity.md)
~~- Off-chain pub-sub / messaging patterns paired with Somnia events for PHI-bearing payloads. Existing approaches (Curie's encrypted-pubsub, Akiri's, etc.) and trade-offs. — added 2026-05-14, priority: medium~~ → investigated 2026-05-17: AWS SQS FIFO + KMS-encrypted S3 is recommended MVP broker (AWS BAA; SQS carries only claimIdHash + S3 pointer, not PHI bytes); NATS JetStream viable for production with HIPAA-compliant Kubernetes infra (no vendor BAA, must self-configure TLS + disk encryption + access controls); `claimId` stored as `DocumentReference.identifier` business key (NOT FHIR resource ID); FHIR Bundle "claim package" is de facto pattern for bundling 837+CDA+metadata (no HL7 standard defines this); PHI-never-on-chain preserved → [docs/research/somnia/offchain-pubsub-phi-messaging-architecture.md](somnia/offchain-pubsub-phi-messaging-architecture.md)
- Should the hospital BAA's "Off-Chain Messaging Exhibit" specify the minimum AWS SQS FIFO configuration (KMS key type, message retention window, dead-letter queue settings) as normative deployment prerequisites — so hospital IT vendors have a specific compliance checklist rather than relying on general HIPAA guidance? — added 2026-05-17, priority: high
- Should cliqueue publish a reference `claimId → documentReference` mapping schema (PostgreSQL DDL: `claimId BYTEA PRIMARY KEY, documentReferenceId UUID, s3Uri TEXT, createdAt TIMESTAMPTZ`) as part of the hospital onboarding package — standardizing the off-chain correlation store across all hospital deployments? — added 2026-05-17, priority: high
- Should the FHIR Bundle "claim package" envelope pattern be published as a `ClaimPackageBundle` TypeScript type in `@cliqueue/fhir-types` — giving hospital integration engineers a typed construct assembling the 837 Binary + DocumentReference + Parameters entries in FHIR-valid format, versioned alongside `PaStatusEncoder`? — added 2026-05-17, priority: medium
- PHI hashing / commitment schemes for finalized ICD-10 claim batches: merkle roots, ZK proofs of code validity, commit-reveal patterns. Which schemes maintain HIPAA de-identification compliance? — added 2026-05-14, priority: medium
- Bridging Somnia → fiat rails or stablecoin payment for actual fund release: what are the available bridges, and what are the latency and compliance implications? — added 2026-05-14, priority: medium
~~- Has zkVerify deployed or does it plan to deploy its `IZkVerify` receiver contract on Somnia mainnet (chain ID 5031)? If not, can cliqueue deploy it permissionlessly as the EVM receiver, or is first-party zkVerify team involvement required to ensure attestation routing? — added 2026-05-14, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/zkverify-somnia-chain-support-and-alternatives.md](somnia/zkverify-somnia-chain-support-and-alternatives.md)
- Does Phala's dstack KMS support multi-chain governance contract deployment (e.g., primary on Ethereum + mirror/relay on Somnia), or is the KMS governance chain a singleton that must be chosen at deploy time, creating a cross-chain dependency for every TEE key issuance? — added 2026-05-14, priority: high
- Under the proposed 2026 HIPAA Security Rule NPRM, does anchoring enclave code hashes (bytes32, no PHI) on Somnia's public chain constitute a "disclosure" to the validator set, or is it HIPAA-neutral because the on-chain value cannot be reversed to PHI? — added 2026-05-14, priority: medium
~~- Should the `SBTRegistry` for HumanAttestor credentials be hospital-scoped (each hospital deploys its own ERC-5484 contract) or network-scoped (cliqueue deploys one registry, hospitals call `grantRole(attestor, hospitalId, credentialType)`)? — added 2026-05-15, priority: high~~ → investigated 2026-05-15
~~- What is the liability exposure if a hospital's SBTRegistry contains a stale entry (coder left, SBT not revoked) and a fraudulent attestation is anchored — should an on-chain `credentialExpiry < block.timestamp` check be enforced at claim submission rather than relying solely on revocation? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/sbt-credential-expiry-enforcement.md](somnia/sbt-credential-expiry-enforcement.md)
- Does the AHIMA (`my.ahima.org/credential-verification`) or AAPC (`aapc.com/certification/credential-verification.aspx`) credential verification API have terms-of-service restrictions on automated verification calls that would block cliqueue from validating credential status before issuing an SBT? — added 2026-05-15, priority: medium
~~- Should the `renewExpiry(tokenId, newExpiry)` function in the SBTRegistry enforce a maximum `newExpiry` ceiling (e.g., `block.timestamp + 31 days`) to prevent a compromised `HOSPITAL_ADMIN` key from minting a de-facto permanent credential by setting `expiry = type(uint64).max`? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/sbt-credential-expiry-enforcement.md](somnia/sbt-credential-expiry-enforcement.md)
- Does AHIMA's credential verification portal support automated/API access, or does programmatic PSV require a commercial third-party aggregator (Verisys, Symplr, Modio) — and what are their pricing and BAA terms for integration with cliqueue's hospital onboarding flow? — added 2026-05-15, priority: medium
- Does the proposed 2026 HIPAA Security Rule NPRM's annual BA verification requirement apply to credentialing aggregator services (Verisys, Symplr) querying AHIMA/AAPC on cliqueue's behalf — would that require a BA chain: hospital → cliqueue → aggregator → AHIMA? — added 2026-05-15, priority: medium
- Does the proposed 2026 HIPAA Security Rule NPRM's annual BA verification requirement, if finalized, require cliqueue to implement an automated threshold-audit mechanism that checks each hospital's `batchThreshold` mapping against its onboarding attestation on a regular schedule — and if so, should this be a Somnia event log check or an off-chain monitoring service? — added 2026-05-15, priority: medium
- Should the `NETWORK_BATCH_FLOOR` constant be embedded in contract bytecode (truly immutable) or stored as a governance-controlled `uint32` behind the `TimelockController` with a minimum value validation — and does making it governable create the same bootstrap paradox as the stablecoin governance timelock? — added 2026-05-15, priority: medium
- EAS (Ethereum Attestation Service) is not yet deployed on Somnia mainnet — can cliqueue deploy EAS contracts permissionlessly on Somnia and use off-chain EAS attestations (zero-gas) for per-claim attestor sign-off at high volume? — added 2026-05-15, priority: medium
- Does AHIMA/AAPC credential verification API rate-limiting allow cliqueue to pre-validate credentials before SBT minting at onboarding time, or must each hospital verify credentials off-chain before calling `grantRole()`, creating a potential verify-then-mint race condition? — added 2026-05-15, priority: medium
~~- Should the `hospitalId` embedded in the SBTRegistry token metadata be a `bytes32` hash of the hospital's NPI (National Provider Identifier) — enabling third-party verification without a separate on-chain NPI registry — and does NPI-to-attestor-address correlation on a public chain constitute a HIPAA disclosure under the proposed 2026 Security Rule? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/sbt-hospitalid-npi-hash-hipaa-disclosure.md](somnia/sbt-hospitalid-npi-hash-hipaa-disclosure.md)
- If a locum tenens coder holds multiple SBTs (one per `hospitalId`), should the `ClaimsAdjudicator` enforce a maximum concurrent active-SBT count per attestor address to prevent a single compromised key from becoming a cross-hospital fraud vector? — added 2026-05-15, priority: medium
- Should the off-chain monitoring system for `SBTRenewed` events be implemented as a Somnia WebSocket `eth_subscribe` subscription or as an Ormi subgraph alert — and does either approach satisfy the "automated traceable audit" requirement in the January 2026 Joint Commission credentialing standard? — added 2026-05-15, priority: medium
~~- If a hospital's JCAHO audit reveals PSV was not completed in a given month (e.g., staff shortage), should the `ClaimsAdjudicator` allow a 72-hour grace window past SBT `expiry` to prevent catastrophic claim submission failure — and does a grace window open an exploitable window for stale-credential attestations? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/sbt-expiry-grace-window-jcaho-fca.md](somnia/sbt-expiry-grace-window-jcaho-fca.md)
- Should cliqueue's hospital onboarding checklist include a specific list of approved continuous PSV monitoring vendors (Verisys, Symplr, ProviderTrust) — and should cliqueue negotiate a partnership/referral with one vendor to reduce onboarding friction for hospitals that do not already have automated monitoring in place? — added 2026-05-15, priority: medium
- Should the `SBTRegistry` emit a `CredentialExpiryWarning(attestorAddress, hospitalId, expiry, warningThreshold)` event at `expiry - 7 days` to allow the hospital's off-chain monitoring system to trigger `renewExpiry()` proactively — and does emitting this event on a public chain create a HIPAA or PII concern for individual coders? — added 2026-05-15, priority: medium
~~- What is the minimum staffing redundancy (e.g., minimum 2 active SBT holders per `hospitalId`) cliqueue should require at onboarding to prevent a single-coder absence from triggering catastrophic claim submission failure — and should this minimum be enforced on-chain in `ClaimsAdjudicator` as a `minAttesters` parameter? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/min-attesters-staffing-redundancy.md](somnia/min-attesters-staffing-redundancy.md)
- If the CLIQUEUE_DOMAIN_SEPARATOR pattern for hospitalId is adopted, should the derivation and separator value be published in cliqueue's on-chain contract metadata (via EIP-1967 storage slot or NatSpec annotation) to facilitate third-party audits without requiring cliqueue's cooperation? — added 2026-05-15, priority: medium
- Should the `SBTRegistry` expose a `view function hospitalIdFromNPI(uint256 npi) returns (bytes32)` to allow on-chain lookup by any party with NPPES access, or should this remain an off-chain operation only (to preserve the option of adding a real salt later)? — added 2026-05-15, priority: low
- Does the permanent immutability of Somnia on-chain data (no deletion possible) create a GDPR Right to Erasure conflict for EU-national individual coders who may hold attestor SBTs, and does the `BurnAuth.IssuerOnly` burn mechanism under ERC-5484 satisfy GDPR erasure requirements? — added 2026-05-15, priority: medium
~~- Does Somnia's `inferToolsChat` `mcpServerUrls` parameter allow specifying a hospital-local (non-public) MCP endpoint, or does it require a publicly reachable URL — and if public-only, does transmitting de-identified code arrays through validator nodes in non-EU jurisdictions create GDPR or HIPAA data-residency issues? — added 2026-05-15, priority: medium~~ → investigated 2026-05-17: NO explicit public-only requirement in docs ("use reachable URLs"); official example URL `http://weather-service:80/` is a Docker Compose private hostname (implies co-deployment, not public internet); validators globally distributed (Frankfurt/NY/Singapore), no static egress IPs published; MCP HTTP call made by Somnia agent orchestrator (not each validator individually — WEAK, unconfirmed); no MCP authentication header parameter exists → API key cannot be passed via inferToolsChat without URL-embedding (on-chain exposure risk); de-identified ICD-10 arrays passing through validator nodes are HIPAA-neutral (not PHI); GDPR Art. 9 applies only if clinical notes transmitted (they must not be); **confirmed security requirement: Corti Symphony calls must NOT route through inferToolsChat** → [docs/research/somnia/infer-tools-chat-mcp-endpoint-network-requirements.md](somnia/infer-tools-chat-mcp-endpoint-network-requirements.md)
- Does the Somnia agent orchestrator (not individual validators) make the MCP HTTP call — and if so, can cliqueue obtain a static egress IP for the orchestrator to allowlist at a hospital-deployed MCP gateway, enabling hospital-side Symphony calls without public internet exposure? — added 2026-05-17, priority: high
~~- Is there a documented mechanism to pass per-request authentication secrets to MCP servers in `inferToolsChat` without embedding them in the URL — or is a reverse proxy pattern (hospital publishes a URL that validates requests via mTLS before forwarding to Symphony) the only viable option? — added 2026-05-17, priority: high~~ → investigated 2026-05-17: nginx credential injection is the viable pattern (proxy accepts unauthenticated inbound URL from Somnia, injects real API key outbound via `proxy_set_header`); mTLS eliminates keys entirely on the nginx→Corti leg but cannot authenticate the Somnia→nginx leg (inferToolsChat cannot present a client cert); OAuth/Bearer require header injection from the MCP client — impossible with inferToolsChat URL-only constraint; CONFIRMED: `mcpServerUrls` URL path segment (128-bit random) is the only available inbound authenticator; URL-as-secret is acceptable if treated as rotatable credential (never committed to public repos, never emitted on-chain) → [docs/research/somnia/mcp-reverse-proxy-auth-without-url-key.md](somnia/mcp-reverse-proxy-auth-without-url-key.md)
- Should cliqueue's hospital onboarding package include a reference nginx configuration (with 128-bit random path + `proxy_set_header Authorization "Bearer ${CORTI_API_KEY}"`) as a normative deployment artifact — so hospital IT vendors implement the proxy consistently and avoid URL-embedded API keys in on-chain calldata? — added 2026-05-17, priority: high
- Should the `mcpServerUrls` path segment (the URL secret) be derived from the hospital's `hospitalId` bytes32 using HKDF expansion — enabling deterministic, auditable URL rotation when `hospitalId` rotates, without requiring a separate secret management step? — added 2026-05-17, priority: medium
- Should the hospital BAA's Off-Chain Infrastructure Exhibit require the hospital to rotate the `mcpServerUrls` path segment at least annually — treating it as a long-lived credential subject to the same rotation policy as the AWS KMS key used for S3 encryption, with a contractual obligation to update the `inferToolsChat` call parameter within 5 business days of rotation? — added 2026-05-17, priority: high
- If `inferToolsChat` is restricted to de-identified ICD-10 context (no Symphony API key, no PHI), what is the practical accuracy of Somnia's native LLM for second-opinion ICD-10 code verification compared to Corti Symphony — and is a lower-accuracy second opinion still valuable for the on-chain audit trail? — added 2026-05-17, priority: medium
- Does the `onchainTools` parameter in `inferToolsChat` count against the `maxIterations` budget, and what is the practical maximum `maxIterations` before the 15-minute default timeout is exceeded for a 5-validator subcommittee under normal load? — added 2026-05-15, priority: medium
- When Phase 2 "Custom User-Defined Agents" launches on Somnia, will agent registration be permissionless or require Somnia team approval — and does a permissioned model create a procurement/legal bottleneck for a healthcare-specific (HIPAA-adjacent) custom agent deployment? — added 2026-05-15, priority: high
~~- Should `SBTRegistry` maintain an explicit `mapping(bytes32 hospitalId => uint32) private _attestorCount` (incremented/decremented in `grantRole`/`revokeRole`) to make the floor check O(1) gas, or derive it on-demand via `getRoleMemberCount` on a per-hospital role bytes32? — added 2026-05-15, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/sbtregistry-attestor-count-mapping-vs-enumerable.md](somnia/sbtregistry-attestor-count-mapping-vs-enumerable.md)
~~- Should the `_attestorCount` mapping key be `bytes32 hospitalId` (consistent with `CLIQUEUE_DOMAIN_SEPARATOR`) or `address hospitalAdmin` — and does mixing key types between `AccessControl` role hashes and the counter mapping create a key-derivation footgun? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/sbtregistry-attestor-count-key-type-bytes32-vs-address.md](somnia/sbtregistry-attestor-count-key-type-bytes32-vs-address.md)
~~- Should `SBTRegistry` override the raw `grantRole(bytes32, address)` / `revokeRole(bytes32, address)` external functions to `revert` unconditionally — forcing all callers through typed `grantAttestorRole` / `revokeAttestorRole` wrappers — and does this create a UUPS upgrade compatibility issue if OZ governance tooling expects the raw `grantRole` interface? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/sbtregistry-grantRole-revert-override-uups-compatibility.md](somnia/sbtregistry-grantRole-revert-override-uups-compatibility.md)
~~- Should `SBTRegistry` emit a dedicated `AttestorCountChanged(bytes32 indexed hospitalId, uint32 newCount, bool isGrant)` event at the public wrapper layer (not inside `_grantRole`) — so Ormi indexers receive both the count change and the `hospitalId` in a single authoritative event without needing to reconstruct `hospitalId` from the role hash in `RoleGranted`? — added 2026-05-16, priority: medium~~ → investigated 2026-05-16 → [docs/research/somnia/isbregistry-typed-interface-design-dlp-exclusion.md](somnia/isbregistry-typed-interface-design-dlp-exclusion.md)
- At what credential-change frequency per hospital per month does the `_attestorCount` slot reliably stay warm in Somnia's 128M LRU cache — and should cliqueue specify a "keep-warm" operation (e.g., monthly attestor verification ping emitting a no-op event) in the deployment runbook? — added 2026-05-16, priority: medium
~~- Should the `renounceAttestorRole(bytes32 hospitalId)` wrapper require the caller to also hold `HOSPITAL_ADMIN_ROLE` (preventing unilateral self-removal without admin approval) — or should self-renounce remain permissionless (as in OZ base AccessControl) subject only to the `_attestorCount` floor check? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/renounce-attestor-role-admin-cosignature-vs-permissionless.md](somnia/renounce-attestor-role-admin-cosignature-vs-permissionless.md)
~~- Should the typed wrapper functions (`grantAttestorRole`, `revokeAttestorRole`, `renounceAttestorRole`) be published as a standalone `ISBTRegistry` interface in the cliqueue spec — so hospital integration engineers, Gnosis Safe Transaction Builder users, and Tally proposal authors have a canonical ABI to import without coupling to the full contract implementation? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/isbregistry-typed-interface-design-dlp-exclusion.md](somnia/isbregistry-typed-interface-design-dlp-exclusion.md)
~~- Should `renounceAttestorRole(bytes32 hospitalId)` require `HOSPITAL_ADMIN_ROLE` co-signature (preventing unilateral self-removal without admin approval) or remain permissionless (floor-guarded only) — and does requiring admin co-signature create a liveness risk if the admin key is temporarily unavailable? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/renounce-attestor-role-admin-cosignature-vs-permissionless.md](somnia/renounce-attestor-role-admin-cosignature-vs-permissionless.md)
~~- Should `renounceAttestorRole` emit `StaffingFloorReached(bytes32 hospitalId, uint32 currentCount)` when post-renounce count equals exactly `MIN_ATTESTERS_FLOOR` — giving hospital monitoring systems an automatic alert before the next renounce causes a revert mid-claim submission? — added 2026-05-16, priority: high~~ → investigated 2026-05-16 → [docs/research/somnia/staffing-floor-reached-event-design.md](somnia/staffing-floor-reached-event-design.md)
- Should `StaffingFloorReached` also be emitted during `revokeAttestorRole` called by a `HOSPITAL_ADMIN` (not only self-initiated `renounceAttestorRole`) — so monitoring systems receive the alert regardless of which revocation path is used? — added 2026-05-16, priority: low
- Should `ISBTRegistry` declare `StaffingFloorReached` as an interface-level event (alongside `AttestorCountChanged`) — so Ormi subgraph definitions bind to the interface ABI without coupling to the concrete implementation? — added 2026-05-16, priority: medium
- Should a secondary `StaffingRiskWarning(bytes32 indexed hospitalId, uint32 currentCount, uint32 floor)` event fire when count drops to floor + 1 — giving earlier warning before floor is reached, given the 12% annual coding shortage rate? — added 2026-05-16, priority: low
- Should the `ISBTRegistry` NatSpec for `renounceAttestorRole` explicitly document that no admin co-signature is required — preventing hospital security officers from misreading the ABI and assuming admin approval is enforced? — added 2026-05-16, priority: medium
- Should cliqueue's hospital BAA include a "Voluntary Attestor Exit Protocol" section specifying that departing coders must self-renounce via `renounceAttestorRole` or that hospital admin must execute `revokeAttestorRole` within one business day of separation — mirroring the HIPAA §164.308(a)(3)(ii)(C) termination procedure requirement on-chain? — added 2026-05-16, priority: high
- Should `getAttestorFloor()` return a network-wide `uint32` constant or support per-hospital overrides — and if per-hospital floors are added for critical-access hospital carve-outs, does `ISBTRegistry` need a `getAttestorFloor(bytes32 hospitalId)` overload that would break the current `ClaimsAdjudicator` interface coupling? — added 2026-05-16, priority: medium
- Should cliqueue's outside counsel DLP opinion letter specifically analyze Somnia block explorer source code verification (both `ISBTRegistry` and `SBTRegistry` implementation) as the primary "publicly available and accessible" satisfying mechanism — committing to a statement that an unverified (private) implementation contract would break the DLP exclusion? — added 2026-05-16, priority: high
- Should `ISBTRegistry` be published as a separate npm package (`@cliqueue/contracts/interfaces`) alongside the main contract artifacts — so hospital integration engineers can import the interface ABI in TypeScript without depending on the full contract build artifacts, and does this publication separately strengthen the GENIUS Act DLP "publicly available and accessible" characterization? — added 2026-05-16, priority: medium
- Should the network-level `MIN_ATTESTERS_FLOOR` be set to 2 (bare minimum) or 3 (allows one absence while still above 1) given the documented 12% annual coding shortage rate — and does a floor of 3 make cliqueue undeployable at small/critical-access hospitals with limited HIM staff? — added 2026-05-15, priority: high
- When a hospital's active attestor count drops to exactly `MIN_ATTESTERS_FLOOR` (the minimum), should `SBTRegistry` emit a `StaffingFloorReached(hospitalId, currentCount)` event to trigger an off-chain alerting system — and should this event be emitted on every `revokeRole` that reaches (but doesn't breach) the floor, or only on the first crossing? — added 2026-05-15, priority: medium
~~- Does Somnia mainnet's `ecPairing` precompile (address `0x08`, EIP-197) execute correctly — a canary test calling `staticcall(0x08, ...)` with a known valid BN254 pairing should be added to the pre-deployment runbook alongside the existing Paris-level opcode canary tests? — added 2026-05-15, priority: high~~ → investigated 2026-05-15 → [docs/research/somnia/ecpairing-precompile-canary-and-bls12-alternative.md](somnia/ecpairing-precompile-canary-and-bls12-alternative.md)
- For the CV range proof circuit (proving `CV(amounts) >= 20` for n≥11 claims), what is the Circom/snarkjs or Noir proving time on a commodity server — does it complete in <5 seconds wall clock, and can the trusted setup be published to remove cliqueue as a single trust anchor for the proving key? — added 2026-05-15, priority: medium
- Should cliqueue proactively request Somnia chain support from zkVerify/Horizen Labs (via `relayer-support@horizenlabs.io`) as a parallel track alongside the self-hosted Groth16 verifier path, so that the zkVerify relayer option is available post-MVP without requiring a contract redeploy? — added 2026-05-15, priority: low
- Has any developer publicly confirmed ecPairing (0x08) executing correctly on Somnia mainnet (chain ID 5031) — is there a block explorer tx, GitHub issue, or Discord report of a successful staticcall to address(8) producing the correct pairing result? — added 2026-05-15, priority: high
- Does Somnia's `point_evaluation (0x0a)` precompile (EIP-4844, Cancun-era) execute correctly at the listed 50× multiplier, or is it listed in the gas table as a future-state item despite the EVM opcode level being Paris? — added 2026-05-15, priority: medium
- Given that BLS12-381 Groth16 pairings are ~3.8× cheaper than BN254 on Somnia (50× vs 250× multiplier), should the post-MVP ZK path use a gnark-compiled BLS12-381 Groth16 verifier rather than circom/snarkjs BN254, contingent on BLS12_PAIRING_CHECK canary test passing on Somnia mainnet? — added 2026-05-15, priority: medium

---

## Investigated

- Should cliqueue's dispute-listener implement connection affinity to avoid the documented `eth_unsubscribe` failure on Somnia load-balanced endpoints — does this require a dedicated non-load-balanced WSS endpoint from a third-party RPC provider? → YES to connection affinity as a design requirement; two viable paths: (A) GetBlock dedicated node ($1,000+/month, single-backend, recommended for $10M+ TVL production) or (B) application-level reconnect + resubscribe + `eth_getLogs` backfill from hospital-local block checkpoint (recommended for MVP using Somnia's free public WSS). Somnia's own JSON-RPC docs explicitly warn that `eth_subscribe` subscriptions are connection-scoped and `eth_unsubscribe` returns `false` across backends. ethers.js v6 WebSocketProvider does NOT auto-resubscribe; manual reconnect loop required. Standard backfill pattern: checkpoint lastSeenBlock in hospital-local PostgreSQL, fetch logs from `lastSeenBlock+1` to head on reconnect, deduplicate by `(blockNumber, txHash, logIndex)`. — 2026-05-16 → [docs/research/somnia/dispute-listener-websocket-connection-affinity.md](somnia/dispute-listener-websocket-connection-affinity.md)

- Should `renounceAttestorRole` emit `StaffingFloorReached(bytes32 indexed hospitalId, uint32 currentCount)` when post-renounce count equals exactly `MIN_ATTESTERS_FLOOR`? → YES — emit on every floor-crossing from both `revokeAttestorRole` and `renounceAttestorRole`, without the attestor address (general PII risk; hospitalId + count is sufficient for monitoring). Event costs ~1,006 gas with NO IceDB cold-SLOAD penalty (LOG opcodes are exempt from EIP-2929 warm/cold tracking). No OZ/Safe precedent exists for pre-threshold warning events, but the pattern is valid and operationally necessary. The event is consumable via Somnia WebSocket `eth_subscribe` topic filter or Ormi subgraph. — 2026-05-16 → [docs/research/somnia/staffing-floor-reached-event-design.md](somnia/staffing-floor-reached-event-design.md)

- Should cliqueue's hospital onboarding checklist include a one-paragraph CAA-21 gag clause defense memo — arguing that any payer contract clause restricting a hospital from sharing de-identified claims data with cliqueue as a BA is unenforceable under CAA-21? → NO — CAA-21 Section 201 (42 U.S.C. § 300gg-119; ERISA 29 U.S.C. § 1185m) is a plan/issuer-facing obligation, not a provider right. The gag clause prohibition protects the **plan/issuer's** ability to share data with its own BAs — it does not expressly protect **hospital-to-hospital's-vendor** flows. No HHS/DOL/CMS FAQ as of May 2026 addresses provider-vendor data flows. HIPAA does not make payer-provider data restriction clauses unenforceable (HIPAA is permissive, not prescriptive). The strongest cliqueue position is a dual-BA model (cliqueue BAA with both hospital AND payer/plan). The DOL January 2025 "functional restriction" standard may extend protection indirectly if a payer-provider clause blocks plan access, but this is an open interpretive argument requiring outside counsel opinion. The onboarding checklist must require payer contract review by hospital outside counsel — not include a CAA-21 self-executing defense memo. — 2026-05-16 → [docs/research/regulatory/caa21-gag-clause-provider-vendor-data-sharing.md](regulatory/caa21-gag-clause-provider-vendor-data-sharing.md)

- Should cliqueue's hospital BAA include a "Payer Contract Attestation Exhibit" documenting that the SBT on-chain attestation satisfies Humana/Cigna's credentialed coder review contractual requirement — and has any healthcare attorney published analysis of whether on-chain attestation satisfies payer contract language requiring "validated by credentialed coders"? → Yes to the Exhibit — it is a novel but necessary document. Humana/Cigna payer contract language for credentialed coder attestation is contractual (not regulatory) and not publicly available verbatim. No healthcare attorney has published analysis of whether blockchain attestation satisfies typical payer contract clause language as of May 2026. On-chain SBT attestation is legally stronger than PDF workflows under E-SIGN Act / UETA (tamper-evident, BFT-timestamped, independently verifiable by the payer). The CMS Medicare signature standard (MLN905364) treats "Validated by [credentials + date]" as an acceptable electronic attestation — a persuasive but non-controlling analogy for commercial payer contracts. The Exhibit should shift the compliance representation to hospital counsel, document the credential-to-SBT mapping, and recommend hospital engagement of outside healthcare counsel for payer-specific legal opinion. cliqueue should commission the first published legal opinion letter on this question as a go-to-market differentiator. — 2026-05-16 → [docs/research/market/payer-contract-attestation-exhibit-on-chain-sbt.md](market/payer-contract-attestation-exhibit-on-chain-sbt.md)

- Should `renounceAttestorRole(bytes32 hospitalId)` require `HOSPITAL_ADMIN_ROLE` co-signature or remain permissionless (floor-guarded only) — and does admin co-signature create a liveness risk? → Permissionless floor-guarded only. Admin co-signature creates a denial-of-renunciation liveness vector (admin multisig unavailability blocks departed coder self-removal, leaving stale credential active), inverts the trust model (requiring admin approval to *leave* a role), and may delay involuntary-termination access revocation beyond HIPAA §164.308(a)(3)(ii)(C)'s prompt-removal standard. The floor check (`_attestorCount[hospitalId] > MIN_ATTESTERS_FLOOR`) is the sole sufficient protection. No OZ canonical precedent exists for admin co-signature on non-admin role renunciation. — 2026-05-16 → [docs/research/somnia/renounce-attestor-role-admin-cosignature-vs-permissionless.md](somnia/renounce-attestor-role-admin-cosignature-vs-permissionless.md)

- Should `SBTRegistry` override the raw `grantRole(bytes32, address)` / `revokeRole(bytes32, address)` external functions to `revert` unconditionally — and does this break UUPS upgrades or OZ governance tooling? → Yes to override-to-revert for `grantRole`, `revokeRole`, AND `renounceRole` (all three public mutation entry points). This is the `AccessControlDefaultAdminRules` pattern verbatim — OZ itself uses override-to-revert for DEFAULT_ADMIN_ROLE. UUPS upgrade path is completely unaffected (`_authorizeUpgrade` uses internal `_grantRole`, not public `grantRole`). Gnosis Safe Transaction Builder, TimelockController, Tally, and Defender all support encoding arbitrary calldata to typed wrappers (`grantAttestorRole(bytes32, address)`) — none require raw `grantRole` to be callable. The `renounceRole` vector is the most commonly overlooked: attestors can self-remove below the floor unless `renounceRole` is also disabled. The `IAccessControl` interface behavioral contract is broken (grantRole reverts) but selector/ERC-165 compliance is preserved — an intentional ABI policy, same trade-off OZ accepts in DefaultAdminRules. — 2026-05-16 → [docs/research/somnia/sbtregistry-grantRole-revert-override-uups-compatibility.md](somnia/sbtregistry-grantRole-revert-override-uups-compatibility.md)

- Should cliqueue define a `ClaimDisputed(bytes32 indexed claimId, address indexed disputant, uint256 disputeWindowEnd)` event that payer monitoring systems subscribe to via Somnia WebSocket — enabling automated payer back-office notification within the dispute window without polling? → Yes. Somnia's native WebSocket at `wss://api.infra.mainnet.somnia.network/ws` supports standard EVM `eth_subscribe` logs with full `topics[0..3]` indexed filtering. The event must index `claimId` (topic[1]) and `disputant` (topic[2]) to allow payer-agent per-claim subscriptions. A companion `DisputeResolved(bytes32 indexed claimId, bool upheld, uint256 resolvedAt)` event completes the subscribe-once/unsubscribe-on-resolution pattern. No EHR/RCM system (Epic, Waystar) natively consumes blockchain WebSocket events — cliqueue must provide a TypeScript dispute-listener middleware service as a required deliverable. The Somnia Data Streams SDK is a higher-level alternative but likely requires client-side topic[1..3] filtering; raw `eth_subscribe` is the confirmed path for per-claim targeting. Ormi subgraph provides complementary batch-query access but GraphQL subscription support is unconfirmed on Somnia. — 2026-05-16 → [docs/research/somnia/claim-disputed-event-websocket-subscription.md](somnia/claim-disputed-event-websocket-subscription.md)

- What is the actual per-claim gas cost (in USD) for a Somnia native `createAdvancedRequest(subcommitteeSize=5)` call vs. the $4–10 outsourced coding benchmark? → LLM Inference costs 0.40 SOMI (sub=5) or 0.24 SOMI (sub=3) per claim at official platform rates. At spot SOMI ($0.168, May 16 2026), the Somnia oracle layer costs $0.040–$0.067/claim; combined with Corti Symphony ($0.012–0.025) and EVM gas ($0.002), total per-claim cost is $0.054–$0.095 — 43–185× cheaper than the $4–10 outsourced benchmark. Even at ATH SOMI ($1.84), sub=5 costs ~$0.77/claim — still 5× below $4. SOMI price volatility (12× ATL-ATH range) is the primary cost risk; `getAdvancedRequestDeposit()` must be called dynamically. The $0.03/invocation figure referenced in prior docs was the JSON API price, not LLM Inference. — 2026-05-16 → [docs/research/somnia/per-claim-gas-economics-llm-inference-cost.md](somnia/per-claim-gas-economics-llm-inference-cost.md)

- Should cliqueue's off-chain agent layer implement a `SOMI_PRICE_CIRCUIT_BREAKER` that reduces subcommitteeSize 5→3 when estimated cost exceeds $0.50/claim — and is there a ready on-chain SOMI/USD oracle on Somnia? → Yes to circuit breaker (off-chain TypeScript only; no Solidity changes). No ready on-chain SOMI/USD oracle exists on Somnia mainnet: Protofire Chainlink feeds cover only ETH/BTC/USDC; DIA and API3 feeds may support SOMI on request but no deployed mainnet SOMI/USD contract address is publicly confirmed. The off-chain agent should use CoinGecko REST API (`/simple/price?ids=somnia&vs_currencies=usd`) with a 5-minute TTL cache as the SOMI price source, combined with `getAdvancedRequestDeposit(subSize)` on-chain. Two-tier ceiling: `estimatedCostUSD(sub=5) > $0.50` → reduce to sub=3; `estimatedCostUSD(sub=3) > $0.25` → pause + alert. Somnia tokenomics (75% supply still locked through 2029) create structural downward price pressure — current costs ($0.092/claim at sub=5 spot) likely to remain stable through MVP phase. The 0.07 SOMI/agent platform price is not governance-locked. — 2026-05-16 → [docs/research/somnia/somi-price-circuit-breaker-oracle-architecture.md](somnia/somi-price-circuit-breaker-oracle-architecture.md)

- Should the `attestationRequired` governance mapping in `ClaimsAdjudicator` be initialized at deployment with all known TOB second-digit categories, or lazily default to `false` for unmapped `claimType` values? → Initialize eagerly at deployment — the lazy-default-zero pattern is a documented smart contract security risk for access-control mappings (silent attestation bypass for unregistered claim types). Somnia's 1M-gas cold-SLOAD surcharge applies ONLY to slots that were previously written but are not in the hot cache; it does NOT apply to never-written (uninitialized) slots. Paradoxically, eager initialization creates cold-SLOAD exposure for rarely-used types (digit 4, digit 6), but this is the correct security trade-off. UB-04 has 8 active second-digit facility types (1–4, 6–9); digit 5 discontinued 10/1/05. IPO list phase-out (2026–2029) does NOT create new TOB codes — removed procedures shift to existing TOB 13X. A `validClaimType` revert guard (not just a mapping default) is required. — 2026-05-16 → [docs/research/market/attestation-required-mapping-initialization-tob-taxonomy.md](market/attestation-required-mapping-initialization-tob-taxonomy.md)

- Should cliqueue define a formal `CodingConfidenceHeuristic` specification — a weighted scoring function over `evidences.length`, `alternatives.length`, and `inCandidatesArray` — and publish it as a testable TypeScript module in the off-chain agent spec? → Yes. No vendor publishes routing thresholds publicly (Fathom, AKASA, CodaMetrix all treat thresholds as internal); ONC HTI-1 (effective Jan 2025) requires versioned, documented decision logic for predictive DSIs; OIG Compliance 2025 requires audit trail of AI version used. A versioned TypeScript module with SemVer tag satisfies both. Default conservative thresholds (MIN_EVIDENCES=2, MAX_ALTERNATIVES=3) plus mandatory hospital calibration attestation exhibit in BAA before go-live. Inpatient `claimType` override is a hard upstream gate, not configurable by this heuristic. — 2026-05-16 → [docs/research/ai-models/coding-confidence-heuristic-spec.md](ai-models/coding-confidence-heuristic-spec.md)

- Given AHIMA's explicit framework requiring human DRG reconciliation for inpatient claims, should cliqueue include a "human-attestor" agent role (certified RHIA/CCS coder signing on-chain) alongside the AI coding agent for inpatient claim types? → Yes — but AHIMA's position is an ethical opinion, not a binding mandate. No federal regulation requires human review of AI-coded claims (as of May 2026); however, (1) HHS OIG assigns residual responsibility to the human signer regardless of AI involvement, (2) Humana/Cigna MA contracts explicitly require credentialed coder attestation of AI-coded claims, (3) AHIMA's Standards of Ethical Coding place full ethical/legal responsibility on the signing coder. Inpatient autonomy rates are structurally lower — Fathom calls inpatient DRG the "holy grail" still aspirational. The `HumanAttestor` SBT design is correct and is differentiated from Fathom/CodaMetrix (on-chain attestation as payer contract compliance artifact). Recommended: mandatory `requiresHumanAttestation` for inpatient DRG claims in `ClaimsAdjudicator`; confidence-threshold-gated for outpatient/ED. — 2026-05-16 → [docs/research/market/inpatient-drg-human-attestor-architecture.md](market/inpatient-drg-human-attestor-architecture.md)

- At what upgrade-authority threshold does the "total independent control over network architecture" test (SIFMA standard) cause a governance multisig to become a DASP — does a 3-of-5 multisig + 48-hour timelock fall below the threshold? → Under the current binding FinCEN 2019 standard, a minority multisig key holder who cannot unilaterally complete transactions does NOT have "total independent control" — the 3-of-5 threshold is defensible. The GENIUS Act's "distributed ledger protocol" exclusion covers `ClaimsAdjudicator` as a smart contract and does NOT require immutability. The SIFMA "effective or ongoing ability to alter, upgrade" standard is a lobbying recommendation, not binding regulation — neither OCC, FinCEN, nor FDIC NPRMs have adopted it. Three-position architecture strategy documented: Position A (3-of-5 + 48h timelock, current), Position B (4-of-7 + 7-day delay after $10M TVL), Position C (ProxyAdmin renounce, satisfies immutable software interface exclusion). — 2026-05-16 → [docs/research/regulatory/multisig-timelock-dasp-control-threshold.md](regulatory/multisig-timelock-dasp-control-threshold.md)

- Does the UUPS proxy upgrade mechanism (which allows the governance multisig to replace `ClaimsAdjudicator`'s implementation) cause the governance multisig holders to be treated as "operators" of a custodial intermediary under the GENIUS Act? → The "person" requirement (smart contracts not in statutory definition) and the "distributed ledger protocol" exclusion (covering deployed smart contracts) both support that `ClaimsAdjudicator` itself is not a DASP. For the governance multisig holders (human persons), DASP risk hinges on the "custody or control" test (SIFMA standard: "total independent control over user assets or network architecture") and the "for compensation or profit" element. The OCC NPRM (Feb 2026) explicitly does not resolve governance participant DASP status. Recommended: add a "frozen implementation" option to the deployment runbook (ProxyAdmin renounce after 90-day stability period) and document the GENIUS Act Non-DASP determination in the Settlement Token Policy. Treasury Section 9 DeFi study (due Jan 2026) may clarify. — 2026-05-16 → [docs/research/regulatory/uups-proxy-governance-multisig-dasp-operator-risk.md](regulatory/uups-proxy-governance-multisig-dasp-operator-risk.md)

- Does the CAQH CORE operating-rules framework create an affirmative obligation to route all claim status and remittance through certified clearinghouses — or is an on-chain settlement layer categorically outside CORE's scope? → CAQH CORE does NOT mandate clearinghouse usage — it specifies data capabilities and content standards, not infrastructure topology. CORE certification is voluntary; CMS enforces only the HIPAA-mandated operating rules governing standard X12 transactions. Phase IV CORE 450 explicitly carves out Real-Time Adjudication workflows from its scope. Cliqueue's on-chain settlement layer is categorically outside CORE's regulatory scope. The payer exclusivity risk (Anthem/UHC clearinghouse-exclusive contracts) is contractual, not regulatory, and applies specifically to 837 EDI submission routing — the onboarding legal review should focus on post-adjudication data sharing clauses, not CORE compliance. — 2026-05-15 → [docs/research/market/caqh-core-scope-and-payer-exclusivity-onboarding.md](market/caqh-core-scope-and-payer-exclusivity-onboarding.md)

- Do major payer provider contracts (Anthem, UHC, Aetna, Cigna, Humana) contain explicit "exclusive clearinghouse data sharing" clauses covering post-adjudication settlement data? → No public primary source documents such clauses. HIPAA and CAQH CORE Phase III EFT/ERA operating rules (mandatory since January 2014) explicitly permit both clearinghouse AND direct connections for 835 remittance delivery — no clearinghouse mandate exists in federal regulation. Anthem's Availity exclusivity and UHC/Optum's clearinghouse preferences govern 837 inbound claim submission routing, not post-adjudication supplemental settlement layers. CAA-21 gag clause prohibition (2021) creates a federal floor making broad payer data exclusivity restrictions on cliqueue as a business associate legally tenuous. Cliqueue's on-chain settlement event (post-adjudication supplemental audit record) is categorically not an EDI 835 transaction and therefore outside CORE routing scope. Avaneer Health (Aetna/Anthem-backed consortium blockchain, 2022) operated alongside traditional clearinghouses — confirming industry acceptance of supplemental blockchain settlement layers. Hospital onboarding must include a payer contract audit for unusual post-adjudication data restriction clauses, but no systematic industry practice has been documented. — 2026-05-16 → [docs/research/market/payer-contract-data-sharing-restrictions.md](market/payer-contract-data-sharing-restrictions.md)

- The Change Healthcare breach exposed systemic clearinghouse concentration risk — is there a documented provider appetite for decentralized alternatives, or is the standard response simply adding a second clearinghouse backup? → The dominant market response is clearinghouse-to-clearinghouse redundancy (multi-vendor EDI contracts), NOT decentralized alternatives. 53% of affected health systems planned to add a second clearinghouse; only 17% pursued direct-payer submission. No primary source (AMA, AHA, HFMA, CMS, Congress) recommends blockchain or decentralized protocol as a response. Cliqueue's pitch must NOT be "replace the clearinghouse" — it sits alongside EDI rails as an immutable settlement and audit layer. 36% of providers face payer-imposed contract exclusivity clauses that may require amendment before adding cliqueue. — 2026-05-15 → [docs/research/market/change-healthcare-clearinghouse-concentration-and-decentralization-appetite.md](market/change-healthcare-clearinghouse-concentration-and-decentralization-appetite.md)

- Does Somnia mainnet's `ecPairing` precompile (address `0x08`, EIP-197) execute correctly — what is the canary test methodology and is BLS12-381 cheaper? → ecPairing is listed in Somnia's official gas table at 250× Ethereum cost, implying it is supported; the empty-input canary (should return 0x01) and generator-negation canary are documented. BN254 Groth16 verification costs ~$0.00028/proof absolute (well within per-claim budget). BLS12-381 is ~3.8× cheaper on Somnia (50× vs 250× multiplier). No external developer confirmation of ecPairing correctness on Somnia mainnet exists — canary test is a required blocking pre-deployment gate. Two-canary test contract spec documented. — 2026-05-15 → [docs/research/somnia/ecpairing-precompile-canary-and-bls12-alternative.md](somnia/ecpairing-precompile-canary-and-bls12-alternative.md)

- Has zkVerify deployed its `IZkVerify` receiver contract on Somnia mainnet (chain ID 5031)? → No. Somnia is not among zkVerify's supported destination chains (Ethereum, Base, Arbitrum, Optimism, Horizen, Ape, Flow as of May 2026). The receiver contract is NOT permissionlessly deployable — it requires first-party Horizen Labs involvement to both deploy the contract and configure the authorized relayer to route aggregations to Somnia. Alternative path: self-hosted Groth16 verifier on Somnia using the Paris EVM `ecPairing` precompile (address `0x08`, EIP-197), deployable without any external dependency. Canary test of `ecPairing` precompile on Somnia mainnet required before relying on it. — 2026-05-15 → [docs/research/somnia/zkverify-somnia-chain-support-and-alternatives.md](somnia/zkverify-somnia-chain-support-and-alternatives.md)

- If a hospital's JCAHO audit reveals PSV was not completed in a given month, should `ClaimsAdjudicator` allow a 72-hour grace window past SBT `expiry`? → No. JCAHO defines no regulatory grace period; AHIMA prohibits credential use during inactive/lapsed status; a contract-level grace window creates an FCA audit trail risk (immutable Somnia event log), violates AHIMA credential-use prohibition, and opens a social-engineering exploit (withhold renewal, submit claims during window). The timestamp manipulation risk is negligible on Somnia BFT. Correct mitigation: require automated continuous PSV (Verisys/Symplr/ProviderTrust) in hospital BAA, enforce 7-day renewal lead-time window in `SBTRegistry.renewExpiry()`, keep hard `require(expiry >= block.timestamp)` in `ClaimsAdjudicator`. — 2026-05-15 → [docs/research/somnia/sbt-expiry-grace-window-jcaho-fca.md](somnia/sbt-expiry-grace-window-jcaho-fca.md)

- Should cliqueue use OZ `TransparentUpgradeableProxy` (which auto-deploys a `ProxyAdmin`) or a custom ProxyAdmin contract with the `TimelockController` as its owner — and does the auto-deployed ProxyAdmin pattern work with Hardhat/Foundry deploy scripts on Somnia? → OZ v5 auto-deploys `ProxyAdmin` inside the proxy constructor; `TimelockController` as `ProxyAdmin` owner is fully supported via `initialOwner` parameter in both Hardhat (`upgrades.deployProxy` `initialOwner` opt) and Foundry (`deployTransparentProxy` arg). Three-layer governance chain: TimelockController → ProxyAdmin → proxy → implementation. Compile with `evmVersion: "paris"` until Somnia Cancun opcode support is confirmed; do not use `ReentrancyGuardTransient` yet. — 2026-05-15 → [docs/research/agreement-layer/transparent-proxy-proxyadmin-timelock-deployment.md](agreement-layer/transparent-proxy-proxyadmin-timelock-deployment.md)

- Smart-contract patterns for conditional fund release + Somnia 5-state adjudication machine gas costs → packed `uint8` enum state machine is canonical; happy-path per-claim gas ~$0.0023 worst-case ($0.00025 at 400 TPS); cold-SLOAD (1M gas, ~$0.0055) requires a hot-tier dispute contract; AgentVault lacks built-in escrow logic — thin `ClaimsAdjudicator` wrapper needed; UUPS proxy recommended with frozen-logic mode during active disputes — 2026-05-14 → [docs/research/agreement-layer/conditional-fund-release-state-machine.md](agreement-layer/conditional-fund-release-state-machine.md)

- What CMS / HIPAA constraints apply to anchoring a hash of a finalized ICD-10 claim on a public blockchain? → Plain SHA-256 of 837 content is unsafe (re-identification attack); HMAC-SHA256 with HSM-held key satisfies expert determination method (§164.514); ICD-10 code arrays and settlement amounts are safe on-chain; HHS has not issued blockchain-specific guidance; no pre-approval process exists — 2026-05-14 → [docs/research/regulatory/hipaa-blockchain-hash-anchoring.md](regulatory/hipaa-blockchain-hash-anchoring.md)

- For the oracle bridging off-chain coding agent output to on-chain adjudication, what attestation scheme (multi-sig, ZK proof, threshold signature) provides sufficient trust without PHI on-chain? → Somnia native Agents consensus (createAdvancedRequest, 5-state, Majority/Threshold) is the lowest-gas primary path; ZK proofs are economically prohibitive (Somnia ecPairing 250× Ethereum); OpPC challenge window is recommended for disputes; TEE (Phala, HIPAA-compliant) handles PHI off-chain with report hash anchored on-chain — 2026-05-14 → [docs/research/agreement-layer/oracle-attestation-schemes.md](agreement-layer/oracle-attestation-schemes.md)

- Annual US spend on ICD-10 coding (CAC + in-house + denial-rework cycles) → US coding market $21–27B in 2024 (10% CAGR); RCM admin costs >$160B/year; per-claim outsourced coding $4–10; rework cost $25–181 per denied claim — 2026-05-14 → [docs/research/market/coding-market-size-and-denial-losses.md](market/coding-market-size-and-denial-losses.md)
- Documented coding-error / denied-claim loss figures → $262B denied in 2024; 84% potentially avoidable; 11.81% initial denial rate; coding-related denial dollar amount +126% YoY to $631/claim; hospitals lose avg $5M/year to unresolved denials — 2026-05-14 → [docs/research/market/coding-market-size-and-denial-losses.md](market/coding-market-size-and-denial-losses.md)
- Competitive landscape of AI ICD-10 coders (Fathom, CodaMetrix, AKASA, Nym Health) → KLAS Aug 2025 report: Fathom 95.5 score / ≥90% automation / 98.3% accuracy; CodaMetrix #1 Best in KLAS 2026; Nym >95% accuracy; market early-stage, concentrated in radiology/ED — 2026-05-14 → [docs/research/market/coding-market-size-and-denial-losses.md](market/coding-market-size-and-denial-losses.md)
- EDI 837/835 end-to-end flow and decentralized claim adjudication precedents → clean claims pay in ~20 days; 5-step tokenized adjudication sequence documented in literature; Avaneer Health (Aetna/Anthem/Cleveland Clinic consortium) dissolved 2023 after failing adoption; oracle dependency and key management are primary failure modes; FHIR prior-auth mandated by 2027 but claim-level FHIR not yet mandated — 2026-05-14 → [docs/research/agreement-layer/edi-837-835-flow-and-blockchain-adjudication.md](agreement-layer/edi-837-835-flow-and-blockchain-adjudication.md)
- Has any decentralized protocol attempted on-chain claim adjudication? Survey Hyperledger, Solve.Care, Akiri, MediBloc → Hyperledger Fabric is most-deployed (permissioned); Avaneer dissolved 2023; Solve.Care/MediBloc do not implement end-to-end adjudication+settlement; 5-step tokenized adjudication is closest proof-of-concept precedent — 2026-05-14 → [docs/research/agreement-layer/edi-837-835-flow-and-blockchain-adjudication.md](agreement-layer/edi-837-835-flow-and-blockchain-adjudication.md)
- What are Somnia's published finality guarantees and demonstrated TPS in practice? → deterministic BFT finality at 100 ms block time; marketing: 1.05M TPS (devnet); Chainspect measured peak: 134,642 TPS; mainnet Q4 2025 average: ~92 TPS (8M tx/day); cold-SLOAD 1M-gas surcharge is key constraint; ZK precompile costs prohibitive (50–250× Ethereum) — 2026-05-14 → [docs/research/somnia/finality-tps-and-gas-model.md](somnia/finality-tps-and-gas-model.md)
- `somnia-agent-kit` autonomous Agent runtime — production deployments, gas patterns → kit is testnet-only (community hackathon origin); no confirmed production mainnet deployments; Somnia has a first-party native Agents runtime (createRequest/handleResponse 5-state machine) that may supersede it — 2026-05-14 → [docs/research/somnia/finality-tps-and-gas-model.md](somnia/finality-tps-and-gas-model.md)
- What fraction of the $262B in 2024 denied claims is specifically attributable to ICD-10 coding errors vs. prior-authorization vs. eligibility mismatches, and do AI coding vendors measurably reduce denial rates? → coding errors ~20% of denials by count (~$52B of $262B pool, rough estimate); CodaMetrix OHSU: 70% lower denial rate (0.33% vs. 1.09%); Nym: 97% reduction in radiology denials; Fathom does not publish denial-rate deltas — 2026-05-14 → [docs/research/market/coding-error-denial-share-and-ai-impact.md](market/coding-error-denial-share-and-ai-impact.md)
- Does the coding-error denial reduction achieved in radiology (CodaMetrix 70%, Nym 97%) hold for inpatient complex/surgical coding where autonomy rates are lower, and does the Waystar/Iodine acquisition compress or open cliqueue's market window? → Radiology/ED AI autonomy rates 85–99%; inpatient DRG autonomy rates unpublished and structurally lower; AHIMA requires human DRG reconciliation (no fully autonomous inpatient coding); MA autonomy without oversight generates 17% denial rate (3× healthy benchmark); Waystar/Iodine does not fill the on-chain settlement gap; Change Healthcare breach validates decentralized alternative demand — 2026-05-14 → [docs/research/market/inpatient-coding-performance-and-market-consolidation.md](market/inpatient-coding-performance-and-market-consolidation.md)
- What is the minimum on-chain claim structure needed to interoperate with payer 835 remittance systems while still providing immutable settlement state, and how should it be structured for CAQH CORE RTA compatibility? → 7-slot Claim struct (claimId=HMAC(CLM01), icd10CodeHash, providerHash, payerHash, billedCents, adjudicatedCents, payerClaimRef); CLP01=CLM01 is the primary reconciliation key; off-chain adapter reconstructs synthetic X12 835 from ClaimSettled event; Somnia tx hash serves as TRN02 trace number; CAQH CORE Phase IV RTA-compatible without contract changes — 2026-05-14 → [docs/research/agreement-layer/on-chain-claim-struct-835-interoperability.md](agreement-layer/on-chain-claim-struct-835-interoperability.md)

- When a claim is denied and resubmitted (CLM05-3 frequency code 7 = replacement), how should the on-chain struct handle replacement — new claimId with parent pointer, or in-place status update that breaks immutability? → New claimId with `parentClaimId` + `replacedByClaimId` dual-pointer is required (in-place update breaks HIPAA audit trail and dispute baseline); state machine gains a `Superseded` terminal state; claimId derivation must include frequencyCode to prevent hash collision; replacement gas cost ~$0.0011 per cycle — negligible — 2026-05-14 → [docs/research/agreement-layer/claim-replacement-resubmission-onchain.md](agreement-layer/claim-replacement-resubmission-onchain.md)

- Is there a Somnia first-party agent framework that supersedes `somnia-agent-kit` for production? → Yes: Somnia native Agents launched April 2026 (mainnet), first-party protocol-level with multi-validator consensus. `somnia-agent-kit` is community-built (0 stars, testnet-only contracts, hackathon origin) — do not use for production. Native runtime is "prototype state" (API may change); Phase 1 has fixed agent set only (no custom models); Phase 2 custom agents coming later in 2026; LLM Inference costs $0.042–$0.070/claim at current SOMI price — well below $4–10 outsourced benchmark — 2026-05-14 → [docs/research/somnia/native-agents-vs-agent-kit.md](somnia/native-agents-vs-agent-kit.md)

- What LLM models does Somnia's native LLM Inference base agent expose, and are they sufficient for ICD-10 coding accuracy? → Phase 1 does not publish a model catalog — underlying model is opaque with no model selection parameter. Base LLMs (GPT-4, Claude) achieve only 15–35% agreement with human coders on ICD-10 tasks without fine-tuning; fine-tuned Llama models reach 69–98% exact match; agentic pipelines (Corti Symphony, MDPI 2026 study) outperform base models by >25%. Somnia native LLM Inference is insufficient as the primary coder; it should serve only as a consensus validation oracle for coding results generated by an off-chain fine-tuned model or specialized agentic pipeline — 2026-05-14 → [docs/research/ai-models/llm-icd10-accuracy-and-somnia-model-availability.md](ai-models/llm-icd10-accuracy-and-somnia-model-availability.md)

- Corti Symphony now supports MCP standard — can cliqueue's off-chain coding agent call Corti Symphony via MCP, with the result hash passed to Somnia's native `inferString` for consensus validation, creating a two-tier pipeline? → Yes: Symphony is MCP-native (launched April 2026), plugs directly into multi-agent systems via Corti Agents Library; POST /tools/coding/ returns structured codes + evidence suitable for HMAC hashing; per-claim cost ~$0.012–0.025 (token-based, $4/M input + $16/M output); HIPAA-compliant sovereign cloud available (BAA must be confirmed separately); Somnia inferString validation adds estimated $0.042–$0.070/claim; combined ~$0.054–$0.095 total — 40–185× cheaper than $4–10 benchmark; Somnia callback latency is undocumented (estimated 5–30s, unconfirmed) — 2026-05-14 → [docs/research/ai-models/corti-symphony-mcp-integration.md](ai-models/corti-symphony-mcp-integration.md)

- What is the BAA process and data processing agreement structure for using Corti Symphony in a US hospital context? Does Corti's sovereign cloud allow PHI to stay on-premise? → Standard cloud API (PHI to Corti Azure US region) is HIPAA-legal with a signed BAA; BAA is not self-serve — requires enterprise procurement via privacy@corti.ai; Corti holds HIPAA, SOC 2, ISO 27001/27017/27018, FedRAMP certifications; on-premise self-hosted option entered limited preview Summer 2025 (production status unclear); for MVP, standard cloud API path + BAA is the viable path; 2026 HIPAA Security Rule NPRM adds annual BA verification requirement — 2026-05-14 → [docs/research/regulatory/corti-symphony-baa-and-phi-data-flow.md](regulatory/corti-symphony-baa-and-phi-data-flow.md)

- Can Phala Network TEE attestation reports be verified on Somnia's EVM at acceptable gas cost, or does the TEE attestation link need to live in a separate off-chain verifiable-credential registry? → Direct DCAP verification (3–8M gas) and SNARK-compressed verification (Groth16, ~250k gas on Ethereum) are both economically prohibitive on Somnia due to ecPairing 250× multiplier. Recommended MVP path: Phala KMS code-hash whitelist on Somnia (~10k gas SLOAD check per claim, ~$0.00055). Post-MVP path: zkVerify L1 → Somnia Merkle attestation (~50–80k gas Merkle check, no ecPairing on Somnia). No confirmed Phala ↔ Somnia integration exists as of May 2026 — requires community deployment of KMS governance contract or zkVerify receiver contract. — 2026-05-14 → [docs/research/somnia/tee-attestation-on-chain-verification.md](somnia/tee-attestation-on-chain-verification.md)

- Does the off-chain EDI adapter's responsibility to resolve `payerICN → original claimId` introduce a single point of failure in the replacement workflow — and should this mapping be cached in a separate lightweight on-chain lookup contract to survive adapter outages? → The mapping is a *recoverable* single point of failure: the `ClaimAdjudicated` event log on Somnia is the authoritative recovery source; the adapter can reconstruct its database from Ormi subgraph (99.99% uptime, 120ms lag) or direct RPC replay. An optional `ICNRegistry` contract (HMAC-hashed ICN → claimId, ~$0.00012/claim SSTORE) adds on-chain redundancy for post-MVP resilience. Raw ICN must never appear on-chain (PHI correlation). — 2026-05-14 → [docs/research/agreement-layer/edi-adapter-payericn-mapping-resilience.md](agreement-layer/edi-adapter-payericn-mapping-resilience.md)

- Does Corti Symphony's 65.2% F1 on full clinical notes hold for inpatient DRG-determination coding, or does the performance gap vs. commercial CAC (95%+) require Symphony to be supplemented with a human-attestor agent role for inpatient claims? → Symphony achieves F1=0.58 on MDACE (inpatient benchmark) vs F1=0.74 outpatient — a 22% relative gap. DRG determination is a separate deterministic CMS grouper step, not a coding task. No commercial CAC vendor has deployed inpatient DRG facility coding at full autonomy as of May 2026 (Fathom lists it as "in development"). Humana/Cigna require credentialed-coder attestation for AI-coded claims (Q2 2025 contracts). DOJ $23M settlement (2025) creates FCA exposure for fully autonomous coding. Recommended: add ClaimType enum and HumanAttestor role gating inpatient-facility claims; outpatient/ED proceeds fully autonomous. — 2026-05-14 → [docs/research/ai-models/corti-symphony-inpatient-drg-gap.md](ai-models/corti-symphony-inpatient-drg-gap.md)

- What is the AgentVault daily-limit calibration strategy when claim volume exceeds the 0.01–100 ether per-day ceiling? At scale ($631/claim × 10,000 claims/day), is a multi-vault fan-out pattern needed? → The question conflated two separate financial flows. AgentVault (100 SOMI/day hard cap) covers only agent invocation deposits (0.24–0.40 SOMI/claim); actual claim settlement amounts ($631/claim avg) require a separate stablecoin escrow layer (USDC preferred over USDso, which lacks confirmed mainnet deployment or compliance framework). Fan-out IS required for agent invocation layer: 40-vault fan-out at 10,000 claims/day ($0.044 deployment cost). SOMI price volatility (12× ATL-ATH range) requires a configurable per-claim cost ceiling and SOMI reserve buffer. — 2026-05-14 → [docs/research/agreement-layer/agentvault-daily-limit-and-stablecoin-settlement.md](agreement-layer/agentvault-daily-limit-and-stablecoin-settlement.md)

- What on-chain credential representation is appropriate for a HumanAttestor in inpatient claims — a hash of (credentialNumber + attestorAddress + claimId), a delegated NFT, or a hospital-issued verifiable credential? → Two-layer recommended: (1) ERC-5484 SBT minted per-attestor at onboarding (~$0.10–0.15 on Somnia, revocable by hospital); (2) `keccak256(attestorAddress, claimId, block.number)` as `bytes32 attestorSig` in the Claim struct per-claim (~$0.000003). W3C VC 2.0 and EAS are post-MVP. EAS is not yet deployed on Somnia. FCA liability confirmed: Humana/Cigna/CMS all require credentialed-coder sign-off for AI-coded inpatient claims (Q2 2025). `ClaimType` enum required to gate inpatient-facility claims. — 2026-05-15 → [docs/research/somnia/human-attestor-credential-representation.md](somnia/human-attestor-credential-representation.md)

- Should the `SBTRegistry` for HumanAttestor credentials be hospital-scoped or network-scoped? → Hybrid network-scoped registry recommended: single cliqueue-deployed contract with `HOSPITAL_ADMIN` role-gated minting and revocation (per `hospitalId`), enforcing `hospitalId` matching at claim submission (~10k gas / ~$0.00055/claim). Saves ~$22k in redundant deployments at 50-hospital scale vs. hospital-scoped. Central-key risk mitigated by 3-of-5 multisig on `REGISTRY_ADMIN`. Aligns with W3C VC 2.0 hybrid model; contrasts with Hyperledger Fabric org-scoped model. ERC-5484 `BurnAuth.IssuerOnly` enforced per-hospital via role gating. Locum tenens coders may hold multiple SBTs (one per hospitalId), each enforced at claim submission. — 2026-05-15 → [docs/research/somnia/sbt-registry-scoping-hospital-vs-network.md](somnia/sbt-registry-scoping-hospital-vs-network.md)

- What is the empirical end-to-end latency for a Somnia `createAdvancedRequest(subcommitteeSize=5)` LLM Inference callback on mainnet, and is the `timeout` parameter in seconds or blocks? → `timeout` default is 15 minutes (900 seconds, unit strongly inferred as seconds from EVM convention — unconfirmed by explicit docs). No SLA or empirical latency published. On timeout: `upkeepRequests()` fires `ResponseStatus.TimedOut` callback (funds refunded). Demo-critical implication: 15-min default incompatible with 90-second demo; Corti Symphony coding must be pre-computed off-chain, with native agent call as async non-blocking attestation. `ClaimSettlement.sol` requires explicit `TimedOut` handler and configurable `agentTimeout` variable. Recommended demo timeout: 60 seconds; production: 300 seconds outpatient, 900 inpatient. — 2026-05-15 → [docs/research/somnia/llm-inference-callback-latency-and-timeout.md](somnia/llm-inference-callback-latency-and-timeout.md)

- What is the liability exposure if a hospital's SBTRegistry contains a stale entry and a fraudulent attestation is anchored — should on-chain `credentialExpiry < block.timestamp` be enforced at claim submission? → FCA liability for stale-credential attestation is hospital-borne and real (per G2Z Law + DOJ $6.8B FY2025 enforcement). JCAHO 2025 mandates monthly PSV re-verification, making revocation-only design non-compliant. AHIMA 12-month lapse window creates up to 12-month stale-SBT risk. Recommended: 30-day expiry cycle with `renewExpiry()` forced after each monthly PSV pass; `require(expiry >= block.timestamp)` check at claim submission. ERC-5727 IERC5727Expirable extension defines the standard pattern; cliqueue should add a custom `expiry` uint64 to the ERC-5484 token struct (packed with hospitalId + revoked in one slot). Gas overhead ~$0.00011–0.00028/claim warm. — 2026-05-15 → [docs/research/somnia/sbt-credential-expiry-enforcement.md](somnia/sbt-credential-expiry-enforcement.md)

- Should hospitalId in SBT metadata be keccak256(NPI) and does NPI-to-attestor-address correlation on Somnia constitute HIPAA disclosure? → NPI is not PHI (it identifies providers, not patients); Type 2 hospital NPIs are public record (NPPES/FOIA). keccak256(NPI) alone provides zero privacy — NPPES has ~1.6M enumerable NPIs, trivially invertible. Recommended: use `keccak256(abi.encodePacked(npi, CLIQUEUE_DOMAIN_SEPARATOR))` for hospital SBTs; never embed individual coder (Type 1) NPIs on-chain. NPI-to-wallet correlation is not a HIPAA violation for org NPIs; for individual coders it is PII linkage (non-HIPAA but consent concern). 2026 HIPAA Security Rule NPRM does not regulate provider identifiers on public chains. — 2026-05-15 → [docs/research/somnia/sbt-hospitalid-npi-hash-hipaa-disclosure.md](somnia/sbt-hospitalid-npi-hash-hipaa-disclosure.md)

- Does Somnia Phase 2 "Full Agent SDK" allow deploying a fine-tuned model as a custom on-chain agent, and what is the Phase 2 timeline? → Phase 2 is dated "2026" only — no quarter published. Phase 1 LLM Inference uses an opaque fixed model with no model-selection parameter; "GPU-backed" is the only description. Phase 2 language is "custom user-defined agents" with no indication fine-tuned domain models (e.g., ICD-10 Llama) will be supported. `inferToolsChat` MCP support enables Corti Symphony calls from within validator consensus (de-identified inputs only). `onchainTools` parameter enables PHI-safe on-chain state reads during inference. Two-tier off-chain (Corti) + on-chain (Somnia consensus oracle) is the permanent architecture, not a Phase 1 workaround. — 2026-05-15 → [docs/research/somnia/phase2-custom-agents-and-llm-model-opacity.md](somnia/phase2-custom-agents-and-llm-model-opacity.md)
- Is USDso (Frax on Somnia) suitable as a claim settlement stablecoin for US hospital-payer payments? → No for MVP. USDso is mainnet-deployed (contract `0x00000022dA000002656c64D9eA6011ea952D008A`, announced May 5 2026) but lacks PPSI certification, enterprise compliance docs, liquidity depth, and fiat on/off-ramp. USDC is the only defensible MVP choice. USDC is listed as "Native" on Somnia mainnet (`0x28bec7e30e6faee657a03e19bf1128aad7632a00`) but likely bridged via Relay/Stargate (not CCTP-minted). Aon March 2026 insurance stablecoin settlement precedent used USDC+PYUSD on Ethereum/Solana. Store settlement token address as configurable parameter in ClaimsAdjudicator. — 2026-05-15 → [docs/research/agreement-layer/usdc-vs-usdso-settlement-stablecoin.md](agreement-layer/usdc-vs-usdso-settlement-stablecoin.md)

- What governance mechanism should control the `stablecoinAddress` parameter in `ClaimsAdjudicator`, and what minimum notice period prevents rug-pull risk? → 48-hour `TimelockController` minimum delay (Compound/MakerDAO standard floor; MakerDAO uses 72 hours post-upgrade); 3-of-5 multisig proposer + 2-of-3 executor (separate key sets); emergency `PAUSER` NOT behind timelock. GENIUS Act (July 2025) requires governance frameworks for stablecoin reserve management but does not prescribe on-chain timelock mechanics. Monitoring (`CallScheduled` event alerts) is mandatory — Beanstalk ($182M, 2022) is the canonical 24-hour-unmonitored-timelock cautionary example. Emit `SettlementTokenChangeScheduled` event with 48-hour future timestamp on proposal. — 2026-05-15 → [docs/research/agreement-layer/stablecoin-address-governance-timelock.md](agreement-layer/stablecoin-address-governance-timelock.md)

- Does HIPAA's Minimum Necessary standard (§164.502(b)) constrain who can observe on-chain HMAC hashes, ICD-10 codes, and payment amounts emitted as Somnia public events? → No: Minimum Necessary applies only to PHI; cliqueue's on-chain data (keyed hashes, unlinked code arrays, unlinked amounts) is de-identified and exits HIPAA's scope under §164.514. The operative constraint is PHI exclusion at the chain boundary, not event visibility restriction. Low-volume provider statistical re-identification is a residual risk requiring expert determination review. — 2026-05-15 → [docs/research/regulatory/hipaa-minimum-necessary-on-chain-events.md](regulatory/hipaa-minimum-necessary-on-chain-events.md)

- Has Circle applied to add Somnia to CCTP V2, and is the USDC on Somnia mainnet native or bridged? → No CCTP-Somnia integration exists or is announced. The USDC on Somnia (`0x28BEc7E30E6faee657a03e19Bf1128AaD7632A00`) is Stargate-bridged USDC.e — NOT native Circle-issued USDC. CCTP V2 supports 25 chains; Somnia is absent. USDC.e carries bridge counterparty risk (Wormhole/Nomad precedents), lacks GENIUS Act PPSI status, and has thin liquidity ($1.58M market cap). Polymarket Feb 2026 USDC.e→native migration is direct precedent validating enterprise concern. Circle's Bridged USDC Standard (not used by Stargate USDC.e) would be the first upgrade step. MVP Settlement Token Policy must disclose bridged status; cliqueue should pursue Circle CCTP V2 or Bridged USDC Standard integration for post-launch. — 2026-05-15 → [docs/research/agreement-layer/usdc-cctp-somnia-native-vs-bridged.md](agreement-layer/usdc-cctp-somnia-native-vs-bridged.md)

- What is the minimum claim volume threshold at which a provider's per-claim `(billedCents, adjudicatedCents, timestamp)` tuple on a public chain becomes statistically re-identifiable under §164.514(b)(1) Expert Determination, and should cliqueue implement automatic batching? → No universal numerical threshold exists in §164.514(b)(1); HHS OCR 2012 guidance explicitly defers to context-specific expert analysis. CMS operational standard for public payment data is n≥11 (Physician PUF cell suppression policy). k=20 (5% re-identification probability) is the recommended threshold where consequences are "extremely high." Billing amounts function as quasi-identifiers (Danaan-gift attack variant on EVM). Expert Determination scope is context-specific — a single network-level report can cover all deployments only if it models the lower bound (small provider) and enforces a minimum batch threshold constraint. Recommended: `ClaimsAdjudicator` emits `ClaimBatchSettled` per-provider per-cycle only when batch size ≥11 (CMS standard), with a `batchFlushInterval` fallback; advisory threshold 20 for high-stakes inpatient deployments. — 2026-05-15 → [docs/research/regulatory/re-identification-risk-low-volume-provider-batching.md](regulatory/re-identification-risk-low-volume-provider-batching.md)

- If `totalBilledCents` is suppressed from the `ClaimBatchSettled` on-chain event, can payer systems still reconcile on-chain settlement proofs with their 835 remittance files — and does this break CAQH CORE Phase IV RTA interoperability? → No, it does not break interoperability. CLP03 (billed amount, usage "R") in the HIPAA 835 is sourced from the adapter's off-chain claim database, not from the on-chain event. CAQH CORE governs the 835 EDI transaction layer, not on-chain event schemas. The synthetic 835 adapter generates a conformant 835 from its own database, triggered by the on-chain settlement event; the on-chain event need only provide `(providerHash, batchSize, windowEnd)` as the settlement proof. CAQH CORE Phase IV RTA explicitly allows payment to be processed after adjudication — 835 delivery is decoupled from the same-session 837 submission. — 2026-05-15 → [docs/research/agreement-layer/835-reconciliation-without-on-chain-billed-amount.md](agreement-layer/835-reconciliation-without-on-chain-billed-amount.md)

- Should `minBatchThreshold` be set at the network level (cliqueue governance) or at the hospital level (`HOSPITAL_ADMIN` role), and does allowing hospital-level override create a compliance gap? → Two-tier architecture required: immutable `NETWORK_BATCH_FLOOR = 11` in contract bytecode (not reducible by any key or governance action) plus per-provider `batchThreshold` mapping settable by `HOSPITAL_ADMIN` with `require(threshold >= NETWORK_BATCH_FLOOR)`. HIPAA §164.514(b)(1) places expert determination obligation on the covered entity (hospital), making hospital-level configuration the correct regulatory locus. Platform-level expert determination report can cover all deployments if it characterizes the full provider CV range and defines minimum threshold floors; hospital privacy officer must sign onboarding attestation. BAA clause required binding hospital to maintain thresholds within expert determination scope. 2026 HIPAA Security Rule NPRM would add annual BA verification, requiring periodic threshold audits. — 2026-05-15 → [docs/research/regulatory/minbatchthreshold-governance-network-vs-hospital.md](regulatory/minbatchthreshold-governance-network-vs-hospital.md)

- Should `ClaimBatchSettled` emit `adjudicatedCents` or suppress it — and is emitting it lower-risk than `billedCents`? → `adjudicatedCents` is materially lower risk than `billedCents`: contract rates are already publicly mandated via TiC MRFs (45 CFR 147.212), so disclosing them on-chain adds little information over what is already public. Re-identification risk is provider-level (not HIPAA patient-level). HIPAA's 18 Safe Harbor identifiers do not include payment amounts. TiC MRF out-of-network suppression threshold (n<11→n<20 proposed) aligns with cliqueue's NETWORK_BATCH_FLOOR=11. Recommended: emit `totalAdjudicatedCents` for CV≥20% providers (n≥11 batch), suppress with `amountSuppressed=true` flag for CV<10% homogeneous providers. Event is a trigger, not a data carrier — payer agent can RPC-call per-claim amounts from contract storage when needed. — 2026-05-15 → [docs/research/agreement-layer/adjudicated-cents-on-chain-emission-risk.md](agreement-layer/adjudicated-cents-on-chain-emission-risk.md)

- Should the `TimelockController` minimum delay in `ClaimsAdjudicator` be stored as a mutable parameter or hard-coded as an immutable constant? → Use Compound/Uniswap-style `immutable MINIMUM_DELAY = 172800` (48 hours) pattern, NOT stock OpenZeppelin `TimelockController` which has no delay floor. OZ's `updateDelay()` accepts delay=0 (known issue since 2021, never patched in stdlib). Prior exploit: OZ 2021 advisory allowed executor-role actors to reset delay to zero. Recommended: extend OZ `TimelockController` with overridden `updateDelay()` enforcing `require(newDelay >= MIN_DELAY_FLOOR)`. Compound and Uniswap both hardcode `MINIMUM_DELAY = 2 days` as a compile-time constant. The bootstrap paradox is avoidable because the floor is set once at deploy time, not governed. — 2026-05-15 → [docs/research/agreement-layer/timelock-min-delay-immutable-vs-mutable.md](agreement-layer/timelock-min-delay-immutable-vs-mutable.md)

- Should `ClaimsAdjudicator` use Compound/Uniswap-style Timelock.sol or OZ `TimelockController` with overridden `updateDelay()` — and does the choice affect the UUPS proxy upgrade path? → Use OZ `TimelockController` (not Compound standalone) + transparent proxy (not UUPS). The UUPS `_authorizeUpgrade` hook does not check `msg.sender == address(this)` — it is purely virtual, and either timelock can serve as owner/caller. However, UUPS carries a known unrecoverable deadlock risk on storage layout migrations (OZ Issue #6362), and no major DeFi protocol (Aave v3, Compound v3, Uniswap) uses UUPS for settlement-critical contracts. OZ `TimelockController` (with AccessControl PROPOSER/EXECUTOR roles) is strictly superior to Compound standalone Timelock.sol for cliqueue's 3-of-5 multisig proposer model. — 2026-05-15 → [docs/research/agreement-layer/timelock-uups-proxy-upgrade-path.md](agreement-layer/timelock-uups-proxy-upgrade-path.md)

- Does Somnia's AOT EVM compilation support Cancun-level opcodes (TLOAD, TSTORE, MCOPY), and what canary contract tests should the deployment runbook require? → Somnia officially targets Paris EVM level (confirmed from `"evm_version": "paris"` in Somnia's canonical contract verification payload). TLOAD, TSTORE, MCOPY, and PUSH0 are ALL absent from Paris — using them causes invalid-opcode reverts. `immutable` variables, ERC-7201 namespaced storage, `block.basefee`, and `block.prevrandao` are Paris-safe. OZ 5.x is Paris-compatible (OZ deliberately sets evm_version=paris for multi-chain releases). `ReentrancyGuardTransient` is prohibited on Somnia. Solidity ≥0.8.20 emits PUSH0 by default (shanghai target) — cliqueue MUST set `evmVersion: "paris"` in Hardhat and `evm_version = "paris"` in Foundry explicitly. 6-canary test suite (PUSH0-revert check, basefee/prevrandao check, TSTORE-revert check, immutable check, ERC-7201 slot check, CREATE2 check) recommended as blocking pre-deployment gate. — 2026-05-15 → [docs/research/somnia/evm-hardfork-level-paris-and-canary-tests.md](somnia/evm-hardfork-level-paris-and-canary-tests.md)

- What is the minimum staffing redundancy cliqueue should require per `hospitalId`, and should `ClaimsAdjudicator` enforce a `minAttesters` parameter on-chain? → Floor = 2 active SBT holders per `hospitalId` (no JCAHO or AHIMA minimum exists; 2 is the operational minimum to eliminate single-point-of-failure given 12% annual coding shortage and documented coder-absence backlog risk). Enforce at `SBTRegistry._revokeRole()` time (proactive, mirrors Gnosis Safe `removeOwner` ownerCount≥threshold constraint), NOT at `ClaimsAdjudicator.submitClaim()` time (reactive, adds cold-SLOAD gas without added protection). `MIN_ATTESTERS_FLOOR = 2` as bytecode constant (not governance-controlled). Onboarding checklist requires 2 SBT mints before first claim submission. — 2026-05-15 → [docs/research/somnia/min-attesters-staffing-redundancy.md](somnia/min-attesters-staffing-redundancy.md)

- Does Somnia/Improbable have an active Circle CCTP V2 dialogue — and should cliqueue initiate a protocol-level integration request before the GENIUS Act Jan 2027 effective date? → No active Somnia-Circle dialogue exists. Somnia is absent from all 25 CCTP V2 supported chains. Somnia explicitly partnered with Frax Finance (not Circle) for its native USDso stablecoin (May 2026). The actionable path is: (1) MVP with Stargate USDC.e + Settlement Token Policy disclosure; (2) advocate with Somnia Foundation to adopt the Bridged USDC Standard (permissionless, no Circle approval needed — deploys via GitHub/Discord); (3) if Frax frxUSD receives PPSI designation before Jan 2027, USDso becomes a compliant alternative; (4) GENIUS Act DASP prohibition (July 2028) is the hard backstop requiring a PPSI-compliant settlement token. Circle's Bridged USDC Standard is the upgrade pre-requisite to CCTP V2 native USDC. — 2026-05-16 → [docs/research/somnia/circle-cctp-somnia-bridged-usdc-standard-genius-act.md](somnia/circle-cctp-somnia-bridged-usdc-standard-genius-act.md)

- Has Frax Finance submitted a formal PPSI license application to OCC/FDIC — and does USDso qualify as a GENIUS Act-compliant settlement token for cliqueue? → Frax Finance has NOT submitted any PPSI application or OCC national trust bank charter application (not on OCC's public digital asset licensing tracker as of May 2026). PPSI applications cannot formally be filed until implementing regulations are finalized (deadline July 18, 2026). Frax is incorporated in Cayman Islands — cannot directly become a PPSI without either forming a US entity or obtaining Treasury's "comparable regime" determination (a 210-day process not yet initiated). "GENIUS Act-compliant" is Frax's reserve design claim, not regulatory certification — no issuer has been granted PPSI status anywhere yet. The DASP prohibition (July 18, 2028, not Jan 2027) is cliqueue's blocking constraint: using a non-PPSI stablecoin is legal through July 2028. Circle is on the PPSI track via OCC conditional approval for a national trust bank charter. For enterprise procurement, USDso should be listed as Tier 3 (contingent on Frax PPSI status) while USDC.e (Tier 1) and native Circle USDC (Tier 2) form the primary settlement token hierarchy. — 2026-05-16 → [docs/research/agreement-layer/frax-ppsi-application-status-genius-act-timeline.md](agreement-layer/frax-ppsi-application-status-genius-act-timeline.md)

- Does the GENIUS Act's DASP definition encompass a smart contract like `ClaimsAdjudicator` holding escrow stablecoin balances — or only human-operated custodial intermediaries? → `ClaimsAdjudicator` as a smart contract (a "distributed ledger protocol" under the Act's definition) is almost certainly excluded from the DASP definition via three independent statutory filters: (1) the DASP definition requires a "person" — the statute's person enumeration does not include software or autonomous contracts; (2) the DASP definition explicitly excludes "a distributed ledger protocol," defined to include "smart contracts or networks of smart contracts"; (3) the "for compensation or profit" requirement excludes autonomous contracts that do not receive compensation. The DeFi Education Fund, a16z, and Gibson Dunn all confirm the "distributed ledger protocol" exclusion safeguards DeFi from DASP regulations. However, the OCC NPRM (Feb 2026) explicitly leaves this gap unresolved and mandates a follow-on legislative study. The UUPS proxy mechanism creates a custody-control argument risk — governance multisig holders could be treated as "operators." Cliqueue must obtain a GENIUS Act legal opinion documenting the non-DASP determination before hospital launch. — 2026-05-16 → [docs/research/regulatory/genius-act-dasp-smart-contract-scope.md](regulatory/genius-act-dasp-smart-contract-scope.md)

- Should the `validClaimType` modifier in `ClaimsAdjudicator` check `claimType < CLAIM_TYPE_COUNT` or maintain `mapping(uint8 => bool) registeredClaimTypes` — and when CMS assigns digit 9, does the 48-hour TimelockController create a gap window? → Use `mapping(uint8 => bool) registeredClaimTypes` with an explicit `require(registeredClaimTypes[claimType])` guard. The immutable-count approach requires a full contract upgrade for any new CMS/NUBC assignment; the mapping approach needs only a TimelockController-gated `addClaimType()` call. TOB second-digit digit 9 (09XX) is currently RESERVED for national assignment (2025 NUBC manual) — not yet assigned. The sole modern precedent for a major new TOB category (OTP, 2020-2021) used TOB 087X (a third-digit sub-type under existing facility type 8), not digit 9; NUBC lead time was ~12 months from SUPPORT Act mandate to code effective date, rendering the 48-hour governance gap operationally irrelevant if cliqueue monitors NUBC annual update announcements (published July for January effective date). Solidity enum function-parameter panic (0x21) does NOT fire automatically when out-of-range uint is passed as a function argument (GitHub issue #13832, unresolved) — the `require` in the modifier is the only safety net. No emergency bypass role (BYPASSER_ROLE) is needed for claim-type registration. Initialize `registeredClaimTypes` at deployment for the 8 active second-digit values (1–4, 6–8); do NOT pre-register digit 9 (mapping default `false` already produces the correct submission-failure revert). — 2026-05-16 → [docs/research/market/validclaimtype-modifier-digit9-governance-gap.md](market/validclaimtype-modifier-digit9-governance-gap.md)

- Should cliqueue's hospital BAA explicitly list the `hospitalSalt` as a "Key Security Parameter" requiring HSM-grade storage — and should cliqueue's onboarding checklist include a salt backup verification step before activating the live Somnia integration? — added 2026-05-16, priority: high
- If a hospital loses its `hospitalSalt` through an IT failure (not a breach), can Somnia event logs (`ClaimSubmitted` events with `claimId` and `timestamp`) be used to reconstruct partial mappings by correlating timestamps with hospital billing system records — and should cliqueue spec a "salt recovery" workflow that allows partial mapping reconstruction without PHI exposure? — added 2026-05-16, priority: medium
- Should cliqueue publish a reference implementation of the hospital-local PostgreSQL mapping store (schema, pgcrypto setup, WAL replication config) as part of the open-source adapter toolkit — so hospital IT teams do not have to design the encrypted store from scratch — and would this lower the barrier to adoption at small/critical-access hospitals? — added 2026-05-16, priority: medium

- Should cliqueue anchor the `satisfied-pa-id` (X12 PA authorization number) as a `bytes32` hash in `ClaimSubmitted` when `paStatus = 0x02 (satisfied)` — creating an on-chain link between the PA approval and the claim without exposing the raw authorization number — and should the `satisfied-pa-id` hash be a second indexed topic or non-indexed data? → YES to anchoring; bytes32 paAuthHash as NON-INDEXED data (5th indexed topic is impossible in Solidity — max 4 topics including event signature). X12 REF*BB authorization number (ASC X12 005010X217 DE 128) is AN 1/30; CRD STU2.1 satisfied-pa-id is valueString with no format constraint (example: "Q8U119"). Constraint crd-ci-q5 requires satisfied-pa-id if and only if paStatus = satisfied. EDI carrier is REF*G1 (Prior Authorization Number) in 837 Loop 2300 — NOT K3 (Free-Form Text, which requires formal ASC X12 approval per X12 RFI #2206). Hash off-chain via keccak256(abi.encodePacked(PA_HASH_DOMAIN_SEPARATOR, satisfiedPaId)); contract enforces paAuthHash == bytes32(0) when paStatus ≠ 0x02. Gas delta: +5,120 on Somnia (32 bytes non-indexed data: 160×32=5,120); LOG4 event rises from 28,800 to 33,920 gas (~$0.000209/emission — negligible). No prior protocol publishes PA auth numbers as on-chain hashes — cliqueue would establish this pattern. — 2026-05-17 → [docs/research/somnia/satisfied-pa-id-on-chain-hash-claim-submitted.md](somnia/satisfied-pa-id-on-chain-hash-claim-submitted.md)

---

**See also** — [[topics/README|topic-hub index]] · this file is the *active* research backlog; once a question is investigated it lands in a single research file (linked above) and that file is hub-mapped from [[topics/README]].
