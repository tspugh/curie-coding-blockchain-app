# Specs

Fleshed-out specifications for what to build — one per file, named
`NNNN-kebab-title.md`. Every spec follows a standard structure (summary & user story,
requirements, technical documentation, deliverables, test cases, pass/fail criteria,
out of scope, open questions), enforced by the **`spec-author` skill**
(`.claude/skills/spec-author/`). Invoke it whenever writing or reviewing a spec.

Status is tracked inline below. Statuses: **done** (built and merged), **to implement**
(spec written, code not yet built), **draft** (spec still in motion).

- **done** · [`0001-mvp0-coverage-negotiation.md`](0001-mvp0-coverage-negotiation.md) — MVP0 coverage-exception negotiation loop.
- **done** · [`0002-demo-experience-and-integration-seam.md`](0002-demo-experience-and-integration-seam.md) — Live demo experience + FDA-label "gotcha" + CDS-Hooks seam.
- **to implement** · [`0003-token-flow-visibility.md`](0003-token-flow-visibility.md) — Design-decisions log for the UI surface: token-flow visibility (§2.1–§2.2), action coherence (§2.3), layout/error/parity polish (§2.4), wallet connection & onboarding (§2.5 — folded from former SPEC-0005), submit-amount gating by balance (§2.6), AI-reasoning + agent-receipt visibility (§2.7). §2.10 R48/R49 (real-mode adjudication visibility, merged from PR #14 on 2026-05-30); R49 carries a self-hosted attribution note (tick 123) that degrades the validator-`executionCost` dichotomy to a 3-state model under Amendment 0006. **R48 unblocked** (SPEC-0004 R25 Tick C landed tick 139; `verify-deploy` 8/8 PASS tick 142); R48 live verification still requires real-mode browser run. Other R-numbers landed.
- **to implement** · [`0004-data-and-evidence-model.md`](0004-data-and-evidence-model.md) — Shared-facts model: cases (one per payer line) + custom path, real per-line formulary, structured-slice evidence packet + Merkle root + Curie packet store (S3+Lambda), ladder labels with R14a sequencing only (R14b enforcement deferred to V1.5), §2.6 arbiter policy defaults, R2b multi-wallet config. R25 chose Amendment 0006 self-hosted path: **all Ticks A+B+C+D landed** (Amendment 0006 Adopted 2026-05-30); contract deployed at `0x2c561f339a0A15cf0550cb9a0880Bb341488ac93` with `selfHosted == true`; `verify-deploy` 8/8 PASS (tick 142). R26 repurposed: `scripts/check-ruling-abi.ts` build-time gate + hardhat mirror-test both landed. **R27 responsible-claim gate** remains in force until at least one real-mode `Settled` event is produced by the orchestrator — blocked on wallet refund to ~8 STT and/or `ANTHROPIC_API_KEY`. Amendment 0006 sprint: ticks 115–143.
- **to implement** · [`0005-usability-and-integration.md`](0005-usability-and-integration.md) — Usability criteria + the **headline integration gate**: full real-wallet, real-chain run from field-request to settled on Somnia testnet. Layout invariants verified by screenshot at named viewports (top-bar density, insurer Detail single-column, view-switch returns to Overview). Generalized N-user runtime registry; curated policy library + free-text override. R20-R23 added 2026-05-30: per-affordance integration coverage, approval+denial path coverage, real-chain LLM verification, pre-flight wallet sufficiency. **R20 + R21 + R23 sim-mode complete:** browser-verify 99/99 PASS across 22 scenarios, re-verified tick 138 against HEAD. **R22 real-mode verification blocked on wallet refund** (5.50 STT, need ~7.35 for full sweep) **and `ANTHROPIC_API_KEY`** (Tick C deploy no longer the blocker — done tick 139).

**To do next:** **Real-mode browser-verify** against deployed contract `0x2c561f33…488ac93` is the single outstanding gate. Two unblocking paths: (1) wallet refunded to ~8 STT for full 22-scenario sweep, or (2) `ANTHROPIC_API_KEY` set for a smaller Tick A live smoke (~0.5 STT, affordable at current 5.50 STT balance). Neither is in the loop's control. While blocked: steady-state maintenance only (coverage polish, doc hygiene, spec conformance checks).

See [`../../CLAUDE.md`](../../CLAUDE.md) for the full convention.
