# Specs

Fleshed-out specifications for what to build — one per file, named
`NNNN-kebab-title.md`. Every spec follows a standard structure (summary & user story,
requirements, technical documentation, deliverables, test cases, pass/fail criteria,
out of scope, open questions), enforced by the **`spec-author` skill**
(`.claude/skills/spec-author/`). Invoke it whenever writing or reviewing a spec.

Status is tracked inline below. Statuses: **done** (built and merged), **to implement**
(spec written, code not yet built), **draft** (spec still in motion).

- **done** · [`0001-mvp0-coverage-negotiation.md`](0001-mvp0-coverage-negotiation.md) — MVP0 coverage-exception negotiation loop.
- **done** · [`0002-demo-experience-and-integration-seam.md`](0002-demo-experience-and-integration-seam.md) — Live demo experience + FDA-label "gotcha" + CDS-Hooks seam.
- **to implement** · [`0003-token-flow-visibility.md`](0003-token-flow-visibility.md) — Design-decisions log for the UI surface: token-flow visibility (§2.1–§2.2), action coherence (§2.3), layout/error/parity polish (§2.4), wallet connection & onboarding (§2.5 — folded from former SPEC-0005), submit-amount gating by balance (§2.6), AI-reasoning + agent-receipt visibility (§2.7). §2.10 R48/R49 (real-mode adjudication visibility, merged from PR #14 on 2026-05-30); R49 carries a self-hosted attribution note (tick 123) that degrades the validator-`executionCost` dichotomy to a 3-state model under Amendment 0006. **R48 unblocked** (SPEC-0004 R25 Tick C landed tick 139; `verify-deploy` 8/8 PASS tick 142); R48 live verification still requires real-mode browser run. Other R-numbers landed. **§2.11 added 2026-06-01** (R50–R57): paged getLogs + deploymentBlock + JSONL hydration (R50–R52), Detail auto-refresh on event-for-active-reqId (R53), profile-switch preserves Detail (R54), Appeal-affordance gated on `state === Denied` (R55), terminal-state CTA (R56), per-party affordance set on Approved/Denied (R57) — distilled from the 2026-06-01 full-flow + dispute browser-verify runs.
- **to implement** · [`0004-data-and-evidence-model.md`](0004-data-and-evidence-model.md) — Shared-facts model: cases (one per payer line) + custom path, real per-line formulary, structured-slice evidence packet + Merkle root + Curie packet store (S3+Lambda), ladder labels with R14a sequencing only (R14b enforcement deferred to V1.5), §2.6 arbiter policy defaults, R2b multi-wallet config. R25 chose Amendment 0006 self-hosted path: **all Ticks A+B+C+D landed** (Amendment 0006 Adopted 2026-05-30); contract deployed at `0x2c561f339a0A15cf0550cb9a0880Bb341488ac93` with `selfHosted == true`; `verify-deploy` 8/8 PASS (tick 142). R26 repurposed: `scripts/check-ruling-abi.ts` build-time gate + hardhat mirror-test both landed. **R27 responsible-claim gate** remains in force until at least one real-mode `Settled` event is produced by the orchestrator — blocked on wallet refund to ~8 STT and/or `ANTHROPIC_API_KEY`. Amendment 0006 sprint: ticks 115–143. **§2.8 added 2026-06-01** (R28–R37): bad-policy `policyHash` detection in the orchestrator (R28); contract-side `appeal()` gate, agentFeeValue, round/appealRound increment (R29); `PolicyInvalidated` terminal-state invariants (R30); seven-affordance evidence-update set (R31); `providerAddr != insurerAddr` enforcement (R32); `ORCHESTRATOR_STUB_DECISION` env knob (R33); PHI loud-signature scanner (R34); `expected-outcome.md` Deny-before-Approve header pin (R35); `slice.kind` closed-enum invariant (R36); `Packet.submittedAt` typed as number (R37).
- **temporary violation ledger** · [`0004.1-temporary-compliance-repair-ledger.md`](0004.1-temporary-compliance-repair-ledger.md) — Fractional specs are ephemeral repair ledgers, not durable roadmap specs. This one lists SPEC-0001 through SPEC-0004 self-compliance gaps found on branch `spec-4-implementation`; each item must be folded into its owning durable spec and implementation, then this file must be deleted.
- **superseded** · [`0005-usability-and-integration.md`](0005-usability-and-integration.md) — **Folded into SPEC-0006 on 2026-06-01.** Body preserved for archaeology. Mapping recorded in SPEC-0005's status banner; SPEC-0006 §2.9–§2.14 carry the requirements.
- **approved** · [`0006-somnia-agent-platform-integration.md`](0006-somnia-agent-platform-integration.md) — **Revised 2026-06-01 to merge SPEC-0005 and add four hard requirements** from the dispute / bad-policy verification session (see `docs/progress/2026-06-01-dispute-and-bad-policy-flows.md`): (1) no off-chain AI, no stubs — banned everywhere (R0/R0a); (2) generalized per-negotiation evidence URL + prompt hint with six worked drug × indication examples (R14–R20); (3) live URL existence check (R21) + live agent invocation assertion (R22); (4) AI reasoning surfaced on-chain via new `RulingRationale` event + rendered in the case UI (R24–R26). Original SPEC-0006 R1–R16 (canonical platform address, `IAgentRequester` integration, ABI pinning, self-host retirement) retained. Now 47 R-numbered requirements. **R-OPEN-1–4 resolved 2026-06-03** via `scripts/identify-inference-agent.ts`: LLM Inference = `agentId 12847293847561029384`, `inferString` selector `0xfe7ca098`; `getRequestDeposit()` = 0.03 STT; `inferString` returns a single `string`. Implementation cascade now unblocked (branch `spec-6-implementation`).

- **approved** · [`0009-party-copilots-and-agent-chat-view.md`](0009-party-copilots-and-agent-chat-view.md) — Off-chain **party copilots** (LangGraph, TS) that act for provider/insurer through a controlled **tools layer** (incl. natural-language case intake, evidence-URL resolution, and completeness-gated filing — file-or-ask, then auto-watch), a conversational **agent chat view** (four message types — `agent`/`chain`/`tool`/`user` — with chain-state glyphs + finality stamps) that demotes today's high-detail views to an on-demand **technical drawer**, and **per-party auto-mode** (insurer-auto default) over the existing synthetic policy/case fixtures. Two AI layers, deliberately separated: the on-chain arbiter (SPEC-0006) stays the **judge**; copilots are **advocates** that *trigger* but never *supply* the ruling (R3 firewall + R4 SDK carve-out). Realizes the "autonomous policy-driven party agents" deferred in SPEC-0001 §7. *(SPEC-0007 and SPEC-0008 are in progress on separate branches; 0009 is the next free durable number in this branch.)*

**To do next:** SPEC-0006 open questions R-OPEN-1–4 are **resolved** (2026-06-03). The build cascade is in progress on branch `spec-6-implementation` via the `spec-implementation-loop`: contract rewrite (per-neg URL + hint + `_fireAgent` → LLM Inference `inferString` payload + 4-arg `handleResponse` + `RulingRationale` event), self-host + stub surface removal, drug-evidence map + six fixtures, redeploy, then the merged R44 headline gate exercises the new deploy against at least 2 of the 6 examples per run. SPEC-0004.1 violation ledger (R1–R12) is absorbed as the cascade lands its owning durable-spec items.

See [`../../CLAUDE.md`](../../CLAUDE.md) for the full convention.
