{
  "name": "curie-claims-contracts",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Hardhat workspace for the Curie Coverage Negotiation contract (Somnia testnet).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:somnia": "hardhat run scripts/deploy.ts --network somniaTestnet",
    "deploy:mock": "hardhat run scripts/deploy-mock.ts --network somniaTestnet",
    "admin:somnia": "hardhat run scripts/admin.ts --network somniaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  }
}
